<sql>


	<statement>
		alter table transactions add bill_id integer default 0;
	</statement>

	<statement>
		alter table transactions add item_id integer default 0;
	</statement>
	<statement>
		alter table transactions add p_id integer default 0;
	</statement>
	<statement>
		alter table transactions add p_remarks text;
	</statement>
	<statement>
		alter table transactions add p_amount real default 0;
	</statement>
	<statement>
		alter table transactions add p_status integer default 0;
	</statement>
	<statement>
		alter table transactions add p_ref_no text;
	</statement>
	<statement>
		alter table transactions add p_curr_id integer default 0;
	</statement>
	--cash_id integer default -3
	<statement>
		alter table transactions add cash_id integer default -3;
	</statement>
	<statement>
		alter table bills add cash_id integer default -3;
	</statement>
	<statement>
		alter table tr_p_temps add cash_id integer default -3;
	</statement>
	--fund_id integer default -13
	<statement>
		alter table transactions add fund_id integer default -13;
	</statement>

	--tr_type integer default 0
	<statement>
		alter table transactions add tr_type integer default 0;
	</statement>

	--##2024-08 temp index to speedup then drop
	<statement>CREATE INDEX if not exists transactions_bill_id_t on transactions(bill_id);</statement>

	<statement>
		update transactions set bill_id =0 where bill_id is null;
	</statement>
	<statement>
		--for discount amount in add amount screen
		alter table transactions add d_amount real default 0;
	</statement>
	<statement>
		--for discount amount in add amount screen
		alter table transactions add d_remarks text ;
	</statement>
	<statement>alter table transactions add curr_price real default 0</statement>
	<statement>alter table transactions add p_curr_price real default 0</statement>
	<statement>alter table bills add curr_price real default 0</statement>
	<statement>alter table items add curr_price real default 0</statement>
	<statement>alter table tr_p_temps add curr_price real default 0</statement>
	<statement>alter table tr_p_temps add p_curr_price real default 0</statement>
	<statement>alter table tr_p_temps add p_curr_id integer default 0</statement>
	<statement>alter table tr_p_temps add p_date text</statement>
	<statement>alter table tr_p_temps add tr_type integer default 0</statement>
	<statement>alter table transactions add p_date text</statement>
	<statement>alter table transactions add curr_mod integer default 0 </statement>
	<statement>alter table tr_p_temps add curr_mod integer default 0 </statement>
	<statement>
		alter table bills add bill_no2  integer  default 0;
	</statement>
	-- for Adjust Inventory
	<statement>
		alter table bills add adj_id integer default 0;
	</statement>
	<statement>
		alter table bills add adj_acc integer default 0;
	</statement>
	<statement>
		alter table bills add curr_mod integer default 0;
	</statement>
	<statement>
		alter table bills_h add curr_mod integer default 0;
	</statement>
	-- discount type

	<statement>create table if not exists discount_type (id integer primary
		key,name text unique);</statement>
	--2024-12 remove enter from name
	<statement>insert or ignore into discount_type(id,name) values(0,'نسبة مئوية');</statement>
	<statement>insert or ignore into discount_type(id,name) values(1,'مبلغ');</statement>

	<statement>alter table bills add discount_id integer
		default 0  references discount_type(id) ON UPDATE RESTRICT ON DELETE RESTRICT</statement>
	<statement>alter table bills add d_val real default 0</statement>

	<statement>alter table bills_h add discount_id integer
		default 0  references discount_type(id) ON UPDATE RESTRICT ON DELETE RESTRICT</statement>
	<statement>alter table bills_h add d_val real default 0</statement>


	<statement>alter table bills2 add discount_id integer
		default 0  references discount_type(id) ON UPDATE RESTRICT ON DELETE RESTRICT</statement>
	<statement>alter table bills2 add d_val real default 0</statement>

	--v195
	<statement>alter table bills add paid_amount real default 0</statement>
	<statement>alter table bills_h add paid_amount real default 0</statement>
	--v195
	<statement>alter table requests_bills add paid_amount real default 0</statement>


	--end

	--temp##v1.99
	<statement>
		update transactions set p_date=date_ where p_date is null and 0>=bill_id
		and (date(strftime('%Y-%m-%d',substr( date_,7,4)||'-' ||substr( date_,4,2)||'-'||substr( date_,1,2) )) !=
		(select ifnull(max(date_),date(date(strftime('%Y-%m-%d',substr(transactions.date_,7,4)||'-' ||substr(transactions.date_,4,2)||'-'||substr(transactions.date_,1,2) )),'-1 day'))
		from closing_year))
		and ROWID in(select b.rowid from transactions b where b.p_date is null and 0>=b.bill_id
		and (date(strftime('%Y-%m-%d',substr( b.date_,7,4)||'-' ||substr( b.date_,4,2)||'-'||substr( b.date_,1,2) )) !=
		(select ifnull(max(date_),date(date(strftime('%Y-%m-%d',substr( b.date_,7,4)||'-' ||substr( b.date_,4,2)||'-'||substr( b.date_,1,2) )),'-1 day'))
		from closing_year))
		limit 100);
	</statement>



	<statement>
		alter table customers add cus_type_id integer default 0 ;
	</statement>
	<statement>
		alter table customers add ADDRESS text ;
	</statement>
	<statement>
		alter table customers add vat_no text default '';
	</statement>

	<statement>
		update customers set gsm='' where gsm is null;
	</statement>
	<statement>alter table bill_transactions add d_amount real default 0</statement>
	<statement>alter table bill_transactions add remark text</statement>
	<statement>alter table items_temp add remark text</statement>
	<statement>alter table bill_transactions_h add remark text</statement>
	<statement>delete from bill_transactions where item_id is null</statement>

	<statement>
		drop view item_avg_cost;
	</statement>
	<statement>
		drop view item_avg_cost2;
	</statement>
	<statement>
		alter table items add o_cost real default 0
	</statement>
	<statement>
		alter table items add e_date text;
	</statement>
	<statement>
		alter table items add pic text;
	</statement>
	<statement>
		alter table items add remarks text;
	</statement>
	<statement>update bills set to_br_id=null where tr_type in(1,2) and to_br_id=0 </statement>
	<statement>create table IF NOT EXISTS tmp_real (r real)</statement>
	<statement>
		CREATE TABLE IF NOT EXISTS  `tran_type` (
		`id` INTEGER PRIMARY KEY,
		`name` TEXT unique,
		param1 text,
		param2 text
		)
	</statement>
	<statement>
		insert or ignore into  `tran_type` (id,name) values(0,'افتتاحي');
	</statement>
	<statement>
		insert or ignore into  `tran_type` (id,name) values(1,'بيع');
	</statement>
	<statement>
		insert or ignore into  `tran_type` (id,name) values(2,'شراء');
	</statement>
	<statement>
		insert or ignore into  `tran_type` (id,name) values(3,'تحويل مخزني');
	</statement>
	<statement>
		update  `tran_type` set name= 'تحويل مخزني' where id=3;
	</statement>
	<statement>
		insert or ignore into  `tran_type` (id,name) values(4,'تسوية مخزنية');
	</statement>

	<statement>
		insert or ignore into  `tran_type` (id,name) values(5,'قبض');
	</statement>

	<statement>
		insert or ignore into  `tran_type` (id,name) values(6,'صرف');
	</statement>

	<statement>
		insert or ignore into  `tran_type` (id,name) values(7,'جرد');
	</statement>

	<statement>
		insert or ignore into  `tran_type` (id,name) values(8,'عرض سعر');
	</statement>
	<statement>
		insert or ignore into  `tran_type` (id,name) values(9,'طلب شراء');
	</statement>
	--v1.101 curr sales
	<statement>
		insert or ignore into  `tran_type` (id,name) values(10,'بيع عملة');
	</statement>
	--change from 11 to 13 to avoid conflict with curr sales
	<statement>
		update  `tran_type` set name='صرف مخزني' where id=11;
	</statement>
	<statement>
		insert or ignore into  `tran_type` (id,name) values(13,'شراء عملة');
	</statement>
	<statement>
		update transactions set tr_type=13 where bill_id=-6 and tr_type=11;
	</statement>

	--v1.120 adj2
	<statement>
		insert or ignore into  `tran_type` (id,name) values(11,'صرف مخزني');
	</statement>
	<statement>
		insert or ignore into  `tran_type` (id,name) values(21,'توريد مخزني');
	</statement>

	<statement>
		insert or ignore into  `tran_type` (id,name) values(-1,'قيد افتتاحي');
	</statement>
	<statement>
		insert or ignore into  `tran_type` (id,name) values(-2,'قيود يومية');
	</statement>
	<statement>
		insert or ignore into  `tran_type` (id,name) values(-3,'سندات');
	</statement>
	<statement>
		insert or ignore into  `tran_type` (id,name) values(-4,'ضريبة');
	</statement>
	<statement>
		insert or ignore into  `tran_type` (id,name) values(-5,'رسوم');
	</statement>
	--v1.101 curr sales
	<statement>
		insert or ignore into  `tran_type` (id,name) values(-6,'عملات');
	</statement>
	<statement>
		CREATE TABLE IF NOT EXISTS  `bill_type` (
		`id` INTEGER PRIMARY KEY,
		`name` TEXT unique,
		param1 text,
		param2 text
		)
	</statement>
	<statement>
		insert or ignore into  `bill_type` (id,name) values(0,'حركة');
	</statement>
	<statement>
		insert or ignore into  `bill_type` (id,name) values(1,'نقد');
	</statement>
	<statement>
		insert or ignore into  `bill_type` (id,name) values(2,'آجل');
	</statement>


	<statement>
		CREATE TABLE IF NOT EXISTS  `cus_type` (
		`id` INTEGER PRIMARY KEY,
		`name` TEXT unique,
		param1 text,
		param2 text
		)
	</statement>
	<statement>
		insert or ignore into  `cus_type` (id,name) values(0,'عملاء');
	</statement>
	<statement>
		insert or ignore into  `cus_type` (id,name) values(1,'موردون');
	</statement>
	<statement>
		insert or ignore into  `cus_type` (id,name) values(2,'مبيعات ومشتريات');
	</statement>
	<statement>
		update cus_type set name='مبيعات ومشتريات' where id=2 and name='مبيعات';
	</statement>
	<statement>
		insert or ignore into  `cus_type` (id,name) values(4,'نقدية');
	</statement>

	<statement>
		insert or ignore into  `cus_type` (id,name) values(5,'مصروفات');
	</statement>
	<statement>
		insert or ignore into  `cus_type` (id,name) values(6,'إيرادات');
	</statement>
	<statement>
		insert or ignore into  `cus_type` (id,name) values(7,'أخرى');
	</statement>

	<statement>

		CREATE TABLE IF NOT EXISTS   branches
		(
		id     INTEGER PRIMARY KEY AUTOINCREMENT,
		name   text UNIQUE,
		date_              text default (strftime('%Y-%m-%d','now')),
		IS_ACTIVE            integer default 1,
		ADDRESS              text,
		gsm                text,
		remarks text,
		`param1`	TEXT,
		`param2`	TEXT
		);
	</statement>
	<statement>
		insert or ignore into branches (id,name) values(0,'المخزن');
	</statement>


	<statement>
		CREATE TABLE if not exists "customers" (
		`id`	INTEGER PRIMARY KEY AUTOINCREMENT,
		`name`	TEXT UNIQUE,
		`gsm`	TEXT,
		g_id INTEGER default 0,
		cus_type_id integer default 0 references cus_type(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		date_ text default (strftime('%Y-%m-%d','now')),
		ADDRESS              text,
		remarks text,
		`param1`	TEXT,
		`param2`	TEXT,
		`f1`	TEXT,
		`f2`	TEXT,
		`f3`	TEXT

		)
	</statement>
	<statement>
		insert or ignore into customers (id,cus_type_id,name) values(-9,2,'مردودات مبيعات آجل');
	</statement>
	<statement>
		insert or ignore into customers (id,cus_type_id,name) values(-10,2,'مردودات  مشتريات آجل');
	</statement>

	<statement>
		insert or ignore into customers (id,cus_type_id,name) values(-11,2,'مردودات  مبيعات نقدي');
	</statement>
	<statement>
		insert or ignore into customers (id,cus_type_id,name) values(-12,2,'مردودات  مشتريات نقدي');
	</statement>

	<statement>
		insert or ignore into customers (id,cus_type_id,name) values(-5,2,'مبيعات نقدي');
	</statement>
	<statement>
		insert or ignore into customers (id,cus_type_id,name) values(-6,2,'مشتريات نقدي');
	</statement>
	<statement>
		insert or ignore into customers (id,cus_type_id,name) values(-1,2,'مبيعات آجل');
	</statement>
	<statement>
		insert or ignore into customers (id,cus_type_id,name) values(-2,2,'مشتريات آجل');
	</statement>

	<statement>
		update customers set name='الصندوق1' where name='الصندوق' and id>0;
	</statement>
	<statement>
		insert or ignore into customers (id,cus_type_id,name) values(-3,4,'الصندوق');
	</statement>

	<statement>
		insert or ignore into customers (id,cus_type_id,name) values(-7,6,'الخصم المكتسب');
	</statement>

	<statement>
		insert or ignore into customers (id,cus_type_id,name) values(-8,5,'الخصم المسموح به');
	</statement>

	<statement>
		insert or ignore into customers (id,cus_type_id,name) values(-13,7,'رأس المال');
	</statement>
	<statement>
		insert or ignore into customers (id,cus_type_id,name) values(-14,7,'بضاعة أول المدة');
	</statement>
	<statement>
		insert or ignore into customers (id,cus_type_id,name,acc_p_id) values(-15,7,'عجز و زيادة البضاعة ',314);
	</statement>
	<statement>
		insert or ignore into customers (id,cus_type_id,name,acc_p_id) values(-16,7,'البضاعة التالفة',314);
	</statement>
	--v1.120 adj2
	--insert or ignore into customers (id,cus_type_id,name,acc_p_id) values(-29,7,' صرف وتوريد مخزني',314);
	--2024-11 move adj2 from -13 to -30 with tr_type 11 and 21
	<statement>
		insert or ignore into customers (id,cus_type_id,name,acc_p_id) values(-30,7,'تسوية المخزون-صرف وتوريد',314);
	</statement>

	<statement>
		insert or ignore into customers (id,cus_type_id,name,acc_p_id) values(-17,7,'ح/الارباح والخسائر',212);
	</statement>
	-- VAT feature
	<statement>
		insert or ignore into customers (id,cus_type_id,name,acc_p_id) values(-18,7,'ضريبة القيمة المضافة',22);
	</statement>
	<statement>
		update customers set name='الضريبة-' where name='الضريبة' ;
	</statement>
	<statement>
		update customers set name='الضريبة' where id=-18;
	</statement>

	<statement>
		update customers set name='الصندوق' where id=-3;
	</statement>
	<statement>
		update customers set name='الصندوق2' where id=-4;
	</statement>
	<statement>
		update customers set name='مبيعات آجل' where id=-1;
	</statement>
	<statement>
		update customers set name='مشتريات آجل' where id=-2;
	</statement>
	<statement>
		update customers set cus_type_id=2 where id=-2 and cus_type_id=3;
	</statement>


	<statement>
		CREATE TABLE if not exists "transactions" (
		`id`	INTEGER PRIMARY KEY AUTOINCREMENT,
		`cus_id`	INTEGER,
		`in`	TEXT,
		`out`	TEXT,
		`date_`	TEXT,
		`remarks`	TEXT,
		now_ TEXT,
		`param1`	TEXT,
		`param2`	TEXT,
		t_cus_id INTEGER ,
		`f1`	TEXT,
		`f2`	TEXT,
		`f3`	TEXT,
		curr_id integer default 0 references currency(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		bill_id integer default 0,
		p_id integer default 0,
		p_remarks text,
		p_amount real default 0,
		p_status integer default 0,
		p_ref_no text,
		d_amount real default 0,
		d_remarks text,
		p_curr_id integer default 0
		)
	</statement>
	<statement>
		CREATE INDEX if not exists transactions_cus_id on transactions(cus_id);
	</statement>
	<statement>
		CREATE INDEX if not exists transactions_t_cus_id on transactions(t_cus_id);
	</statement>
	<statement>
		CREATE INDEX if not exists transactions_curr_id on transactions(curr_id);
	</statement>

	<statement>
		create index if not exists cus_ind_g_id on customers(g_id);
	</statement>

	<statement>
		CREATE TABLE IF NOT EXISTS "valid" (

		`imei`	TEXT,
		`imei_code`	TEXT

		)
	</statement>
	<statement>
		CREATE TABLE if not exists "groups" (
		`id`	INTEGER PRIMARY KEY AUTOINCREMENT,
		`name`	TEXT UNIQUE,
		`param1`	TEXT,
		`param2`	TEXT
		)
	</statement>
	<statement>
		insert or ignore into groups(id,name) values(0,'عام');
	</statement>

	<statement>
		CREATE TABLE IF NOT EXISTS "currency" (
		`id`	INTEGER PRIMARY KEY AUTOINCREMENT,
		`name`	TEXT UNIQUE,
		curr_type integer default 1,
		`param1`	TEXT,
		`param2`	TEXT
		);
	</statement>
	<statement> alter table currency add curr_type integer default 1; </statement>
	<statement> alter table currency add fils_name text  </statement>
	<statement> alter table currency add code_name text  </statement>
	<statement>
		insert or ignore into currency (id,curr_type,name) values(0,0,'محلي');
	</statement>
	<statement>
		insert or ignore into currency (id,name) values(1,'دولار');
	</statement>
	<statement> update currency set curr_type=0 where id=0 </statement>
	<statement> update currency set fils_name='فلس' where id=0 and fils_name is null; </statement>
	<statement> update currency set fils_name='سنت' where id=1 and fils_name is null;</statement>
	<statement> update currency set code_name='YR' where id=0 and code_name is null; </statement>
	<statement> update currency set code_name='USD' where id=1 and code_name is null;</statement>
	--item type..represent item group header
	<statement>
		CREATE TABLE IF NOT EXISTS "item_type" (
		`id`	INTEGER PRIMARY KEY AUTOINCREMENT,
		`name`	TEXT UNIQUE,
		remarks text,
		`param1`	TEXT,
		`param2`	TEXT
		);
	</statement>
	<statement>
		insert or ignore into item_type (id,name) values(0,'عام');
	</statement>
	--item group
	<statement>
		CREATE TABLE IF NOT EXISTS "item_type_" (
		`id`	INTEGER PRIMARY KEY AUTOINCREMENT,
		`name`	TEXT UNIQUE,
		remarks text,
		`param1`	TEXT,
		`param2`	TEXT
		);
	</statement>
	<statement>
		insert or ignore into item_type_ (id,name) values(0,'سلعة');
	</statement>
	<statement>
		insert or ignore into item_type_ (id,name) values(1,'خدمة');
	</statement>
	<statement>
		CREATE TABLE IF NOT EXISTS items
		(
		`id`	INTEGER PRIMARY KEY AUTOINCREMENT,
		`name`	TEXT UNIQUE,
		item_type_id integer default 0 REFERENCES item_type(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		date_                    text default (strftime('%Y-%m-%d','now')),
		IS_ACTIVE                  integer default 1  ,
		o_qty real default 0,
		o_cost real default 0,
		o_br_id integer default 0,
		o_date text default (strftime('%Y-%m-%d','now')),
		e_date text,
		pic text,
		remarks text,
		qty real default 0,
		price real default 0,
		curr_id integer default 0,
		`param1`	TEXT,
		`param2`	TEXT
		)
		;
	</statement>
	<statement>
		alter table items add item_type_id_ integer default 0 REFERENCES item_type_(id);
	</statement>
	<statement>
		CREATE TABLE IF NOT EXISTS  `items_temp` (
		item_id integer references items(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		item_type_id integer references item_type(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		curr_id integer ,
		qty real default 0,
		price real default 0,
		no_ INTEGER PRIMARY KEY,
		remarks text

		)
	</statement>

	<statement>
		CREATE TABLE IF NOT EXISTS item_price
		(

		item_id integer REFERENCES items(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		curr_id integer REFERENCES currency(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		SLS_U_PRICE real,
		date_                    text default (strftime('%Y-%m-%d','now')) ,
		remarks text,
		`param1`	TEXT,
		`param2`	TEXT
		--, unique(item_id,curr_id)
		)
		;
	</statement>
	<statement>
		alter table item_price add unit_id integer  REFERENCES units(id) ON UPDATE RESTRICT ON DELETE RESTRICT;

	</statement>
	<statement>CREATE UNIQUE INDEX if not exists uq_item_price ON item_price(item_id,curr_id,unit_id);</statement>


	<statement>
		CREATE TABLE IF NOT EXISTS item_price_history
		(
		`id`	INTEGER PRIMARY KEY AUTOINCREMENT,
		item_id integer REFERENCES items(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		curr_id integer REFERENCES currency(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		sls_u_price real,
		date_                    text ,
		log_date text default (strftime('%Y-%m-%d','now')),
		remarks text,
		`param1`	TEXT,
		`param2`	TEXT
		)
		;
	</statement>
	<statement>
		alter table item_price_history add unit_id integer  REFERENCES units(id) ON UPDATE RESTRICT ON DELETE RESTRICT;

	</statement>
	<statement>
		CREATE TABLE IF NOT EXISTS "currency_price_temp" (
		`id`	INTEGER PRIMARY KEY AUTOINCREMENT,
		curr_id integer references currency(id),
		price real,
		`param1`	TEXT,
		`param2`	TEXT,
		unique(curr_id)
		);
	</statement>
	<statement>
		CREATE TABLE IF NOT EXISTS "currency_price" (
		`id`	INTEGER PRIMARY KEY AUTOINCREMENT,
		curr_id integer references currency(id),
		f_date text,
		t_date text,
		price real,
		is_new integer default 1,
		`param1`	TEXT,
		`param2`	TEXT,
		unique(curr_id,f_date)
		);
	</statement>
	-- #### also existing in fun.java

	--param3 used for closing starting from 2024-01
	<statement>
		CREATE TABLE IF NOT EXISTS bills
		(
		`id`	INTEGER PRIMARY KEY AUTOINCREMENT,
		date_            text default (strftime('%Y-%m-%d','now')),
		remarks         text,
		bill_no text,
		curr_id          integer default 0 references currency(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		bill_type integer default 0,
		tr_type integer references tran_type(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		is_back            integer default 0,
		br_id          integer default 0 references branches(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		cus_id integer  ,
		tran_status integer,
		to_br_id integer  references branches(id) ON UPDATE RESTRICT ON DELETE RESTRICT ,
		amount real default 0,
		d_amount real default 0,
		tax_amount             real default 0,
		param1 text,
		param2 text,
		param3 text

		);
	</statement>
	<statement>
		CREATE TABLE IF NOT EXISTS bill_transactions
		(
		bill_id                  integer references bills(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		item_id                    integer references items(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		item_type_id                    integer references item_type(id) ,
		curr_id                    integer references currency(id) ,
		qty                          integer,
		cost_price                    real default 0,
		sls_u_price               	real default 0,
		d_amount real default 0,
		param1 text,
		param2 text,
		param3 text,
		cash_id integer default -3,
		remark         text

		);
	</statement>
	--v1.136 sync
	<statement>CREATE TABLE trigger_flags (is_active INTEGER DEFAULT 1 UNIQUE);</statement>
	<statement>INSERT or ignore into trigger_flags (is_active) VALUES(1);</statement>

	<statement>
		CREATE TRIGGER if not exists items_delete after delete ON items
		BEGIN
		delete from transactions where item_id=OLD.id;
		END
	</statement>
	<statement>drop TRIGGER items_OP_insert</statement>
	<statement>
		CREATE TRIGGER items_OP_insert after insert ON items
		when (NEW.o_cost!=0 and NEW.o_qty!=0)
		BEGIN
		--delete from transactions where item_id=NEW.id and date_=strftime('%d-%m-%Y',NEW.o_date);
		delete from transactions where item_id=NEW.id ;
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,item_id,remarks,param2)
		values(-14,-13,
		1,NEW.o_qty*NEW.o_cost,NEW.curr_id,strftime('%d-%m-%Y',NEW.o_date),-4,NEW.id,
		(select name from tran_type where id=0)||'#'||NEW.name
		,
		strftime('%H:%M', datetime('now','localtime'))
		);
		END
	</statement>

	<statement>drop TRIGGER items_OP_update</statement>
	<statement>
		CREATE TRIGGER items_OP_update after update ON items
		when (( NEW.o_cost!=0 or OLD.o_cost!=0)
		--to allow change unit--2023-01-23
		--prevent update if unit_id is changed after closing year--2023-01-23
		and not (date(OLD.o_date)&#60;=
		(select ifnull(max(date_),date(OLD.o_date,'-1 day')) from closing_year))
		)


		BEGIN
		--delete from transactions where item_id=NEW.id and date_=strftime('%d-%m-%Y',OLD.o_date);
		delete from transactions where item_id=NEW.id ;
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,item_id,remarks,param2)
		select -14,-13,
		1,NEW.o_qty*NEW.o_cost,NEW.curr_id,strftime('%d-%m-%Y',NEW.o_date),-4,NEW.id,
		(select name from tran_type where id=0)||'#'||NEW.name
		,
		strftime('%H:%M', datetime('now','localtime'))
		where NEW.o_cost!=0;
		--delete if new cost is zero
		delete from transactions where item_id=NEW.id and date_=strftime('%d-%m-%Y',NEW.o_date) and [out]=0.0;
		END
	</statement>

	<statement>
		CREATE TRIGGER if not exists bills_delete after delete ON bills
		BEGIN
		delete from transactions where bill_id=OLD.id;
		END
	</statement>

	<statement>drop TRIGGER bills_purchase_insert</statement>
	<statement>
		CREATE TRIGGER bills_purchase_insert after insert ON bills
		when (NEW.tr_type=2 and NEW.bill_type=2 and NEW.is_back=0 and NEW.tax_amount=0
		and NEW.cost2=0)
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-2,NEW.cus_id,
		1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2

		);
		--v195
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		select NEW.cus_id,NEW.cash_id,
		1,NEW.paid_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		'من قيمة فاتورة مشتريات#'
		||NEW.bill_no2
		,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2
		where NEW.paid_amount!=0;
		END
	</statement>

	<statement>drop TRIGGER bills_purchase_insert_t</statement>
	<statement>
		CREATE TRIGGER bills_purchase_insert_t after insert ON bills
		when (NEW.tr_type=2 and NEW.bill_type=2 and NEW.is_back=0 and
		(NEW.tax_amount!=0 or NEW.cost2!=0) )
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-2,NEW.tr_type,
		1,NEW.amount-NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2 );

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cus_id,NEW.tr_type,
		-1,NEW.amount-NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2 );

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cash_id,-5,
		-1,NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks||'_'||
		'مقابل رسوم فاتورة مشتريات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2 );

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-18,-4,
		1,NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks||'_'||
		'مقابل ضريبة فاتورة مشتريات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2 );

		--v195
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		select NEW.cus_id,NEW.cash_id,
		1,NEW.paid_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		'من قيمة فاتورة مشتريات#'
		||NEW.bill_no2
		,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2
		where NEW.paid_amount!=0;
		delete from transactions where bill_id=NEW.id and [out]=0.0;

		END
	</statement>

	<statement>drop TRIGGER bills_purchase_insert_is_back</statement>
	<statement>
		CREATE TRIGGER bills_purchase_insert_is_back after insert ON bills
		when (NEW.tr_type=2 and NEW.bill_type=2 and NEW.is_back=1 and NEW.tax_amount=0
		and NEW.cost2=0)
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(t_cus_id,cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-10,NEW.cus_id,
		1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2

		);
		--v195
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		select NEW.cash_id,NEW.cus_id,
		1,NEW.paid_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		'من قيمة فاتورة مرتجع مشتريات#'
		||NEW.bill_no2
		,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2
		where NEW.paid_amount!=0;
		END
	</statement>

	<statement>drop TRIGGER bills_purchase_insert_is_back_t</statement>
	<statement>
		CREATE TRIGGER bills_purchase_insert_is_back_t after insert ON bills
		when (NEW.tr_type=2 and NEW.bill_type=2 and NEW.is_back=1
		and (NEW.cost2!=0 or NEW.tax_amount!=0))
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-10,NEW.tr_type,
		-1,NEW.amount-NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cus_id,NEW.tr_type,
		1,NEW.amount-NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cash_id,-5,
		1,NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks||'_'||
		'مقابل رسوم مرتجع مشتريات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-18,-4,
		-1,NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks||'_'||
		'مقابل ضريبة مرتجع مشتريات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		--v195
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		select NEW.cash_id,NEW.cus_id,
		1,NEW.paid_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		'من قيمة فاتورة مرتجع مشتريات#'
		||NEW.bill_no2
		,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2
		where NEW.paid_amount!=0;

		delete from transactions where bill_id=NEW.id and [out]=0.0;


		END
	</statement>


	<statement>drop TRIGGER bills_sales_insert</statement>
	<statement>
		CREATE TRIGGER bills_sales_insert after insert ON bills
		when (NEW.tr_type=1 and NEW.bill_type=2 and NEW.is_back=0 and NEW.tax_amount=0
		and NEW.cost2=0)
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cus_id,-1,
		1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2

		);
		--v195
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		select NEW.cash_id,NEW.cus_id,
		1,NEW.paid_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		'من قيمة فاتورة مبيعات#'
		||NEW.bill_no2
		,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2
		where NEW.paid_amount!=0;

		END
	</statement>

	<statement>drop TRIGGER bills_sales_insert_t</statement>
	<statement>
		CREATE TRIGGER bills_sales_insert_t after insert ON bills
		when (NEW.tr_type=1 and NEW.bill_type=2 and NEW.is_back=0 and
		(NEW.cost2!=0 or NEW.tax_amount!=0 ))
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cus_id,NEW.tr_type,
		1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-1,NEW.tr_type,
		-1,NEW.amount-NEW.tax_amount-NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-18,-4,
		-1,NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks||'_'||
		'مقابل ضريبة فاتورة مبيعات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cost_id,-5,
		-1,NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks||'_'||
		'مقابل رسوم فاتورة مبيعات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		--v195
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		select NEW.cash_id,NEW.cus_id,
		1,NEW.paid_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		'من قيمة فاتورة مبيعات#'
		||NEW.bill_no2
		,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2
		where NEW.paid_amount!=0;



		delete from transactions where bill_id=NEW.id and [out]=0.0;


		END
	</statement>

	<statement>drop TRIGGER bills_sales_insert_is_back</statement>
	<statement>
		CREATE TRIGGER bills_sales_insert_is_back after insert ON bills
		when (NEW.tr_type=1 and NEW.bill_type=2 and NEW.is_back=1 and NEW.tax_amount=0
		and NEW.cost2=0)
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(t_cus_id,cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cus_id,-9,
		1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2

		);

		--v195
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		select NEW.cus_id,NEW.cash_id,
		1,NEW.paid_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		'من قيمة فاتورة مرتجع مبيعات#'
		||NEW.bill_no2
		,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2
		where NEW.paid_amount!=0;

		END
	</statement>

	<statement>drop TRIGGER bills_sales_insert_is_back_t</statement>
	<statement>
		CREATE TRIGGER bills_sales_insert_is_back_t after insert ON bills
		when (NEW.tr_type=1 and NEW.bill_type=2 and NEW.is_back=1 and
		(NEW.cost2!=0 or NEW.tax_amount!=0 ))
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cus_id,NEW.tr_type,
		-1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-9,NEW.tr_type,
		1,NEW.amount-NEW.tax_amount-NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-18,-4,
		1,NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks||'_'||
		'مقابل ضريبة مرتجع مبيعات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cost_id,-5,
		1,NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks||'_'||
		'مقابل رسوم مرتجع مبيعات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		--v195
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		select NEW.cus_id,NEW.cash_id,
		1,NEW.paid_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		'من قيمة فاتورة مرتجع مبيعات#'
		||NEW.bill_no2
		,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2
		where NEW.paid_amount!=0;



		delete from transactions where bill_id=NEW.id and [out]=0.0;


		END
	</statement>



	<statement>drop  TRIGGER bills_sales_insert_cash</statement>
	<statement>
		CREATE TRIGGER bills_sales_insert_cash after insert ON bills
		when ( NEW.tr_type=1 and NEW.bill_type=1 and NEW.is_back=0 and NEW.tax_amount=0
		and NEW.cost2=0)
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cash_id,-5,
		1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2

		);
		END
	</statement>

	<statement>drop  TRIGGER bills_sales_insert_cash_t</statement>
	<statement>
		CREATE TRIGGER bills_sales_insert_cash_t after insert ON bills
		when ( NEW.tr_type=1 and NEW.bill_type=1 and NEW.is_back=0 and
		(NEW.cost2!=0 or NEW.tax_amount!=0 or NEW.r_cost!=0))
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cash_id,NEW.tr_type,
		1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-5,NEW.tr_type,
		-1,NEW.amount-NEW.tax_amount-NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-18,-4,
		-1,NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||	NEW.remarks||'_'||
		'مقابل ضريبة فاتورة مبيعات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cost_id,-5,
		-1,NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||	'#'||NEW.remarks||'_'||
		'مقابل رسوم فاتورة مبيعات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);




		delete from transactions where bill_id=NEW.id and [out]=0.0;

		END
	</statement>


	<statement>drop  TRIGGER bills_sales_insert_cash_is_back</statement>
	<statement>
		CREATE TRIGGER bills_sales_insert_cash_is_back after insert ON bills
		when ( NEW.tr_type=1 and NEW.bill_type=1 and NEW.is_back=1 and NEW.tax_amount=0
		and NEW.cost2=0)
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(t_cus_id,cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cash_id,-11,
		1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2

		);
		END
	</statement>

	<statement>drop  TRIGGER bills_sales_insert_cash_is_back_t</statement>
	<statement>
		CREATE TRIGGER bills_sales_insert_cash_is_back_t after insert ON bills
		when ( NEW.tr_type=1 and NEW.bill_type=1 and NEW.is_back=1 and
		(NEW.cost2!=0 or NEW.tax_amount!=0 ))
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cash_id,NEW.tr_type,
		-1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-11,NEW.tr_type,
		1,NEW.amount-NEW.tax_amount-NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-18,-4,
		1,NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||	NEW.remarks||'_'||
		'مقابل ضريبة مرتجع مبيعات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cost_id,-5,
		1,NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||	NEW.remarks||'_'||
		'مقابل رسوم مرتجع مبيعات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);


		delete from transactions where bill_id=NEW.id and [out]=0.0;




		END
	</statement>

	<statement>drop TRIGGER bills_purchase_insert_cash</statement>
	<statement>
		CREATE TRIGGER bills_purchase_insert_cash after insert ON bills
		when ( NEW.tr_type=2 and NEW.bill_type=1 and NEW.is_back=0 and NEW.tax_amount=0
		and NEW.cost2=0)
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-6,NEW.cash_id,
		1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2

		);
		END
	</statement>

	<statement>drop TRIGGER bills_purchase_insert_cash_t</statement>
	<statement>
		CREATE TRIGGER bills_purchase_insert_cash_t after insert ON bills
		when ( NEW.tr_type=2 and NEW.bill_type=1 and NEW.is_back=0 and
		(NEW.cost2!=0 or NEW.tax_amount!=0 ))
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-6,NEW.tr_type,
		1,NEW.amount-NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cash_id,NEW.tr_type,
		-1,NEW.amount-NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-18,-4,
		1,NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||	NEW.remarks||'_'||
		'مقابل ضريبة فاتورة مشتريات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cash_id,-5,
		-1,NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||	NEW.remarks||'_'||
		'مقابل رسوم فاتورة مشتريات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		delete from transactions where bill_id=NEW.id and [out]=0.0;

		END
	</statement>

	<statement>drop TRIGGER bills_purchase_insert_cash_is_back</statement>
	<statement>
		CREATE TRIGGER bills_purchase_insert_cash_is_back after insert ON bills
		when ( NEW.tr_type=2 and NEW.bill_type=1 and NEW.is_back=1 and NEW.tax_amount=0
		and NEW.cost2=0)
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(t_cus_id,cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-12,NEW.cash_id,
		1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2

		);
		END
	</statement>

	<statement>drop TRIGGER bills_purchase_insert_cash_is_back_t</statement>
	<statement>
		CREATE TRIGGER bills_purchase_insert_cash_is_back_t after insert ON bills
		when ( NEW.tr_type=2 and NEW.bill_type=1 and NEW.is_back=1 and
		(NEW.cost2!=0 or NEW.tax_amount!=0 ))
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cash_id,NEW.tr_type,
		1,NEW.amount-NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-12,NEW.tr_type,
		-1,NEW.amount-NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-18,-4,
		-1,NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||	NEW.remarks||'_'||
		'مقابل ضريبة مرتجع مشتريات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cash_id,-5,
		1,NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||	NEW.remarks||'_'||
		'مقابل رسوم مرتجع مشتريات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);
		delete from transactions where bill_id=NEW.id and [out]=0.0;



		END
	</statement>

	<statement>drop TRIGGER bills_purchase_update_cash</statement>
	<statement>
		CREATE TRIGGER bills_purchase_update_cash after update ON bills
		when ( NEW.tr_type=2 and NEW.bill_type=1 and NEW.is_back=0 and NEW.tax_amount=0
		and NEW.cost2=0 and (select is_active from trigger_flags)=1 )
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-6,NEW.cash_id,
		1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2

		);
		END
	</statement>

	<statement>drop TRIGGER bills_purchase_update_cash_t</statement>
	<statement>
		CREATE TRIGGER bills_purchase_update_cash_t after update ON bills
		when ( NEW.tr_type=2 and NEW.bill_type=1 and NEW.is_back=0 and
		(NEW.cost2!=0 or NEW.tax_amount!=0) and (select is_active from trigger_flags)=1 )
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-6,NEW.tr_type,
		1,NEW.amount-NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cash_id,NEW.tr_type,
		-1,NEW.amount-NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cash_id,-5,
		-1,NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks||'_'||
		'مقابل رسوم فاتورة مشتريات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2 );

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-18,-4,
		1,NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks||'_'||
		'مقابل ضريبة فاتورة مشتريات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2 );

		delete from transactions where bill_id=NEW.id and [out]=0.0;

		END
	</statement>

	<statement>drop TRIGGER bills_purchase_update_cash_is_back</statement>
	<statement>
		CREATE TRIGGER bills_purchase_update_cash_is_back after update ON bills
		when ( NEW.tr_type=2 and NEW.bill_type=1 and NEW.is_back=1 and NEW.tax_amount=0
		and NEW.cost2=0 and (select is_active from trigger_flags)=1 )
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(t_cus_id,cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-12,NEW.cash_id,
		1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2

		);
		END
	</statement>

	<statement>drop TRIGGER bills_purchase_update_cash_is_back_t</statement>
	<statement>
		CREATE TRIGGER bills_purchase_update_cash_is_back_t after update ON bills
		when ( NEW.tr_type=2 and NEW.bill_type=1 and NEW.is_back=1 and
		(NEW.cost2!=0 or NEW.tax_amount!=0) and (select is_active from trigger_flags)=1 )
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cash_id,NEW.tr_type,
		1,NEW.amount-NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-12,NEW.tr_type,
		-1,NEW.amount-NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cash_id,-5,
		1,NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks||'_'||
		'مقابل رسوم مرتجع مشتريات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-18,-4,
		-1,NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks||'_'||
		'مقابل ضريبة مرتجع مشتريات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		delete from transactions where bill_id=NEW.id and [out]=0.0;


		END
	</statement>


	<statement>drop TRIGGER bills_purchase_update_post</statement>
	<statement>
		CREATE TRIGGER bills_purchase_update_post after update ON bills
		when ( NEW.tr_type=2 and NEW.bill_type=2 and NEW.is_back=0 and NEW.tax_amount=0
		and NEW.cost2=0 and (select is_active from trigger_flags)=1 )
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-2,NEW.cus_id,
		1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2

		);

		--v195
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		select NEW.cus_id,NEW.cash_id,
		1,NEW.paid_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		'من قيمة فاتورة مشتريات#'
		||NEW.bill_no2
		,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2
		where NEW.paid_amount!=0;


		END
	</statement>

	<statement>drop TRIGGER bills_purchase_update_post_t</statement>
	<statement>
		CREATE TRIGGER bills_purchase_update_post_t after update ON bills
		when ( NEW.tr_type=2 and NEW.bill_type=2 and NEW.is_back=0 and
		(NEW.cost2!=0 or NEW.tax_amount!=0) and (select is_active from trigger_flags)=1 )
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-2,NEW.tr_type,
		1,NEW.amount-NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2 );

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cus_id,NEW.tr_type,
		-1,NEW.amount-NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2 );

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cash_id,-5,
		-1,NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks||'_'||
		'مقابل رسوم فاتورة مشتريات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2 );

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-18,-4,
		1,NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks||'_'||
		'مقابل ضريبة فاتورة مشتريات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2 );

		--v195
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		select NEW.cus_id,NEW.cash_id,
		1,NEW.paid_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		'من قيمة فاتورة مشتريات#'
		||NEW.bill_no2
		,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2
		where NEW.paid_amount!=0;

		delete from transactions where bill_id=NEW.id and [out]=0.0;


		END
	</statement>

	<statement>drop TRIGGER bills_purchase_update_post_is_back</statement>
	<statement>
		CREATE TRIGGER bills_purchase_update_post_is_back after update ON bills
		when ( NEW.tr_type=2 and NEW.bill_type=2 and NEW.is_back=1 and NEW.tax_amount=0
		and NEW.cost2=0  and (select is_active from trigger_flags)=1 )
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(t_cus_id,cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-10,NEW.cus_id,
		1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2

		);

		--v195
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		select NEW.cash_id,NEW.cus_id,
		1,NEW.paid_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		'من قيمة فاتورة مرتجع مشتريات#'
		||NEW.bill_no2
		,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2
		where NEW.paid_amount!=0;

		END
	</statement>

	<statement>drop TRIGGER bills_purchase_update_post_is_back_t</statement>
	<statement>
		CREATE TRIGGER bills_purchase_update_post_is_back_t after update ON bills
		when ( NEW.tr_type=2 and NEW.bill_type=2 and NEW.is_back=1 and
		(NEW.cost2!=0 or NEW.tax_amount!=0) and (select is_active from trigger_flags)=1 )
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-10,NEW.tr_type,
		-1,NEW.amount-NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cus_id,NEW.tr_type,
		1,NEW.amount-NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cash_id,-5,
		1,NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks||'_'||
		'مقابل رسوم مرتجع مشتريات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-18,-4,
		-1,NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks||'_'||
		'مقابل ضريبة مرتجع مشتريات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		--v195
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		select NEW.cash_id,NEW.cus_id,
		1,NEW.paid_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		'من قيمة فاتورة مرتجع مشتريات#'
		||NEW.bill_no2
		,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2
		where NEW.paid_amount!=0;

		delete from transactions where bill_id=NEW.id and [out]=0.0;



		END
	</statement>

	<statement>drop  TRIGGER bills_sales_update_cash</statement>
	<statement>
		CREATE TRIGGER bills_sales_update_cash after update ON bills
		when ( NEW.tr_type=1 and NEW.bill_type=1 and NEW.is_back=0 and NEW.tax_amount=0
		and NEW.cost2=0 and (select is_active from trigger_flags)=1 )
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cash_id,-5,
		1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2

		);
		END
	</statement>

	<statement>drop  TRIGGER bills_sales_update_cash_t</statement>
	<statement>
		CREATE TRIGGER bills_sales_update_cash_t after update ON bills
		when ( NEW.tr_type=1 and NEW.bill_type=1 and NEW.is_back=0 and
		(NEW.cost2!=0 or NEW.tax_amount!=0 ) and (select is_active from trigger_flags)=1 )
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cash_id,NEW.tr_type,
		1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-5,NEW.tr_type,
		-1,NEW.amount-NEW.tax_amount-NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-18,-4,
		-1,NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||	NEW.remarks||'_'||
		'مقابل ضريبة فاتورة مبيعات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cost_id,-5,
		-1,NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||NEW.remarks||'_'||
		'مقابل رسوم فاتورة مبيعات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);




		delete from transactions where bill_id=NEW.id and [out]=0.0;


		END
	</statement>

	<statement>drop  TRIGGER bills_sales_update_cash_is_back</statement>
	<statement>
		CREATE TRIGGER bills_sales_update_cash_is_back after update ON bills
		when ( NEW.tr_type=1 and NEW.bill_type=1 and NEW.is_back=1 and NEW.tax_amount=0
		and NEW.cost2=0 and (select is_active from trigger_flags)=1 )
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(t_cus_id,cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cash_id,-11,
		1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2

		);
		END
	</statement>

	<statement>drop  TRIGGER bills_sales_update_cash_is_back_t</statement>
	<statement>
		CREATE TRIGGER bills_sales_update_cash_is_back_t after update ON bills
		when ( NEW.tr_type=1 and NEW.bill_type=1 and NEW.is_back=1 and
		(NEW.cost2!=0 or NEW.tax_amount!=0 ) and (select is_active from trigger_flags)=1 )
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cash_id,NEW.tr_type,
		-1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-11,NEW.tr_type,
		1,NEW.amount-NEW.tax_amount-NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-18,-4,
		1,NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||NEW.remarks||'_'||
		'مقابل ضريبة مرتجع مبيعات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cost_id,-5,
		1,NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		(select name from customers where id=NEW.cus_id)||'#'||NEW.remarks||'_'||
		'مقابل رسوم مرتجع مبيعات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);


		delete from transactions where bill_id=NEW.id and [out]=0.0;

		END
	</statement>

	<statement>drop  TRIGGER bills_sales_update_post</statement>
	<statement>
		CREATE TRIGGER bills_sales_update_post after update ON bills
		when ( NEW.tr_type=1 and NEW.bill_type=2 and NEW.is_back=0 and NEW.tax_amount=0
		and NEW.cost2=0 and (select is_active from trigger_flags)=1 )
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cus_id,-1,
		1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2

		);
		--v195
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		select NEW.cash_id,NEW.cus_id,
		1,NEW.paid_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		'من قيمة فاتورة مبيعات#'
		||NEW.bill_no2
		,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2
		where NEW.paid_amount!=0;

		END
	</statement>

	<statement>drop  TRIGGER bills_sales_update_post_t</statement>
	<statement>
		CREATE TRIGGER bills_sales_update_post_t after update ON bills
		when ( NEW.tr_type=1 and NEW.bill_type=2 and NEW.is_back=0 and
		(NEW.cost2!=0 or NEW.tax_amount!=0 ) and (select is_active from trigger_flags)=1 )
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cus_id,NEW.tr_type,
		1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-1,NEW.tr_type,
		-1,NEW.amount-NEW.tax_amount-NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-18,-4,
		-1,NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks||'_'||
		'مقابل ضريبة فاتورة مبيعات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cost_id,-5,
		-1,NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks||'_'||
		'مقابل رسوم فاتورة مبيعات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		--v195
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		select NEW.cash_id,NEW.cus_id,
		1,NEW.paid_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		'من قيمة فاتورة مبيعات#'
		||NEW.bill_no2
		,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2
		where NEW.paid_amount!=0;



		delete from transactions where bill_id=NEW.id and [out]=0.0;


		END
	</statement>

	<statement>drop  TRIGGER bills_sales_update_post_is_back</statement>
	<statement>
		CREATE TRIGGER bills_sales_update_post_is_back after update ON bills
		when ( NEW.tr_type=1 and NEW.bill_type=2 and NEW.is_back=1 and NEW.tax_amount=0
		and NEW.cost2=0 and (select is_active from trigger_flags)=1 )
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(t_cus_id,cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cus_id,-9,
		1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2

		);

		--v195
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		select NEW.cus_id,NEW.cash_id,
		1,NEW.paid_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		'من قيمة فاتورة مرتجع مبيعات#'
		||NEW.bill_no2
		,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2
		where NEW.paid_amount!=0;
		END
	</statement>

	<statement>drop  TRIGGER bills_sales_update_post_is_back_t</statement>
	<statement>
		CREATE TRIGGER bills_sales_update_post_is_back_t after update ON bills
		when ( NEW.tr_type=1 and NEW.bill_type=2 and NEW.is_back=1 and
		(NEW.cost2!=0 or NEW.tax_amount!=0 ) and (select is_active from trigger_flags)=1 )
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cus_id,NEW.tr_type,
		-1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-9,NEW.tr_type,
		1,NEW.amount-NEW.tax_amount-NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-18,-4,
		1,NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks||'_'||
		'مقابل ضريبة مرتجع مبيعات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cost_id,-5,
		1,NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks||'_'||
		'مقابل رسوم مرتجع مبيعات#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		--v195
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		select NEW.cus_id,NEW.cash_id,
		1,NEW.paid_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		'من قيمة فاتورة مرتجع مبيعات#'
		||NEW.bill_no2
		,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2
		where NEW.paid_amount!=0;


		delete from transactions where bill_id=NEW.id and [out]=0.0;

		END
	</statement>

	--v1.120 adj2
	--###Begin
	<statement>drop TRIGGER bills_inv_out_insert</statement>
	<statement>
		CREATE TRIGGER bills_inv_out_insert after insert ON bills
		when (NEW.tr_type=11 and NEW.bill_type=2 and NEW.is_back=0 and NEW.tax_amount=0
		and NEW.cost2=0 and NEW.r_cost=0)
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no,tr_type)
		values(NEW.cus_id,-30,
		1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2,NEW.tr_type

		);
		END
	</statement>
	<statement>drop TRIGGER bills_inv_out_insert_t</statement>
	<statement>
		CREATE TRIGGER bills_inv_out_insert_t after insert ON bills
		when (NEW.tr_type=11 and NEW.bill_type=2 and NEW.is_back=0
		and (NEW.cost2!=0 or NEW.tax_amount!=0 or NEW.r_cost!=0))
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cus_id,NEW.tr_type,
		1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);



		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-30,NEW.tr_type,
		-1,NEW.amount-NEW.tax_amount-NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);


		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-18,-4,
		-1,NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks||'_'||
		'مقابل ضريبة صرف مخزني#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cash_id,-5,
		-1,NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks||'_'||
		'مقابل رسوم صرف مخزني#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);


		delete from transactions where bill_id=NEW.id and [out]=0.0;

		END
	</statement>
	<statement>drop  TRIGGER bills_inv_out_update_post</statement>
	<statement>
		CREATE TRIGGER bills_inv_out_update_post after update ON bills
		when ( NEW.tr_type=11 and NEW.bill_type=2 and NEW.is_back=0 and NEW.tax_amount=0
		and NEW.cost2=0 and NEW.r_cost=0 and (select is_active from trigger_flags)=1 )
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no,tr_type)
		values(NEW.cus_id,-30,
		1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2,NEW.tr_type

		);
		END
	</statement>

	<statement>drop  TRIGGER bills_inv_out_update_post_t</statement>
	<statement>
		CREATE TRIGGER bills_inv_out_update_post_t after update ON bills
		when (NEW.tr_type=11 and NEW.bill_type=2 and NEW.is_back=0
		and (NEW.cost2!=0 or NEW.tax_amount!=0 or NEW.r_cost!=0) and (select is_active from trigger_flags)=1 )
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cus_id,NEW.tr_type,
		1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-30,NEW.tr_type,
		-1,NEW.amount-NEW.tax_amount-NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);


		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-18,-4,
		-1,NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks||'_'||
		'مقابل ضريبة صرف مخزني#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cash_id,-5,
		-1,NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks||'_'||
		'مقابل رسوم صرف مخزني#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2);


		delete from transactions where bill_id=NEW.id and [out]=0.0;


		END
	</statement>

	-- Inv In-out(11,21)
	<statement>drop TRIGGER bills_inv_in_insert</statement>
	<statement>
		CREATE TRIGGER bills_inv_in_insert after insert ON bills
		when (NEW.tr_type=21 and NEW.bill_type=2 and NEW.is_back=0 and NEW.tax_amount=0
		and NEW.cost2=0)
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no,tr_type)
		values(-30,NEW.cus_id,
		1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2,NEW.tr_type

		);
		END
	</statement>

	<statement>drop TRIGGER bills_inv_in_insert_t</statement>
	<statement>
		CREATE TRIGGER bills_inv_in_insert_t after insert ON bills
		when (NEW.tr_type=21 and NEW.bill_type=2 and NEW.is_back=0
		and (NEW.cost2!=0 or NEW.tax_amount!=0)
		)
		BEGIN
		delete from transactions where bill_id=NEW.id;

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-30,NEW.tr_type,
		1,NEW.amount-NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2 );

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cus_id,NEW.tr_type,
		-1,NEW.amount-NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2 );

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cash_id,-5,
		-1,NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks||'_'||
		'مقابل رسوم توريد مخزني#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2 );



		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-18,-4,
		1,NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks||'_'||
		'مقابل ضريبة توريد مخزني#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2 );

		delete from transactions where bill_id=NEW.id and [out]=0.0;

		END
	</statement>
	<statement>drop TRIGGER bills_inv_in_update_post</statement>
	<statement>
		CREATE TRIGGER bills_inv_in_update_post after update ON bills
		when ( NEW.tr_type=21 and NEW.bill_type=2 and NEW.is_back=0 and NEW.tax_amount=0
		and NEW.cost2=0 and (select is_active from trigger_flags)=1 )
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no,tr_type)
		values(-30,NEW.cus_id,
		1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2,NEW.tr_type

		);
		END
	</statement>

	<statement>drop TRIGGER bills_inv_in_update_post_t</statement>
	<statement>
		CREATE TRIGGER bills_inv_in_update_post_t after update ON bills
		when (NEW.tr_type=21 and NEW.bill_type=2 and NEW.is_back=0
		and (NEW.cost2!=0 or NEW.tax_amount!=0)
		and (select is_active from trigger_flags)=1 )
		BEGIN
		delete from transactions where bill_id=NEW.id;

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-30,NEW.tr_type,
		1,NEW.amount-NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2 );

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cus_id,NEW.tr_type,
		-1,NEW.amount-NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2 );

		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(NEW.cash_id,-5,
		-1,NEW.cost2,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks||'_'||
		'مقابل رسوم توريد مخزني#'
		||NEW.bill_no2
		,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2 );



		insert or ignore into transactions(cus_id,tr_type,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no)
		values(-18,-4,
		1,NEW.tax_amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks||'_'||
		'مقابل ضريبة توريد مخزني#'
		||NEW.bill_no2,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2 );

		delete from transactions where bill_id=NEW.id and [out]=0.0;

		END
	</statement>


	--### End v1.120 adj2



	--Adj Bills
	<statement>drop TRIGGER if exists bills_adj_insert</statement>
	<statement>drop  TRIGGER if exists bills_adj_update</statement>
	<statement>
		create table if not exists revenue_type (id integer primary key,name text);
	</statement>
	<statement>
		insert or ignore into revenue_type (id,name) values(1,'المخزون الافتتاحي');
	</statement>
	<statement>
		insert or ignore into revenue_type (id,name) values(2,'المخزون المتبقي');
	</statement>
	<statement>
		insert or ignore into revenue_type (id,name) values(3,'صافي المشتريات');
	</statement>
	<statement>
		insert or ignore into revenue_type (id,name) values(4,'صافي المبيعات');
	</statement>
	<statement>
		update revenue_type set name='تكلفة المبيعات' where id=2;
	</statement>
	<statement>
		update revenue_type set name='صافي المبيعات' where id=1;
	</statement>
	<statement>
		insert or ignore into revenue_type (id,name) values(5,'الايرادات');
	</statement>
	<statement>
		insert or ignore into revenue_type (id,name) values(6,'المصروفات');
	</statement>

	<statement>
		CREATE VIEW item_avg_cost2 as
		select  date(a.date_) date_ ,c.name curr_name,b.item_id,sum(b.qty) qty,b.cost_price,sum(b.qty*b.cost_price) tot_cost
		from bill_transactions b ,bills a,currency c
		where a.id=b.bill_id  and a.curr_id=c.id and a.tr_type =2
		group by b.item_id,c.name,b.cost_price,date(a.date_)
		union all
		select a.o_date,b.name,a.id,a.o_qty,a.o_cost,o_qty*o_cost
		from items a,currency b
		where a.curr_id=b.id
	</statement>
	<statement>drop view item_avg_cost4;</statement>
	<statement>
		CREATE view  item_avg_cost4 as
		select  a.id,a.date_,d.name br_name,e.name cus_name,h.name item_name,sum(b.qty) qty,
		(case when a.is_back=1 then 'مرتجع'||' '||f.name else f.name end) name,
		(case when a.is_back=1 then -1*f.id else f.id end) tr_type,b.cost_price+b.sls_u_price as price
		from bill_transactions b ,bills a,currency c,branches d  ,customers e,tran_type f,items h
		where a.id=b.bill_id  and a.curr_id=c.id and a.tr_type in (1,2)
		and (a.br_id=d.id or a.to_br_id=d.id)
		and a.cus_id=e.id and a.tr_type=f.id and b.item_id=h.id
		group by b.item_id,a.date_,a.id,c.name,d.name  ,e.id,e.name,f.id,f.name,h.id,h.name,b.cost_price,b.sls_u_price
		union all
		select  a.id,a.date_,(select d.name from branches d where d.id=a.br_id)  br_name,(select d.name from branches d where d.id=a.to_br_id) cus_name,h.name item_name,sum(b.qty) qty,f.name
		,f.id,b.cost_price+b.sls_u_price as price
		from bill_transactions b ,bills a,currency c  ,tran_type f,items h
		where a.id=b.bill_id  and a.curr_id=c.id and a.tr_type=3
		and a.tr_type=f.id and b.item_id=h.id
		group by b.item_id,a.date_,a.id,c.name  ,f.id,f.name,h.id,h.name,b.cost_price,b.sls_u_price
		union all
		select  a.id,a.date_,(select d.name from branches d where d.id=a.to_br_id)
		br_name,(select d.name from branches d where d.id=a.br_id) cus_name,h.name item_name,sum(b.qty) qty,f.name
		,f.id*-1,b.cost_price+b.sls_u_price as price
		from bill_transactions b ,bills a,currency c  ,tran_type f,items h
		where a.id=b.bill_id  and a.curr_id=c.id and a.tr_type=3
		and a.tr_type=f.id and b.item_id=h.id
		group by b.item_id,a.date_,a.id,c.name  ,f.id,f.name,h.id,h.name,b.cost_price,b.sls_u_price
		union all
		select 0 ,a.o_date,c.name,' ' cus_name,a.name,a.o_qty,'إفتتاحي'
		,-1,a.o_cost
		from items a,currency b,branches c
		where a.curr_id=b.id and a.curr_id=c.id;
	</statement>


	--############## Account Tree#############
	<statement>create table if not exists account_tree( id integer PRIMARY KEY ,name text ,parent_id integer default 0,
		p integer default 0,cus_id integer default 0,admin integer default 0,unique(name));</statement>
	<statement>alter table customers add acc_p_id integer default 0;</statement>
	<statement>
		create index if not exists cus_ind_acc_id on customers(acc_p_id);
	</statement>
	<statement>
		create index if not exists acc_tree_ind_id on account_tree(id);
	</statement>
	<statement>update customers set acc_p_id=123 where acc_p_id=0 and cus_type_id=0;</statement>
	<statement>update customers set acc_p_id=221 where acc_p_id=0 and cus_type_id=1;</statement>
	<statement>update customers set acc_p_id=121 where acc_p_id=0 and cus_type_id=4;</statement>
	<statement>update customers set acc_p_id=322 where acc_p_id=0 and cus_type_id=5 and id>0;</statement>
	<statement>update customers set acc_p_id=42 where acc_p_id=0 and cus_type_id=6 and id>0;</statement>
	<statement>update customers set acc_p_id=124 where acc_p_id=0 and cus_type_id=7 and id>0;</statement>

	<statement>update customers set acc_p_id=211 where acc_p_id=0 and id=-13;</statement>
	<statement>update customers set acc_p_id=125 where acc_p_id=0 and id=-14;</statement>
	<statement>update customers set acc_p_id=313 where acc_p_id=0 and id=-8;</statement>
	<statement>update customers set acc_p_id=413 where acc_p_id=0 and id=-7;</statement>
	<statement>update customers set acc_p_id=411 where acc_p_id=0 and id in (-5,-1);</statement>
	<statement>update customers set acc_p_id=412 where acc_p_id=0 and id in (-12,-10);</statement>
	<statement>update customers set acc_p_id=311 where acc_p_id=0 and id in (-2,-6);</statement>
	<statement>update customers set acc_p_id=312 where acc_p_id=0 and id in (-11,-9);</statement>


	<statement>create table if not exists account_tree_type (id integer primary key,name text);</statement>
	<statement>insert or ignore into account_tree_type (id,name) values(0,'فرعي') ;</statement>
	<statement>insert or ignore into account_tree_type (id,name) values(1,'رئيسي') ;</statement>




	-- ########## End Account Tree
	<statement>
		insert or ignore into customers (id,cus_type_id,name,acc_p_id) values(-18,7,'الضريبة',22);
	</statement>
	<statement>
		insert or ignore into customers (id,cus_type_id,name,acc_p_id) values(-20,7,'رسوم أخرى',323);
	</statement>
	<statement>
		insert or ignore into customers (id,cus_type_id,name,acc_p_id) values(-24,7,'اجور نقل',322);
	</statement>
	--v1.101 curr sales
	<statement>
		insert or ignore into customers (id,cus_type_id,name,acc_p_id) values(-27,7,'فوارق بيع وشراء العملات',42);
	</statement>
	<statement>
		update customers set acc_p_id=42 where id=-27;
	</statement>


	--### Prepare need Views ----- #### also existing in fun.java
	<statement>drop view transactions_v;</statement>
	<statement>create view transactions_v as select * from transactions;</statement>
	<statement>drop view bills_v;</statement>
	<statement>create view bills_v as select * from bills;</statement>

	--2024-04 bills2_v
	<statement>drop view bills2_v;</statement>
	<statement>create view bills2_v as select * from bills2;</statement>

	<statement>drop view if exists bill_transactions_v;</statement>
	-- #### also existing in fun.java
	--### v1.99 fix closing bill_transactions_h
	<statement>create view bill_transactions_v as select a.*,
		ifnull((select u_val from unit_item where item_id=a.item_id and unit_id=a.base_unit ),1) c_val
		,(select b.date_ from bills b where b.id=a.bill_id)  bill_date
		from bill_transactions a;</statement>
	<statement>drop view items_v;</statement>
	<statement>create view items_v as select * from items;</statement>
	<statement>drop view units_v;</statement>
	<statement>create view units_v as select * from units;</statement>



	--### End Prepare need Views ---
	<statement>drop  view currency_price_v ;</statement>
	<statement>
		CREATE  view  currency_price_v as
		select * from currency_price where t_date is null;
	</statement>
	--v1.135 users to add user_id in cus_tr_curr_view
	<statement>alter table transactions add user_id integer references users(id) ON UPDATE RESTRICT ON DELETE RESTRICT;</statement>
	<statement>alter table transactions_h add user_id integer references users(id) ON UPDATE RESTRICT ON DELETE RESTRICT;</statement>
	<statement>alter table transactions_h2 add user_id integer references users(id) ON UPDATE RESTRICT ON DELETE RESTRICT;</statement>

	--Begin 2024-02 android 14 to fire index
	<statement>
		CREATE INDEX customers_cus_type_id on customers(cus_type_id);
	</statement>
	--### End 2024-02 android 14 to fire index

	<statement>
		drop  view if exists cus_tr_curr_view ;
	</statement>
	--without transactions_v as slow issue
    --2026-02 address
	<statement>
		CREATE VIEW cus_tr_curr_view as
		select a.id as a_id,a.*,b.id b_id,b.name b_name,c.id c_id,c.name c_name,ifnull(d.id,0) d_id,
		ifnull(d.name,(select name from currency where id=0)) d_name,b.g_id,e.id e_id,e.name e_name,b.gsm
		,(case when a.curr_id=0 then 1 else a.curr_price end )*a.out out_,f.id f_id,f.name f_name
		,(case when a.curr_id=0 then 1 else (select price from currency_price_v where curr_id=a.curr_id) end )*a.out out_2
		,a.param2,a.user_id,b.address
		FROM groups c inner join customers AS b on c.id=b.g_id  left join transactions AS a
		on (  a.cus_id = b.id)
		left join currency d on (a.curr_id=d.id)
		inner join cus_type e on e.id=b.cus_type_id
		inner join account_tree f on f.id=b.acc_p_id
		--where  b.cus_type_id in (0,1,4)
		where a.id is not null
		union all
		select a.id as a_id,a.*,b.id b_id,b.name b_name,c.id c_id,c.name c_name,ifnull(d.id,0) d_id,
		ifnull(d.name,(select name from currency where id=0)) d_name,b.g_id,e.id e_id,e.name e_name,b.gsm
		,(case when a.curr_id=0 then 1 else a.curr_price end )*a.out out_,f.id f_id,f.name f_name
		,(case when a.curr_id=0 then 1 else (select price from currency_price_v where curr_id=a.curr_id) end )*a.out out_2
		,a.param2,a.user_id,b.address
		FROM groups c inner join customers AS b on c.id=b.g_id  left join transactions AS a
		on (  a.t_cus_id = b.id)
		left join currency d on (a.curr_id=d.id)
		inner join cus_type e on e.id=b.cus_type_id
		inner join account_tree f on f.id=b.acc_p_id
		where a.t_cus_id is not null
		--and  b.cus_type_id in (0,1,4)
		and a.id is not null
		;
	</statement>

	<statement>
		CREATE TABLE IF NOT EXISTS items_closing_balance
		(
		`id`	INTEGER,
		`name`	TEXT ,
		item_type_id integer REFERENCES item_type(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		date_                    text default (strftime('%Y-%m-%d','now')),
		IS_ACTIVE                  integer default 1  ,
		o_qty real default 0,
		o_cost real default 0,
		o_br_id integer default 0,
		o_date text default (strftime('%Y-%m-%d','now')),
		remarks text,
		qty real default 0,
		price real default 0,
		curr_id integer default 0,
		`param1`	TEXT,
		`param2`	TEXT
		)
		;
	</statement>
	<statement>
		alter table items_closing_balance add e_qty real default 0;
	</statement>



	<statement>
		CREATE TABLE IF NOT EXISTS current_closing_balance
		(
		`id`	INTEGER PRIMARY KEY AUTOINCREMENT,
		date_                    text default (strftime('%Y-%m-%d','now')),
		updated                  integer default 1
		)
		;
	</statement>
	<statement>
		insert or ignore into current_closing_balance(id,date_,updated) values (0,(select date(strftime('%Y-%m-%d','now'),'-1 year')),1);
	</statement>
	<statement>
		CREATE INDEX if not exists bill_trans_id on bill_transactions(bill_id);
	</statement>
	<statement>
		CREATE INDEX if not exists bill_trans_item_id on bill_transactions(item_id);
	</statement>
	<statement>
		CREATE INDEX if not exists bills_br_id on bills(br_id);
	</statement>
	<statement>
		CREATE INDEX if not exists bills_curr_id on bills(curr_id);
	</statement>
	<statement>
		CREATE INDEX if not exists bills_tr_type_id on bills(tr_type);
	</statement>
	<statement>
		CREATE TABLE if not exists "act_req" (
		`imei`	TEXT ,
		aid text,
		`name`	TEXT ,
		`phone`	TEXT,
		`country`	TEXT,
		`email`	TEXT,
		`status`	INTEGER,
		`date_`	TEXT,
		`reply`	TEXT,
		`user_reply`	TEXT,
		`reply_from`	TEXT
		);
	</statement>
	<statement>
		create table if not exists days_ (id integer primary key,name text);
	</statement>
	<statement>
		insert or ignore into days_ (id,name) values(0,'الأحد');
	</statement>
	<statement>
		insert or ignore into days_ (id,name) values(1,'الإثنين');
	</statement>
	<statement>
		insert or ignore into days_ (id,name) values(2,'الثلاثاء');
	</statement>
	<statement>
		insert or ignore into days_ (id,name) values(3,'الأربعاء');
	</statement>
	<statement>
		insert or ignore into days_ (id,name) values(4,'الخميس');
	</statement>
	<statement>
		insert or ignore into days_ (id,name) values(5,'الجمعة');
	</statement>
	<statement>
		insert or ignore into days_ (id,name) values(6,'السبت');
	</statement>

	-- Setllement Transactions
	<statement>drop TRIGGER transactions_cr_insert</statement>
	<statement>
		CREATE TRIGGER transactions_cr_insert after insert ON transactions
		when (NEW.[in]='-1' and NEW.t_cus_id is null and NEW.bill_id=0  )
		BEGIN
		update transactions set [in]=1,t_cus_id=NEW.cus_id,cus_id=NEW.cash_id
		,curr_price=
		(case when NEW.curr_id!=0 then ifnull((select price from currency_price
		where curr_id=NEW.curr_id and
		date(strftime('%Y-%m-%d',substr( NEW.date_,7,4)||'-' ||substr( NEW.date_,4,2)||'-'||substr( NEW.date_,1,2) ))
		between f_date and
		ifnull(t_date,date(strftime('%Y-%m-%d',substr( NEW.date_,7,4)||'-' ||substr( NEW.date_,4,2)||'-'||substr( NEW.date_,1,2) )))),0)
		else 1 end )

		,p_curr_price=
		(case when NEW.p_curr_id!=0 then ifnull((select price from currency_price
		where curr_id=NEW.p_curr_id and
		date(strftime('%Y-%m-%d',substr( NEW.date_,7,4)||'-' ||substr( NEW.date_,4,2)||'-'||substr( NEW.date_,1,2) ))
		between f_date and
		ifnull(t_date,date(strftime('%Y-%m-%d',substr( NEW.date_,7,4)||'-' ||substr( NEW.date_,4,2)||'-'||substr( NEW.date_,1,2) )))),0)
		else 1 end )
		,p_date=NEW.date_

		where id=NEW.id;
		END
	</statement>
	<statement>drop TRIGGER transactions_db_insert</statement>
	<statement>
		CREATE TRIGGER transactions_db_insert after insert ON transactions
		when (NEW.[in]='1' and NEW.t_cus_id is null and NEW.bill_id=0 )
		BEGIN
		update transactions set t_cus_id=NEW.cash_id
		,p_date=NEW.date_
		where id=NEW.id;
		END
	</statement>


	-- Opening Balance Transactions
	<statement>drop TRIGGER transactions_cr_op_insert</statement>
	<statement>
		CREATE TRIGGER transactions_cr_op_insert after insert ON transactions
		when (NEW.[in]='-1' and NEW.t_cus_id is null and NEW.bill_id=-1  )
		BEGIN
		--Supplier
		update transactions set [in]=1,t_cus_id=NEW.cus_id,cus_id=New.fund_id


		where id=NEW.id;
		END
	</statement>
	<statement>drop TRIGGER transactions_db_op_insert</statement>
	<statement>
		CREATE TRIGGER transactions_db_op_insert after insert ON transactions
		when (NEW.[in]='1' and NEW.t_cus_id is null and NEW.bill_id=-1 )
		BEGIN
		--Customer
		update transactions set t_cus_id=New.fund_id

		where id=NEW.id;
		END
	</statement>

	<statement>drop TRIGGER transactions_cr_op_update</statement>
	<statement>
		CREATE TRIGGER transactions_cr_op_update after update ON transactions
		when (NEW.[in]='-1' and NEW.t_cus_id is null and NEW.bill_id=-1  and (select is_active from trigger_flags)=1  )
		BEGIN
		--Supplier
		update transactions set [in]=1,t_cus_id=NEW.cus_id,cus_id=New.fund_id



		where id=NEW.id;
		END
	</statement>
	<statement>drop TRIGGER transactions_db_op_update</statement>
	<statement>
		CREATE TRIGGER transactions_db_op_update after update ON transactions
		when (NEW.[in]='1' and NEW.t_cus_id is null and NEW.bill_id=-1 and (select is_active from trigger_flags)=1  )
		BEGIN
		--Customer
		update transactions set t_cus_id=New.fund_id



		where id=NEW.id;
		END
	</statement>


	-- Discount transactions triggers


	<statement>drop TRIGGER transactions_cr_disc_insert</statement>
	<statement>
		CREATE TRIGGER transactions_cr_disc_insert after insert ON transactions
		when (NEW.[in]='-1' and NEW.d_amount !=0 and NEW.bill_id=0  )
		BEGIN
		insert or ignore into transactions(cus_id,t_cus_id,date_,remarks,[in],out,param1,param2,d_amount,bill_id,p_id,curr_id,p_curr_id,p_date
		)
		values(-8,NEW.cus_id,NEW.date_,NEW.d_remarks,1,NEW.d_amount,NEW.param1,NEW.param2,0,
		-2,
		(select ifnull(max(a.p_id),0)+1   as _id from transactions a where a.bill_id=-2)
		,NEW.curr_id,NEW.curr_id
		,NEW.date_

		);
		update transactions set bill_id=-2,p_id=
		--without incremental as prev. record
		(select ifnull(max(a.p_id),0)   as _id from transactions a where a.bill_id=-2)
		,p_curr_id=NEW.curr_id

		where id=NEW.id;
		END
	</statement>
	<statement>drop TRIGGER transactions_db_disc_insert</statement>
	<statement>
		CREATE TRIGGER transactions_db_disc_insert after insert ON transactions
		when (NEW.[in]='1' and  NEW.d_amount !=0 and NEW.bill_id=0 )
		BEGIN
		insert or ignore into transactions(cus_id,t_cus_id,date_,remarks,[in],out,param1,param2,d_amount,bill_id,p_id
		,curr_id,p_curr_id,p_date)
		values(NEW.cus_id,-7,NEW.date_,NEW.d_remarks,1,NEW.d_amount,NEW.param1,NEW.param2,0,
		-2,
		(select ifnull(max(a.p_id),0)+1   as _id from transactions a where a.bill_id=-2)
		,NEW.curr_id,NEW.curr_id
		,NEW.date_
		);
		update transactions set bill_id=-2,p_id=
		--without incremental as prev. record
		(select ifnull(max(a.p_id),0)   as _id from transactions a where a.bill_id=-2)
		,p_curr_id=NEW.curr_id
		where id=NEW.id;
		END
	</statement>

	<statement>
		CREATE TABLE if not exists "tr_p_temps" (
		`no_`	INTEGER PRIMARY KEY AUTOINCREMENT,
		`cus_id`	INTEGER,
		`in_`	TEXT,
		`out`	TEXT,
		`date_`	TEXT,
		`remarks`	TEXT,
		now_ TEXT,
		`param1`	TEXT,
		`param2`	TEXT,
		t_cus_id INTEGER ,
		`f1`	TEXT,
		`f2`	TEXT,
		`f3`	TEXT,
		curr_id integer default 0 references currency(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		p_curr_id integer default 0 references currency(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		bill_id integer default 0,
		p_id integer default 0,
		p_remarks text,
		p_amount real default 0,
		p_status integer default 0,
		p_ref_no text,
		curr_price real default 0

		)
	</statement>



	<statement>drop TRIGGER currency_price_delete</statement>
	<statement>
		CREATE TRIGGER currency_price_delete after delete ON currency_price
		when (OLD.t_date is null)
		BEGIN
		update currency_price set
		t_date=null
		where curr_id=OLD.curr_id
		and f_date = (select max(f_date) from currency_price where curr_id=OLD.curr_id );
		END
	</statement>
	<statement>drop TRIGGER currency_price_delete2</statement>
	<statement>
		CREATE TRIGGER currency_price_delete2 after delete ON currency_price
		when (OLD.t_date is not null)
		BEGIN
		update currency_price set
		t_date=OLD.t_date
		where curr_id=OLD.curr_id
		and f_date = (select max(f_date) from currency_price
		where curr_id=OLD.curr_id and f_date
		--less than
		&#60;
		OLD.f_date);
		END
	</statement>

	-- update currency price in transactions table
	<statement>drop TRIGGER transactions_curr_price_insert</statement>
	<statement>
		CREATE TRIGGER transactions_curr_price_insert after insert ON transactions
		when ((NEW.curr_id!=0 or NEW.p_curr_id!=0  ) and NEW.curr_mod=0) --new record
		BEGIN
		update transactions set
		curr_price=
		(case when NEW.curr_id!=0 then ifnull((select price from currency_price
		where curr_id=NEW.curr_id and
		date(strftime('%Y-%m-%d',substr( NEW.date_,7,4)||'-' ||substr( NEW.date_,4,2)||'-'||substr( NEW.date_,1,2) ))
		between f_date and
		ifnull(t_date,date(strftime('%Y-%m-%d',substr( NEW.date_,7,4)||'-' ||substr( NEW.date_,4,2)||'-'||substr( NEW.date_,1,2) )))),0)
		else 1 end )
		,p_curr_price=
		(case when NEW.p_curr_id!=0 then ifnull((select price from currency_price
		where curr_id=NEW.p_curr_id and
		date(strftime('%Y-%m-%d',substr( NEW.date_,7,4)||'-' ||substr( NEW.date_,4,2)||'-'||substr( NEW.date_,1,2) ))
		between f_date and
		ifnull(t_date,date(strftime('%Y-%m-%d',substr( NEW.date_,7,4)||'-' ||substr( NEW.date_,4,2)||'-'||substr( NEW.date_,1,2) )))),0)
		else 1 end )

		where id=NEW.id;
		END
	</statement>

	<statement>drop TRIGGER transactions_curr_price_update</statement>
	<statement>
		CREATE TRIGGER transactions_curr_price_update after update ON transactions
		--when ( NEW.curr_id!=0 and (OLD.curr_id!=NEW.curr_id  or OLD.date_!=NEW.date_ ) and NEW.curr_mod=0   ) --update curr or date
		when (( NEW.curr_id!=0 or NEW.p_curr_id!=0   ) and  NEW.curr_mod=0  and (select is_active from trigger_flags)=1 ) --update curr or date
		BEGIN
		update transactions set curr_price=
		(case when NEW.curr_id!=0 then ifnull((select price from currency_price
		where curr_id=NEW.curr_id and
		date(strftime('%Y-%m-%d',substr( NEW.date_,7,4)||'-' ||substr( NEW.date_,4,2)||'-'||substr( NEW.date_,1,2) ))
		between f_date and
		ifnull(t_date,date(strftime('%Y-%m-%d',substr( NEW.date_,7,4)||'-' ||substr( NEW.date_,4,2)||'-'||substr( NEW.date_,1,2) )))),0)
		else 1 end )
		,p_curr_price=
		(case when NEW.p_curr_id!=0 then ifnull((select price from currency_price
		where curr_id=NEW.p_curr_id and
		date(strftime('%Y-%m-%d',substr( NEW.date_,7,4)||'-' ||substr( NEW.date_,4,2)||'-'||substr( NEW.date_,1,2) ))
		between f_date and
		ifnull(t_date,date(strftime('%Y-%m-%d',substr( NEW.date_,7,4)||'-' ||substr( NEW.date_,4,2)||'-'||substr( NEW.date_,1,2) )))),0)
		else 1 end )

		where id=NEW.id;
		END
	</statement>


	-- update currency price in tr_p_temps table
	<statement>drop TRIGGER tr_p_temps_curr_price_insert</statement>
	<statement>
		CREATE TRIGGER tr_p_temps_curr_price_insert after insert ON tr_p_temps
		when ((NEW.curr_id!=0 or NEW.p_curr_id!=0 ) and NEW.curr_mod=0) --new record
		BEGIN
		update tr_p_temps set
		curr_price=
		(case when NEW.curr_id!=0 then ifnull((select price from currency_price
		where curr_id=NEW.curr_id and
		date(strftime('%Y-%m-%d',substr( NEW.date_,7,4)||'-' ||substr( NEW.date_,4,2)||'-'||substr( NEW.date_,1,2) ))
		between f_date and
		ifnull(t_date,date(strftime('%Y-%m-%d',substr( NEW.date_,7,4)||'-' ||substr( NEW.date_,4,2)||'-'||substr( NEW.date_,1,2) )))),0)
		else 1 end )
		,p_curr_price=
		(case when NEW.p_curr_id!=0 then ifnull((select price from currency_price
		where curr_id=NEW.p_curr_id and
		date(strftime('%Y-%m-%d',substr( NEW.date_,7,4)||'-' ||substr( NEW.date_,4,2)||'-'||substr( NEW.date_,1,2) ))
		between f_date and
		ifnull(t_date,date(strftime('%Y-%m-%d',substr( NEW.date_,7,4)||'-' ||substr( NEW.date_,4,2)||'-'||substr( NEW.date_,1,2) )))),0)
		else 1 end )

		where no_=NEW.no_;
		END
	</statement>

	<statement>drop TRIGGER tr_p_temps_curr_price_update</statement>
	<statement>
		CREATE TRIGGER tr_p_temps_curr_price_update after update ON tr_p_temps
		--when ( NEW.curr_id!=0 and (OLD.curr_id!=NEW.curr_id  or OLD.date_!=NEW.date_ )   ) --update curr or date
		when (( NEW.curr_id!=0 or NEW.p_curr_id!=0  ) and NEW.curr_mod=0) --update curr or date
		BEGIN
		update tr_p_temps set curr_price=
		(case when NEW.curr_id!=0 then ifnull((select price from currency_price
		where curr_id=NEW.curr_id and
		date(strftime('%Y-%m-%d',substr( NEW.date_,7,4)||'-' ||substr( NEW.date_,4,2)||'-'||substr( NEW.date_,1,2) ))
		between f_date and
		ifnull(t_date,date(strftime('%Y-%m-%d',substr( NEW.date_,7,4)||'-' ||substr( NEW.date_,4,2)||'-'||substr( NEW.date_,1,2) )))),0)
		else 1 end )
		,p_curr_price=
		(case when NEW.p_curr_id!=0 then ifnull((select price from currency_price
		where curr_id=NEW.p_curr_id and
		date(strftime('%Y-%m-%d',substr( NEW.date_,7,4)||'-' ||substr( NEW.date_,4,2)||'-'||substr( NEW.date_,1,2) ))
		between f_date and
		ifnull(t_date,date(strftime('%Y-%m-%d',substr( NEW.date_,7,4)||'-' ||substr( NEW.date_,4,2)||'-'||substr( NEW.date_,1,2) )))),0)
		else 1 end )

		where no_=NEW.no_;
		END
	</statement>


	-- update currency price in bill_transactions table
	<statement>drop TRIGGER bill_transactions_curr_price_insert</statement>
	<statement>
		CREATE TRIGGER bill_transactions_curr_price_insert after insert ON bills
		when (NEW.curr_id!=0 and NEW.curr_price=0 ) --new record
		BEGIN
		update bills set
		curr_price=
		(case when NEW.curr_id!=0 then ifnull((select price from currency_price
		where curr_id=NEW.curr_id and
		NEW.date_
		between f_date and
		ifnull(t_date,NEW.date_))
		,0)
		else 1 end )
		where id=NEW.id;
		END
	</statement>

	<statement>drop TRIGGER bill_transactions_curr_price_update</statement>
	<statement>
		CREATE TRIGGER bill_transactions_curr_price_update after update ON bills
		--when ( NEW.curr_id!=0 and (OLD.curr_id!=NEW.curr_id  or OLD.date_!=NEW.date_ )   ) --update curr or date
		when ( NEW.curr_id!=0  and NEW.curr_mod=0  and (select is_active from trigger_flags)=1 ) --update curr or date
		BEGIN
		update bills set curr_price=
		(case when NEW.curr_id!=0 then ifnull((select price from currency_price
		where curr_id=NEW.curr_id and
		NEW.date_
		between f_date and
		ifnull(t_date,NEW.date_))
		,0)
		else 1 end )

		where id=NEW.id;
		END
	</statement>

	-- update currency price in items table
	<statement>drop TRIGGER items_curr_price_insert</statement>
	<statement>
		CREATE TRIGGER items_curr_price_insert after insert ON items
		when (NEW.curr_id!=0 and NEW.curr_price=0 ) --new record
		BEGIN
		update items set
		curr_price=
		(case when NEW.curr_id!=0 then ifnull((select price from currency_price
		where curr_id=NEW.curr_id and
		NEW.o_date
		between f_date and
		ifnull(t_date,NEW.o_date))
		,0)
		else 1 end )
		where id=NEW.id;
		END
	</statement>

	<statement>drop TRIGGER items_curr_price_update</statement>
	<statement>
		CREATE TRIGGER items_curr_price_update after update ON items
		--when ( NEW.curr_id!=0 and (OLD.curr_id!=NEW.curr_id  or OLD.date_!=NEW.date_ )   ) --update curr or date
		when ( NEW.curr_id!=0   ) --update curr or date
		BEGIN
		update items set
		curr_price=
		(case when NEW.curr_id!=0 then ifnull((select price from currency_price
		where curr_id=NEW.curr_id and
		NEW.o_date
		between f_date and
		ifnull(t_date,NEW.o_date))
		,0)
		else 1 end )

		where id=NEW.id;
		END
	</statement>



	<statement>
		--update previous currency transactions
		--v1.94 updated
		update transactions set curr_id=curr_id,p_curr_id=p_curr_id where
		((curr_id!=0 and curr_price=0) or (p_curr_id!=0 and p_curr_price=0))
		and (date(strftime('%Y-%m-%d',substr( date_,7,4)||'-' ||substr( date_,4,2)||'-'||substr( date_,1,2) ))>
		(select ifnull(max(date_),date(date(strftime('%Y-%m-%d',substr(transactions.date_,7,4)||'-' ||substr(transactions.date_,4,2)||'-'||substr(transactions.date_,1,2) )),'-1 day')) from closing_year))
		and ROWID in(select b.rowid from transactions b where
		((b.curr_id!=0 and b.curr_price=0) or (b.p_curr_id!=0 and b.p_curr_price=0))
		and (date(strftime('%Y-%m-%d',substr( b.date_,7,4)||'-' ||substr( b.date_,4,2)||'-'||substr( b.date_,1,2) ))>
		(select ifnull(max(date_),date(date(strftime('%Y-%m-%d',substr( b.date_,7,4)||'-' ||substr( b.date_,4,2)||'-'||substr( b.date_,1,2) )),'-1 day')) from closing_year))
		limit 100);
	</statement>
	--v1.94
	<statement>
		--update previous currency transactions1
		update transactions set curr_id=curr_id,p_curr_id=p_curr_id where curr_id!=0 and curr_price=0
		and (date(strftime('%Y-%m-%d',substr( date_,7,4)||'-' ||substr( date_,4,2)||'-'||substr( date_,1,2) ))>
		(select ifnull(max(date_),date(date(strftime('%Y-%m-%d',substr(transactions.date_,7,4)||'-' ||substr(transactions.date_,4,2)||'-'||substr(transactions.date_,1,2) )),'-1 day')) from closing_year))
		and ROWID in(select b.rowid from transactions b where b.curr_id!=0 and b.curr_price=0
		and (date(strftime('%Y-%m-%d',substr( b.date_,7,4)||'-' ||substr( b.date_,4,2)||'-'||substr( b.date_,1,2) ))>
		(select ifnull(max(date_),date(date(strftime('%Y-%m-%d',substr( b.date_,7,4)||'-' ||substr( b.date_,4,2)||'-'||substr( b.date_,1,2) )),'-1 day')) from closing_year))
		limit 100);
	</statement>
	<statement>
		--update previous currency transactions2
		update transactions set curr_id=curr_id,p_curr_id=p_curr_id where p_curr_id!=0 and p_curr_price=0
		and (date(strftime('%Y-%m-%d',substr( date_,7,4)||'-' ||substr( date_,4,2)||'-'||substr( date_,1,2) ))>
		(select ifnull(max(date_),date(date(strftime('%Y-%m-%d',substr(transactions.date_,7,4)||'-' ||substr(transactions.date_,4,2)||'-'||substr(transactions.date_,1,2) )),'-1 day')) from closing_year))
		and ROWID in(select b.rowid from transactions b where b.p_curr_id!=0 and b.p_curr_price=0
		and (date(strftime('%Y-%m-%d',substr( b.date_,7,4)||'-' ||substr( b.date_,4,2)||'-'||substr( b.date_,1,2) ))>
		(select ifnull(max(date_),date(date(strftime('%Y-%m-%d',substr( b.date_,7,4)||'-' ||substr( b.date_,4,2)||'-'||substr( b.date_,1,2) )),'-1 day')) from closing_year))
		limit 100);
	</statement>
	<statement>
		--update previous currency bills
		update bills set curr_id=curr_id where curr_id!=0 and curr_price=0 ;
	</statement>

	-- end v1.94

	<statement>
		--v1.04 updated
		--update previous currency bills
		--2024-10
		update bills set curr_id=curr_id where curr_id!=0 and rowid in(select b.rowid from bills b where b.curr_id!=0 and b.curr_price=0 limit 100) ;
	</statement>

	<statement>
		--update previous currency items
		update items set curr_id=curr_id where curr_id!=0 ;
	</statement>

	<statement>
		--update previous OP items
		update items set o_cost=o_cost where o_cost!=0  ;
	</statement>

	<statement>
		--update p_curr_id previous disc trs as old tr was not set p_curr_id
		update transactions set p_curr_id=curr_id where bill_id=-2 and p_ref_no is null and p_curr_id != curr_id ;
	</statement>

	--Adj type
	<statement>
		create table if not exists adj_type (id integer primary key,name text,acc_id integer,in_ integer);
	</statement>
	<statement>
		insert or ignore into adj_type (id,name,acc_id,in_) values(1,'عجز',-15,1);
	</statement>
	<statement>
		insert or ignore into adj_type (id,name,acc_id,in_) values(2,'زيادة',-15,-1);
	</statement>
	<statement>
		insert or ignore into adj_type (id,name,acc_id,in_) values(3,'تالف',-16,1);
	</statement>
	<statement>
		insert or ignore into adj_type (id,name,acc_id,in_) values(4,'افتتاحي',-14,1);
	</statement>
	--split bill no based on type and is_back


	--ADJ Opening Balance
	<statement>drop TRIGGER bills_opening_adj_insert</statement>
	<statement>
		CREATE TRIGGER bills_opening_adj_insert after insert ON bills
		when (NEW.tr_type=4 and NEW.adj_id=4 and NEW.is_back=0)
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no,tr_type)
		values(-14,-13,
		1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2,NEW.tr_type
		);
		END
	</statement>
	<statement>drop TRIGGER bills_opening_adj_update</statement>
	--updated on 2025-01 where adj_id=4
	<statement>
		CREATE TRIGGER bills_opening_adj_update after update ON bills
		when ( NEW.tr_type=4  and NEW.is_back=0 and (select is_active from trigger_flags)=1 )
		BEGIN
		delete from transactions where bill_id=NEW.id;
		insert or ignore into transactions(cus_id,t_cus_id,[in],[out],curr_id,date_,bill_id,remarks,param2,p_ref_no,tr_type)
		select -14,-13,
		1,NEW.amount,NEW.curr_id,strftime('%d-%m-%Y',NEW.date_),NEW.id,
		NEW.remarks
		||'',
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.bill_no2,NEW.tr_type

		where NEW.adj_id=4;
		END
	</statement>



	--#### Units feature
	<statement>
		CREATE TABLE IF NOT EXISTS units (
		id INTEGER PRIMARY KEY AUTOINCREMENT,
		name text NOT NULL ,
		code text NOT NULL,
		param1 text ,
		param2 text ,
		unique( name),
		unique( code)
		)
	</statement>
	<statement>
		insert or ignore into units(id,name,code) values(0,'بدون','.');
	</statement>
	<statement>
		insert or ignore into units(name,code) values('حبة','حبة');
	</statement>
	<statement>
		insert or ignore into units(name,code) values('كيلو','ك');
	</statement>
	<statement>
		insert or ignore into units(name,code) values('كرتون','كرتون');
	</statement>
	<statement>
		insert or ignore into units(name,code) values('كيس','كيس');
	</statement>

	<statement>
		alter table items add unit_id integer default 0;
	</statement>
	<statement>
		alter table items add u_val integer default 1;
	</statement>
	-- for closing year tables
	<statement>
		alter table items_h add unit_id integer default 0;
	</statement>
	<statement>
		alter table items_h add u_val integer default 1;
	</statement>
	--Barcode feature
	<statement>
		alter table items add barcode text ;
	</statement>
	<statement>
		alter table items_h add barcode text ;
	</statement>
	<statement>CREATE UNIQUE INDEX if not exists uq_item_barcode ON items(barcode);</statement>

	--end
	<statement>
		CREATE TABLE IF NOT EXISTS unit_item
		(
		id INTEGER PRIMARY KEY AUTOINCREMENT,
		item_id                    integer references items(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		unit_id                  integer references units(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		u_val               	real default 1,
		date_            text default (strftime('%Y-%m-%d','now')),
		param1 text,
		param2 text,
		param3 text,
		unique( item_id,unit_id)
		);
	</statement>
	<statement>
		insert or ignore into unit_item(item_id,unit_id,u_val) select id,unit_id,u_val from items
		where not exists(select * from unit_item where item_id=items.id and unit_id=items.unit_id);
	</statement>

	<statement>drop TRIGGER items_unit_insert</statement>
	<statement>
		CREATE TRIGGER items_unit_insert after insert ON items
		--when (NEW.o_cost!=0 and NEW.o_qty!=0)
		BEGIN
		insert or ignore into unit_item(item_id,unit_id,u_val) values(NEW.id,NEW.unit_id,NEW.u_val);
		END
	</statement>
    --2025-12 update old base unit if used to fire sync
    <statement>drop TRIGGER items_unit_update</statement>
	<statement>
		CREATE TRIGGER items_unit_update after update ON items
		when ( NEW.unit_id!=OLD.unit_id)
		BEGIN
		-- if base unit not existing before
		insert or ignore into unit_item(item_id,unit_id,u_val) select id,unit_id,u_val from items where id=NEW.id
		and not exists(select * from unit_item where item_id=NEW.id and unit_id=NEW.unit_id);
		-- delete old base unit
		delete from unit_item where item_id=NEW.id and unit_id=OLD.unit_id
		and not exists(select * from bill_transactions where item_id=NEW.id and unit_id=OLD.unit_id);
		--and not exists(select * from bill_transactions_h where item_id=NEW.id and unit_id=OLD.unit_id);
		--update old base unit if used
        --update existing u_val in unit_item except base unit
        update unit_item set id=id where item_id=NEW.id and unit_id=OLD.unit_id
        and exists(select * from bill_transactions where item_id=NEW.id and unit_id=OLD.unit_id);
        --and not exists(select * from bill_transactions_h where item_id=NEW.id and unit_id=OLD.unit_id);

        update unit_item
		set u_val=u_val/(select u_val from unit_item where item_id=NEW.id and unit_id=NEW.unit_id)
		where item_id=NEW.id and unit_id!=NEW.unit_id;
		-- update u_val of base unit id
		update unit_item set u_val=1 where item_id=NEW.id and unit_id=NEW.unit_id;
		END
	</statement>
	-- Prevent update u_val for base unit_id
	<statement>drop TRIGGER prevent_invalid_u_val; </statement>
	<statement>
		CREATE TRIGGER prevent_invalid_u_val
		BEFORE update ON unit_item
		FOR EACH ROW
		WHEN ( NEW.u_val!=1 and OLD.unit_id=(select unit_id from items where id=OLD.item_id) )
		BEGIN
		SELECT RAISE(ABORT, 'Invalid Update base Unit') ;
		END;
	</statement>
    --2025-11
    -- Prevent update unit_id null
    <statement>drop TRIGGER prevent_invalid_unit_id; </statement>
    <statement>
        CREATE TRIGGER prevent_invalid_unit_id
        BEFORE update ON unit_item
        FOR EACH ROW
        WHEN ( NEW.unit_id is null or NEW.item_id is null  )
        BEGIN
        SELECT RAISE(ABORT, 'Invalid Update Unit_Item') ;
        END;
    </statement>
	-- Prevent delete base unit_id
	<statement>drop TRIGGER prevent_delete_base_unit; </statement>
	<statement>
		CREATE TRIGGER prevent_delete_base_unit
		BEFORE delete  ON unit_item
		FOR EACH ROW
		WHEN ( OLD.unit_id=(select unit_id from items where id=OLD.item_id) )
		BEGIN
		SELECT RAISE(ABORT, 'Cannot Remove Base Unit') ;
		END;
	</statement>
	<statement>
		alter table bill_transactions add unit_id integer default 0 references units(id) ON UPDATE RESTRICT ON DELETE RESTRICT;
	</statement>
	<statement>
		alter table bill_transactions add u_val real default 1 ;
	</statement>
	<statement>
		alter table bill_transactions add base_unit integer default -1 ;
	</statement>

	-- Prevent update base unit id if it is releated to bills
	<statement>drop TRIGGER if exists prevent_update_base_unit_id; </statement>
	<statement>
		--CREATE TRIGGER prevent_update_base_unit_id
		--BEFORE update  ON items
		--FOR EACH ROW
		--WHEN ( NEW.unit_id!=OLD.unit_id and OLD.unit_id=
		--(select base_unit from bill_transactions where item_id=OLD.id and base_unit!=-1 order by base_unit desc limit 1 ) )
		--BEGIN
		--SELECT RAISE(ABORT, 'Cannot Update base Unit') ;
		--END;
	</statement>

	<statement>
		alter table bill_transactions_h add unit_id integer default 0 references units(id) ON UPDATE RESTRICT ON DELETE RESTRICT;
	</statement>
	<statement>
		alter table bill_transactions_h add u_val real default 1 ;
	</statement>
	<statement>
		alter table bill_transactions_h add base_unit integer default -1 ;
	</statement>
	<statement>
		alter table items_temp add unit_id integer default 0 references units(id) ON UPDATE RESTRICT ON DELETE RESTRICT;
	</statement>
	<statement>
		alter table items_temp add u_val real default 1 ;
	</statement>
	<statement>
		alter table items_temp add base_unit integer default -1 ;
	</statement>
	--qty percentage..for reports...items_cost_calc_v
	<statement>
		alter table bill_transactions add qty_pr real  ;
	</statement>
	<statement>
		alter table bill_transactions add qty_t text  ;
	</statement>
	--fix old data
	<statement>
		update bill_transactions set qty_pr=qty where qty_pr is null  ;
	</statement>

	<statement>
		update bill_transactions set qty_t=qty where qty_t is null  ;
	</statement>
	<statement>
		update bill_transactions set qty_t=qty_pr where qty_t=''  ;
	</statement>


	<statement>
		update bill_transactions set qty=qty_pr where qty=''  ;
	</statement>


	<statement>
		alter table bill_transactions_h add qty_pr real  ;
	</statement>
	<statement>
		alter table bill_transactions_h add qty_t text  ;
	</statement>
	<statement>
		alter table items_temp add qty_pr real  ;
	</statement>
	<statement>
		alter table items_temp add qty_t text  ;
	</statement>

	-- avg cost
	<statement>
		alter table bill_transactions add u_cost2 real default 0 ;
	</statement>
	<statement>
		alter table bill_transactions_h add u_cost2 real default 0  ;
	</statement>

	<statement>
		alter table items_temp add u_cost2 real default 0  ;
	</statement>

	--### v1.99 fix closing bill_transactions_h
	<statement>
		alter table bill_transactions_h add date_ text;
	</statement>
	<statement>
		alter table bill_transactions add date_ text;
	</statement>
	--## end v1.99
	--### 2024-04 fix closing bill_transactions2_h
	<statement>
		alter table bill_transactions2_h add date_ text;
	</statement>
	<statement>
		alter table bill_transactions2 add date_ text;
	</statement>

	--### 2024-10 fix old ver without cash_id
	<statement>
		alter table bill_transactions_h add cash_id integer default -3;
	</statement>
	<statement>
		alter table bill_transactions add cash_id integer default -3;
	</statement>
	--### 2024-12 fix old ver without cash_id
	<statement>
		alter table bill_transactions2 add cash_id integer default -3;
	</statement>
	<statement>
		alter table bill_transactions2_h add cash_id integer default -3;
	</statement>


	--##v1.119 end_date
	<statement>
		alter table items_temp add e_date text;
	</statement>
	<statement>
		alter table bill_transactions_h add e_date text;
	</statement>
	<statement>
		alter table bill_transactions add e_date text;
	</statement>
	<statement>
		alter table bill_transactions2 add e_date text;
	</statement>
    --2025-01
    <statement>
        alter table bill_transactions2_h add e_date text;
    </statement>
	-- Inventory features
	<statement>
		create table if not exists bills2 as select * from bills where 1=2  ;
	</statement>
	<statement>
		drop INDEX if EXISTS bills2_uq ;
	</statement>
	<statement>
		CREATE UNIQUE INDEX if not exists bills2_uq2 ON bills2(tr_type,bill_no2,br_id);
	</statement>
	<statement>
		create table if not exists bill_transactions2 as select * from bill_transactions where 1=2  ;
	</statement>
	--v1.119 end_date
	<statement>
		alter table bill_transactions2 add date_ text;
	</statement>
	--2024-10
	<statement>
		alter table bill_transactions2_h add date_ text;
	</statement>
	<statement>drop view bill_transactions2_v;</statement>
	<statement>create view bill_transactions2_v as select a.*,
		ifnull((select u_val from unit_item where item_id=a.item_id and unit_id=a.base_unit ),1) c_val
		,(select b.date_ from bills2 b where b.id=a.bill_id)  bill_date
		from bill_transactions2 a;</statement>
	-- end inventory feature

	-- #### also existing in fun.java
	<statement>drop view bill_transactions_v;</statement>
	<statement>create view bill_transactions_v as select a.*,
		ifnull((select u_val from unit_item where item_id=a.item_id and unit_id=a.base_unit ),1) c_val
		,(select b.date_ from bills b where b.id=a.bill_id)  bill_date
		from bill_transactions a;</statement>
	--######### END

	--2024-03 id2 item balance negative balance issue when add purchase for zero qty
	<statement>
		alter table bills add id2 integer ;
	</statement>
	<statement>
		alter table bills_h add id2 integer ;
	</statement>
	<statement>drop view bills_v;</statement>
	<statement>create view bills_v as select * from bills;</statement>
	<statement>alter table bills_31_211_$yyyy add id2 integer;</statement>
	--###end 2024-03 id2 item balance negative balance issue when add purchase for zero qty

	--Continue Stock
	<statement>drop view if exists items_cost_calc_v</statement>
	--v1.99 ####Also found in cost_view.txt file
	--v1.119 end_date
	<statement>
		CREATE VIEW if not exists items_cost_calc_v as

		-- Opening balance
		select   c.id as item_id,c.o_br_id as br_id,0 to_br_id,c.o_date as date_,-1 bill_id,0 tr_type, 0 is_back,'' name,
		0 bill_no,0 bill_no2,
		c.o_qty as i_q,
		(case when c.curr_id=0 then c.o_cost  else c.curr_price*c.o_cost end ) i_u, 0 o_q,0 as o_u,
		0 n_q,0 n_u,0 n_t,0 r_u,0 adj_id,c.e_date e_date,0 id2
		from items_v c
		where c.o_qty !=0

		union all
		-- purchase and sales with is_back=0
		--v1.120 adj2
		select   b.item_id,a.br_id,a.br_id,a.date_,b.bill_id,a.tr_type,a.is_back,(select name from customers where id=a.cus_id) name,
		a.bill_no,a.bill_no2,
		(case when a.tr_type in(2,21) then b.qty_pr*b.u_val*b.c_val else 0 end) i_q,(case when a.tr_type in(2,21) then
		(case when a.curr_id=0 then b.cost_price+b.u_cost2-b.d_amount/b.u_val
		else a.curr_price*(cost_price+b.u_cost2-b.d_amount/b.u_val) end )

		else 0 end)/b.c_val i_u,
		(case when a.tr_type in(1,11) then b.qty_pr*b.u_val*b.c_val else 0 end) o_q,(case when a.tr_type in(1,11) then
		(case when a.curr_id=0 then b.sls_u_price-b.d_amount/b.u_val
		else a.curr_price*(sls_u_price-b.d_amount/b.u_val ) end )
		else 0 end)/b.c_val o_u,
		0 n_q,0 n_u,0 n_t,0 r_u,a.adj_id,b.e_date e_date,ifnull(a.id2,a.id) id2

		from bills_v a,bill_transactions_v b,items_v c
		where a.id=b.bill_id and b.item_id=c.id
		and a.tr_type in (1,2,11,21) and a.is_back=0
		--2024-12 old yrs
		and a.date_=b.bill_date


		union all

		-- purchase and sales with is_back=1
		--v1.120 adj2 no back with (11,21)
		select   b.item_id,a.br_id,a.br_id,a.date_,b.bill_id,a.tr_type,a.is_back,(select name from customers where id=a.cus_id) name,
		a.bill_no,a.bill_no2,
		(case when a.tr_type in(1,11)  then b.qty_pr*b.u_val*b.c_val else 0 end) i_q,
		(case when a.tr_type in(1,11) then
		(case when a.curr_id=0 then b.sls_u_price-b.d_amount/b.u_val
		else a.curr_price*(sls_u_price-b.d_amount/b.u_val) end )

		else 0 end)/b.c_val i_u,
		(case when a.tr_type=2 then b.qty_pr*b.u_val*b.c_val else 0 end) o_q,
		(case when a.tr_type=2 then
		(case when a.curr_id=0 then b.cost_price+b.u_cost2-b.d_amount/b.u_val
		else a.curr_price*(cost_price+b.u_cost2-b.d_amount/b.u_val ) end )
		else 0 end)/b.c_val o_u,
		0 n_q,0 n_u,0 n_t,0 r_u,a.adj_id,b.e_date e_date,ifnull(a.id2,a.id) id2

		from bills_v a,bill_transactions_v b,items_v c
		where a.id=b.bill_id and b.item_id=c.id
		and a.tr_type in (1,2) and a.is_back=1
		--2024-12 old yrs
		and a.date_=b.bill_date

		--Order Iusse...OUT
		union all
		select  b.item_id,a.br_id,a.to_br_id,a.date_,b.bill_id,a.tr_type,a.is_back,(select name from branches where id=a.to_br_id) name,
		a.bill_no,a.bill_no2,
		0 i_q,0 i_u,
		b.qty_pr*b.u_val*b.c_val o_q,0 o_u,
		0 n_q,0 n_u,0 n_t,0 r_u,a.adj_id,b.e_date e_date,ifnull(a.id2,a.id) id2
		from bills_v a,bill_transactions_v b,items_v c
		where a.id=b.bill_id and b.item_id=c.id
		and a.tr_type =3
		--2024-12 old yrs
		and a.date_=b.bill_date
		union all

		-- Order Issue...IN
		select  b.item_id ,a.to_br_id,a.br_id,a.date_,b.bill_id,a.tr_type,a.is_back,(select name from branches where id=a.br_id) name,
		a.bill_no,a.bill_no2,
		b.qty_pr*b.u_val*b.c_val i_q,0 i_u,
		0 o_q,0 o_u,
		0 n_q,0 n_u,0 n_t,0 r_u,a.adj_id,b.e_date e_date,ifnull(a.id2,a.id) id2
		from bills_v a,bill_transactions_v b,items_v c
		where a.id=b.bill_id and b.item_id=c.id
		and a.tr_type =3
		--2024-12 old yrs
		and a.date_=b.bill_date

		--Adj...IN
		union all
		select  b.item_id ,a.br_id,a.to_br_id,a.date_,b.bill_id,a.tr_type,a.is_back,
		(select name from adj_type where id=a.adj_id) name,
		a.bill_no,a.bill_no2,
		b.qty_pr*b.u_val*b.c_val i_q,b.cost_price/b.c_val i_u,
		0 o_q,0 o_u,
		0 n_q,0 n_u,0 n_t,0 r_u,a.adj_id,b.e_date e_date,ifnull(a.id2,a.id) id2
		from bills_v a,bill_transactions_v b,items_v c
		where a.id=b.bill_id and b.item_id=c.id
		and a.tr_type =4 and a.adj_id in (2,4)
		--2024-12 old yrs
		and a.date_=b.bill_date

		--Adj...OUT
		union all
		select  b.item_id ,a.br_id,a.to_br_id,a.date_,b.bill_id,a.tr_type,a.is_back,
		(select name from adj_type where id=a.adj_id) name,
		a.bill_no,a.bill_no2,
		0 i_q,0 i_u,
		b.qty_pr*b.u_val*b.c_val o_q,b.cost_price/b.c_val o_u,
		0 n_q,0 n_u,0 n_t,0 r_u,a.adj_id,b.e_date e_date,ifnull(a.id2,a.id) id2
		from bills_v a,bill_transactions_v b,items_v c
		where a.id=b.bill_id and b.item_id=c.id
		and a.tr_type =4 and a.adj_id not in(2,4)
		--2024-12 old yrs
		and a.date_=b.bill_date
		order by a.date_,id2,b.bill_id desc,b.item_id ;

	</statement>

	-- only to create view profit_loss_vw successfully
	<statement>create table if not exists items_cost_calc as
		select * from items_cost_calc_v;
	</statement>
	<statement>CREATE INDEX if not exists items_cost_calc_item_id on  items_cost_calc(item_id);	</statement>
	<statement>CREATE INDEX if not exists items_cost_calc_date_ on  items_cost_calc(date_);	</statement>
	--2024-08 speedUp update performance and item_det_all report
	<statement>CREATE INDEX if not exists items_cost_calc_br_bill_id on items_cost_calc(br_id,bill_id);	</statement>
	<statement>alter table items_cost_calc add adj_id integer default 0;	</statement>
	--##v1.119 end_date
	<statement>alter table items_cost_calc add e_date text;	</statement>
	--2024-03 id2 item balance negative balance issue when add purchase for zero qty
	<statement>alter table items_cost_calc add id2 integer;	</statement>
	--##v1.119 end_date Begin##
	<statement>
		drop view if exists items_cost_calc_end_yr;
	</statement>
	--v1.120 adj2
	<statement>
		create VIEW items_cost_calc_end_yr as
		select a.br_id,a.item_id,ifnull(a.e_date,'') e_date,
		ifnull(sum( case when ( (  substr(a.tr_type,1,1)*1=2 and a.is_back=0)  or (a.tr_type=1 and a.is_back=1)
		or (a.tr_type=3 and a.i_q!=0) or a.tr_type=0 or (a.tr_type=4 and a.adj_id in (2,4)) ) then a.i_q else 0 end)
		- sum( case when ( ( substr(a.tr_type,1,1)*1=1 and a.is_back=0)  or (a.tr_type=2 and a.is_back=1)
		or (a.tr_type=3 and a.o_q!=0) or (a.tr_type=4 and a.adj_id not in (2,4)) ) then a.o_q else 0 end),0) n_q
		from items_cost_calc_v a
		where date('now','localtime','start of year','-1 day')>=date(a.date_)
		group by a.br_id,a.item_id,ifnull(a.e_date,'');
	</statement>
	<statement>
		create table if not exists error_exception
		(id integer primary key autoincrement,desc_ text,
		now_ text default (strftime('%Y-%m-%d','now')));
	</statement>
	<statement>
		drop trigger if exists raise_error1_tr;
	</statement>
	<statement>
		CREATE TRIGGER if not EXISTS raise_error1_tr
		BEFORE INSERT  ON error_exception
		FOR EACH ROW
		WHEN (NEW.id=1 )
		BEGIN
		SELECT RAISE(ABORT, 'ERROR:Closing Year with Items Cost') ;
		END;
	</statement>

	--##v1.119 end_date END####

	<statement>
		drop VIEW profit_loss_vw;
	</statement>
	<statement>
		CREATE VIEW profit_loss_vw as
		select 1 id,
		ifnull(abs(sum( case when (case when (a.t_cus_id=a.b_id) then -1*a.out_ else
		(a.[in]*a.out_) end)>= 0 then  (case when (a.t_cus_id=a.b_id) then -1*a.out_ else
		(a.[in]*a.out_) end) else 0 end )),0) AS f3,
		ifnull(abs( sum(
		(case when a.b_id in(-8,-11,-9) then -1 else 1 end )*
		case when (case when (a.t_cus_id=a.b_id) then -1*a.out_ else
		(a.[in]*a.out_) end)>= 0 then 0 else  (case when (a.t_cus_id=a.b_id) then -1*a.out_ else
		(a.[in]*a.out_) end)  end )),0) AS f4,
		--Net Sales and Discount OUT
		'Net.Sales' ,
		date(strftime('%Y-%m-%d',substr(a.date_,7,4)||'-' ||substr(a.date_,4,2)||'-'||substr(a.date_,1,2) )) date_
		,a.b_name as b_name
		FROM cus_tr_curr_view a
		where  ( a.e_id=2 or a.b_id in (-7,-8))
		and a.a_id is not null
		and a.b_id in (-8,-11,-9,-1,-5)
		group by date(strftime('%Y-%m-%d',substr(a.date_,7,4)||'-' ||substr(a.date_,4,2)||'-'||substr(a.date_,1,2) ))
		,a.b_name

		union all

		select 2 id,
		sum(ifnull((case
		when (substr(a.tr_type,1,1) and a.is_back=0)  then a.o_q*a.n_u
		when (a.tr_type=1 and a.is_back=1)  then -1*a.i_q*a.r_u
		else 0 end) ,0)) AS f3,
		0 f4,
		'Cost.Sales' ,
		a.date_,''
		FROM items_cost_calc a

		group by a.date_

		union all

		select 2 id,
		ifnull(abs(sum(
		(case when a.b_id in(-7,-10,-12) then -1 else 1 end )*
		case when (case when (a.t_cus_id=a.b_id) then -1*a.out_ else
		(a.[in]*a.out_) end)>= 0 then  (case when (a.t_cus_id=a.b_id) then -1*a.out_ else
		(a.[in]*a.out_) end) else 0 end )),0) AS f3,
		ifnull(abs( sum(case when (case when (a.t_cus_id=a.b_id) then -1*a.out_ else
		(a.[in]*a.out_) end)>= 0 then 0 else  (case when (a.t_cus_id=a.b_id) then -1*a.out_ else
		(a.[in]*a.out_) end)  end )),0) AS f4,
		'Discount.IN' ,
		date(strftime('%Y-%m-%d',substr(a.date_,7,4)||'-' ||substr(a.date_,4,2)||'-'||substr(a.date_,1,2) )) date_
		,a.b_name
		FROM cus_tr_curr_view a
		where  ( a.e_id=2 or a.b_id in (-7,-8))
		and a.a_id is not null
		and a.b_id in (-7)

		group by date(strftime('%Y-%m-%d',substr(a.date_,7,4)||'-' ||substr(a.date_,4,2)||'-'||substr(a.date_,1,2) ))
		,a.b_name
		union
		select 5 id,
		ifnull(abs(sum(case when (case when (a.t_cus_id=a.b_id) then -1*a.out_ else
		(a.[in]*a.out_) end)>= 0 then  (case when (a.t_cus_id=a.b_id) then -1*a.out_ else
		(a.[in]*a.out_) end) else 0 end )),0) AS f3,
		ifnull(abs( sum(case when (case when (a.t_cus_id=a.b_id) then -1*a.out_ else
		(a.[in]*a.out_) end)>= 0 then 0 else  (case when (a.t_cus_id=a.b_id) then -1*a.out_ else
		(a.[in]*a.out_) end)  end )),0) AS f4,
		'Income' ,
		date(strftime('%Y-%m-%d',substr(a.date_,7,4)||'-' ||substr(a.date_,4,2)||'-'||substr(a.date_,1,2) )) date_
		,a.b_name
		FROM cus_tr_curr_view a
		where  ( a.f_id like '4%' and a.f_id not in(41) )
		and a.a_id is not null
		and a.b_id >0

		group by date(strftime('%Y-%m-%d',substr(a.date_,7,4)||'-' ||substr(a.date_,4,2)||'-'||substr(a.date_,1,2) ))
		,a.b_name
		union
		select 6 id,
		ifnull(abs(sum(case when (case when (a.t_cus_id=a.b_id) then -1*a.out_ else
		(a.[in]*a.out_) end)>= 0 then  (case when (a.t_cus_id=a.b_id) then -1*a.out_ else
		(a.[in]*a.out_) end) else 0 end )),0) AS f3,
		ifnull(abs( sum(case when (case when (a.t_cus_id=a.b_id) then -1*a.out_ else
		(a.[in]*a.out_) end)>= 0 then 0 else  (case when (a.t_cus_id=a.b_id) then -1*a.out_ else
		(a.[in]*a.out_) end)  end )),0) AS f4,
		'Outcome' ,
		date(strftime('%Y-%m-%d',substr(a.date_,7,4)||'-' ||substr(a.date_,4,2)||'-'||substr(a.date_,1,2) )) date_
		,a.b_name
		FROM cus_tr_curr_view a
		where  ( a.f_id like '3%' and a.f_id not in (31) )
		and a.a_id is not null
		and a.b_id >0

		group by date(strftime('%Y-%m-%d',substr(a.date_,7,4)||'-' ||substr(a.date_,4,2)||'-'||substr(a.date_,1,2) ))
		,a.b_name;


	</statement>



	--Screen table
	<statement>create table if not exists screens (id integer primary key,name text,p_id integer default 0);
	</statement>
	<statement>delete from screens;</statement>
	<statement>insert or ignore into screens(id,name,p_id) values(1,'عمليات مخزنية',0);</statement>
	<statement>insert or ignore into screens(id,name,p_id) values(2,'قيود وحسابات',0);</statement>
	<statement>insert or ignore into screens(id,name,p_id) values(3,'أصناف',0);</statement>
	<statement>insert or ignore into screens(id,name,p_id) values(4,'العملات ',0);</statement>
	<statement>insert or ignore into screens(id,name,p_id) values(5,'التقارير',0);</statement>
	<statement>insert or ignore into screens(id,name,p_id) values(6,'تحويل مخزني',1);</statement>
	<statement>insert or ignore into screens(id,name,p_id) values(7,'تسوية مخزنية',1);</statement>
	<statement>insert or ignore into screens(id,name,p_id) values(8,'إضافة مخزن',1);</statement>
	<statement>insert or ignore into screens(id,name,p_id) values(28,'جرد مخزني',1);</statement>
	--v1.120 adj2
	<statement>insert or ignore into screens(id,name,p_id) values(-2,'توريد مخزني',1);</statement>
	<statement>insert or ignore into screens(id,name,p_id) values(-3,'صرف مخزني',1);</statement>

	<statement>insert or ignore into screens(id,name,p_id) values(9,'قيد يومي',2);</statement>
	<statement>insert or ignore into screens(id,name,p_id) values(10,'قيد إفتتاحي',2);</statement>
	<statement>insert or ignore into screens(id,name,p_id) values(11,'إضافة حساب',2);</statement>
	<statement>insert or ignore into screens(id,name,p_id) values(12,'حركة الصندوق',2);</statement>
	<statement>insert or ignore into screens(id,name,p_id) values(13,'دليل الحسابات',2);</statement>

	<statement>insert or ignore into screens(id,name,p_id) values(14,'إقفال سنوي',2);</statement>
	<statement>insert or ignore into screens(id,name,p_id) values(15,'الأصناف',3);</statement>
	<statement>insert or ignore into screens(id,name,p_id) values(16,'أسعار البيع',3);</statement>
	<statement>insert or ignore into screens(id,name,p_id) values(27,'وحدات الصنف',3);</statement>
	<statement>insert or ignore into screens(id,name,p_id) values(29,' فاتورة عرض سعر',3);</statement>
	<statement>insert or ignore into screens(id,name,p_id) values(30,'  طلب شراء',3);</statement>
	<statement>--insert or ignore into screens(id,name,p_id) values(17,'المخزون',3);</statement>
	<statement>--insert or ignore into screens(id,name,p_id) values(18,'أرباح الأصناف',3);</statement>
	<statement>insert or ignore into screens(id,name,p_id) values(19,'إضافة عملة',4);</statement>
	<statement>insert or ignore into screens(id,name,p_id) values(20,'سعر العملات',4);</statement>
	<statement>insert or ignore into screens(id,name,p_id) values(31,'سقف الحساب',4);</statement>
	--v1.101 curr sales
	--insert or ignore into screens(id,name,p_id) values(32,'بيع وشراء العملات',4);


	<statement>insert or ignore into screens(id,name,p_id) values(21,'حركة الأصناف',5);</statement>
	<statement>--insert or ignore into screens(id,name,p_id) values(22,'المخزون المتبقي',5);</statement>
	<statement>insert or ignore into screens(id,name,p_id) values(23,'ميزان المراجعة',5);</statement>
	<statement>insert or ignore into screens(id,name,p_id) values(24,'قائمة الدخل',5);</statement>
	<statement>insert or ignore into screens(id,name,p_id) values(25,'المركز المالي',5);</statement>
	<statement>insert or ignore into screens(id,name,p_id) values(26,'تقارير أخرى',5);</statement>
	-- ## begin v1.135 users
	<statement>alter table screens add is_active integer default 1</statement>
	<statement>alter table screens add new integer default 1</statement>
	<statement>alter table screens add edit integer default 1</statement>
	<statement>alter table screens add view integer default 1</statement>
	<statement>alter table screens add del integer default 1</statement>
	<statement>insert or ignore into screens(id,name,p_id,is_active) values(-4,'حفظ نسخة احتياطية',1,0);</statement>
	<statement>insert or ignore into screens(id,name,p_id,is_active) values(-5,'استرجاع نسخة احتياطية',1,0);</statement>
	<statement>insert or ignore into screens(id,name,p_id,is_active) values(-6,'المبيعات',1,0);</statement>
	<statement>insert or ignore into screens(id,name,p_id,is_active) values(-7,'المشتريات',1,0);</statement>
	<statement>insert or ignore into screens(id,name,p_id,is_active) values(-8,'قبض/صرف',1,0);</statement>
	<statement>insert or ignore into screens(id,name,p_id,is_active) values(-9,'الحسابات',1,0);</statement>

	<statement>update screens set new=-1,edit=-1,del=-1 where p_id=5;</statement>
	<statement>update screens set new=-1,edit=-1,del=-1 where id in(12,14);</statement>
	<statement>update screens set edit=-1 where id in(16,20);</statement>
	<statement>update screens set new=-1,edit=-1,del=-1 where id in(-4,-5);</statement>

	--## end v1.135 users

	--Config table
	<statement>create table if not exists sys_conf (id integer PRIMARY KEY,desc_ text,value_ text);
	</statement>
	<statement>insert or ignore into sys_conf (id ,desc_ ,value_ ) values (1,'Account Cash','-3');</statement>
	--fix removed def account
	<statement>update sys_conf set value_=-3 where id=1 and
		not EXISTS(select 1 from customers a where a.id=value_);</statement>
	<statement>insert or ignore into sys_conf (id ,desc_ ,value_ ) values (2,'F-DATE','now');</statement>
	<statement>insert or ignore into sys_conf (id ,desc_ ,value_ ) values (3,'CURR','0');</statement>
	<statement>insert or ignore into sys_conf (id ,desc_ ,value_ ) values (4,'Fund Cash','-13');</statement>
	--VAT feature
	<statement>insert or ignore into sys_conf (id ,desc_ ,value_ ) values (5,'VAT Acc','-18');</statement>
	<statement>insert or ignore into sys_conf (id ,desc_ ,value_ ) values (6,'VAT enable','0');</statement>
	--online
	<statement>insert or ignore into sys_conf (id ,desc_ ,value_ ) values (8,'share_type','0');</statement>
	--v1.101 diff_id
	<statement>insert or ignore into sys_conf (id ,desc_ ,value_ ) values (9,'curr_diff_id','-27');</statement>
	--v1.119 end_date
	<statement>insert or ignore into sys_conf (id ,desc_ ,value_ ) values (10,'show_end_date','0');</statement>
	--v1.136 sync
	<statement>insert or ignore into sys_conf (id ,desc_ ,value_ ) values (100,'dev_id','');</statement>
	--v1.136 sync 2024-12
	<statement>insert or ignore into sys_conf (id ,desc_ ,value_ ) values (101,'online2','0');</statement>
	---######### TEMP QUERIES #########################
	--update null daily transactoins number
	<statement>update transactions
		set p_ref_no=p_id
		where bill_id=-2 and p_ref_no='';
	</statement>

	<statement>update bills
		set bill_no2=(select count(*) from bills b
		where b.id
		--less than char.
		&#60;=bills.id and b.tr_type=bills.tr_type  and b.is_back=bills.is_back
		and b.br_id=bills.br_id  )
		where bill_no2 =0;
	</statement>

	-- TEMP to fetch bill_no to transactions table
	<statement>
		update transactions
		set p_ref_no=(select bill_no2 from bills where id=bill_id)
		where bill_id>0 and p_ref_no is null;
	</statement>

	--#####################################END

	<statement>
		drop INDEX if EXISTS uq_bills_no ;
	</statement>
	--2024-08 fix duplicated bill_no2 issue due to uq_bills_no3 issue
	<statement>
		update	 bills
		set bill_no2=(select ifnull(max(a.bill_no2),0)+1   as _id from bills a
		where a.tr_type=bills.tr_type and a.is_back=bills.is_back and a.br_id=bills.br_id and a.adj_id=bills.adj_id
		and ifnull(a.online_ref2,0)=ifnull(bills.online_ref2,0)
		),remarks=ifnull(remarks,'')||'#'||bill_no2
		where id in(
		select max(id)
		from bills
		where online_ref2 is null
		group by tr_type,is_back,br_id,bill_no2,adj_id,online_ref2
		HAVING count(*)>1
		) and online_ref2 is null;
	</statement>
	--v1.136 sync uq_bills_no2 is replaced by uq_bills_no3 which include online_ref2
	<statement>CREATE UNIQUE  INDEX  if not EXISTS uq_bills_no2 ON bills(tr_type,is_back,br_id,bill_no2,adj_id);</statement>

	--2024-08 cus_search_tmp performance..sum and join in single query issue
	<statement>drop view customers_tree_v</statement>
	<statement>create view if not exists customers_tree_v as
		select b.id b_id,b.name b_name,c.id c_id,c.name c_name,e.id e_id,e.name e_name,f.id f_id,f.name f_name,b.gsm
		from groups c LEFT join customers AS b on b.g_id=c.id
		INNER join  cus_type e on b.cus_type_id=e.id
		inner join  account_tree f on b.acc_p_id=f.id;
	</statement>
	<statement>drop view transactions_tot_v</statement>
	<statement>
		create view transactions_tot_v as
		select a.curr_id curr_id, a.cus_id cus_id,a.t_cus_id t_cus_id,a.[in] [in],sum(a.out) out
		,max(strftime('%Y-%m-%d',substr(a.date_,7,4)||'-' ||substr(a.date_,4,2)||'-'||substr(a.date_,1,2) )) d,count(*) cnt
		,max(a.id) a_id,(select name from currency d where d.id=a.curr_id) d_name
		from transactions_v a
		group by a.curr_id , a.cus_id ,a.t_cus_id ,a.[in] ;
	</statement>
	-- to update PC view version
	<statement>drop  view cus_curr; </statement>
	<statement>
		CREATE VIEW cus_curr as

		SELECT b.b_id as ID, b.b_name as name,b.gsm gsm ,b.c_id as g_id, b.c_name g_name ,b.f_id as f_id  ,b.f_name,
		sum(case when not(a.[in] = 1 and (a.t_cus_id!=b.b_id or a.t_cus_id is null)) then a.out else 0 end )*1.0 as cr ,
		sum  ( case when a.[in] = 1 and (a.t_cus_id!=b.b_id or a.t_cus_id is null) then a.out else 0 end)*1.0 as db ,
		ifnull( (sum(case when (a.t_cus_id=b.b_id) then -1*a.out else  (a.[in]*a.out) end)),0) AS balance,
		ifnull( a.d_name, (select name from currency where id=0))  curr
		,a.curr_id as curr_id,b.gsm as phone
		,max(a.d) d
		, ( julianday(strftime('%Y-%m-%d','now'))
		-julianday(max(a.d))
		) as days_late,
		ifnull(max(a.a_id),0) max_id,sum(a.cnt) cnt

		from transactions_tot_v a,customers_tree_v b
		where (a.cus_id=b.b_id or a.t_cus_id=b.b_id)
		GROUP BY b.b_name, b.b_id,a.d_name,b.c_id,b.c_name,b.f_id,b.f_name,b.gsm,a.curr_id
		union
		select a.id,a.name,a.gsm,a.g_id,(select name from groups where id=a.g_id) g_name,a.acc_p_id,
		(select name from account_tree where id=a.acc_p_id) f_name,
		0,0,0,
		(select name from currency where id=0),0,a.gsm,strftime('%Y-%m-%d','now'),0,0,0
		from customers a
		where a.id not in (select distinct cus_id from transactions union  select distinct t_cus_id from transactions where t_cus_id is not null)
		ORDER BY d DESC,max_id desc;
	</statement>
	<statement>
		--CREATE VIEW cus_curr as
		--SELECT a.b_id as ID, a.b_name as name,a.gsm gsm ,a.c_id as g_id, a.c_name g_name ,a.f_id as f_id  ,a.f_name,
		--sum(case when not(a.[in] = 1 and (a.t_cus_id!=a.b_id or a.t_cus_id is null)) then a.out else 0 end )*1.0 as cr ,
		--sum  ( case when a.[in] = 1 and (a.t_cus_id!=a.b_id or a.t_cus_id is null) then a.out else 0 end)*1.0 as db ,
		--ifnull( (sum(case when (a.t_cus_id=a.b_id) then -1*a.out else  (a.[in]*a.out) end)),0) AS balance,
		--ifnull( a.d_name, (select name from currency where id=0))  curr
		--,a.curr_id as curr_id,a.gsm as phone
		--,max(strftime('%Y-%m-%d',substr(a.date_,7,4)||'-' ||substr(a.date_,4,2)||'-'||substr(a.date_,1,2) )) d
		--,( (select julianday(strftime('%Y-%m-%d','now'))
		--	-julianday(max(strftime('%Y-%m-%d',substr(c.date_,7,4)||'-'
		--||substr(c.date_,4,2)||'-'||substr(c.date_,1,2) )))
		--from transactions_v as c where (c.cus_id=a.b_id or c.t_cus_id=a.b_id)) ) as days_late,
		--ifnull(max(a.a_id),0) max_id,count(*) cnt
		--FROM  cus_tr_curr_view a
		--GROUP BY a.b_name, a.b_id,a.d_name,a.d_id,a.c_id,a.c_name,a.f_id,a.f_name,a.gsm,a.curr_id,a.d_name
		--union
		--select a.id,a.name,a.gsm,a.g_id,(select name from groups where id=a.g_id) g_name,a.acc_p_id,
		--(select name from account_tree where id=a.acc_p_id) f_name,
		--0,0,0,
		--(select name from currency where id=0),0,a.gsm,strftime('%Y-%m-%d','now'),0,0,0
		--from customers a
		--where a.id not in (select distinct b_id from cus_tr_curr_view)
		--ORDER BY d DESC,max_id desc ;
	</statement>

	--###end 2024-08 cus_search_tmp performance..sum and join in single query issue


	--fix negative numbers
	<statement>update transactions set out=abs(out) where out!=abs(out) and not (out>0)
		and (date(strftime('%Y-%m-%d',substr( date_,7,4)||'-' ||substr( date_,4,2)||'-'||substr( date_,1,2) )) !=
		(select ifnull(max(date_),date(date(strftime('%Y-%m-%d',substr(transactions.date_,7,4)||'-' ||substr(transactions.date_,4,2)||'-'||substr(transactions.date_,1,2) )),'-1 day'))
		from closing_year)); </statement>
	<statement>
		create table if not exists closing_year
		(id integer primary key autoincrement,date_ text, cnt real default 0,
		now_ text default (strftime('%Y-%m-%d','now')));
	</statement>

	-- Prevent unkown transactions
	--2024-01 or NEW.cus_id=ifnull(NEW.t_cus_id,0) NOT Tested till now
	<statement>drop TRIGGER if exists prevent_invalid_tr; </statement>
	<statement>
		CREATE TRIGGER prevent_invalid_tr
		BEFORE INSERT  ON transactions
		FOR EACH ROW
		WHEN (NEW.cus_id is null or not(NEW.out*1>=0) or NEW.cus_id=ifnull(NEW.t_cus_id,0)
		and (select is_active from trigger_flags)=1
		)
		BEGIN
		SELECT RAISE(ABORT, 'ERROR:Invalid Transaction') ;
		END;
	</statement>
	--refresh opening balance to be adapted with account#13
	<statement>
		update transactions set bill_id=-1 where bill_id=-1 and t_cus_id is null;
	</statement>

	-- Closed Year constaints --Transactions--Bills--Items
	--########### Begin
	<statement>
		CREATE TRIGGER if not exists closing_year_update_tr
		BEFORE update  ON transactions
		FOR EACH ROW
		WHEN (date(strftime('%Y-%m-%d',substr( OLD.date_,7,4)||'-' ||substr( OLD.date_,4,2)||'-'||substr( OLD.date_,1,2) ))&#60;=
		(select ifnull(max(date_),date(date(strftime('%Y-%m-%d',substr( OLD.date_,7,4)||'-' ||substr( OLD.date_,4,2)||'-'||substr( OLD.date_,1,2) )),'-1 day')) from closing_year))
		BEGIN
		SELECT RAISE(ABORT, 'CLOSED YEAR') ;
		END;

	</statement>

	--recreate in closing year(drop then recreate)
	<statement>
		CREATE TRIGGER if not exists closing_year_delete_tr
		BEFORE delete  ON transactions
		FOR EACH ROW
		WHEN (date(strftime('%Y-%m-%d',substr( OLD.date_,7,4)||'-' ||substr( OLD.date_,4,2)||'-'||substr( OLD.date_,1,2) ))&#60;=
		(select ifnull(max(date_),date(date(strftime('%Y-%m-%d',substr( OLD.date_,7,4)||'-' ||substr( OLD.date_,4,2)||'-'||substr( OLD.date_,1,2) )),'-1 day')) from closing_year))
		BEGIN
		SELECT RAISE(ABORT, 'CLOSED YEAR') ;
		END;

	</statement>
	<statement>
		CREATE TRIGGER if not exists closing_year_insert_tr
		BEFORE insert  ON transactions
		FOR EACH ROW
		WHEN (date(strftime('%Y-%m-%d',substr( NEW.date_,7,4)||'-' ||substr( NEW.date_,4,2)||'-'||substr( NEW.date_,1,2) ))
		&#60;=
		(select ifnull(max(date_),date(date(strftime('%Y-%m-%d',substr( NEW.date_,7,4)||'-'
		||substr( NEW.date_,4,2)||'-'||substr( NEW.date_,1,2) )),'-1 day')) from closing_year))
		BEGIN
		SELECT RAISE(ABORT, 'CLOSED YEAR') ;
		END;

	</statement>
	<statement>
		CREATE TRIGGER if not exists closing_year_update_bills
		BEFORE update ON bills
		FOR EACH ROW
		WHEN (date(OLD.date_)&#60;=
		(select ifnull(max(date_),date(OLD.date_,'-1 day')) from closing_year))
		BEGIN
		SELECT RAISE(ABORT, 'CLOSED YEAR') ;
		END;
	</statement>
	--v195 paid is large than bill amount
	<statement>drop trigger if exists paid_amount_update_bills</statement>
	<statement>
		CREATE TRIGGER if not EXISTS paid_amount_update_bills
		BEFORE update ON bills
		FOR EACH ROW
		WHEN (NEW.paid_amount>NEW.amount and NEW.tr_type in(1,2) )
		BEGIN
		SELECT RAISE(ABORT, 'paid amount Error') ;
		END;
	</statement>
	--2024-08 prevent empty bill_no2
	<statement>
		CREATE TRIGGER if not EXISTS invalid_update_bills
		BEFORE update ON bills
		FOR EACH ROW
		WHEN (NEW.bill_no2='' )
		BEGIN
		SELECT RAISE(ABORT, 'Invalid Bill') ;
		END;
	</statement>

	-- recreate in fun in closing year
	<statement>
		CREATE TRIGGER if not exists closing_year_delete_bills
		BEFORE delete ON bills
		FOR EACH ROW
		WHEN (date(OLD.date_)&#60;=
		(select ifnull(max(date_),date(OLD.date_,'-1 day')) from closing_year))
		BEGIN
		SELECT RAISE(ABORT, 'CLOSED YEAR') ;
		END;
	</statement>
	<statement>
		CREATE TRIGGER if not exists closing_year_insert_bills
		BEFORE insert ON bills
		FOR EACH ROW
		WHEN (date(NEW.date_)&#60;=
		(select ifnull(max(date_),date(NEW.date_,'-1 day')) from closing_year))
		BEGIN
		SELECT RAISE(ABORT, 'CLOSED YEAR') ;
		END;
	</statement>
	-- 2024-04 prevent closing year for bills2(7)
	<statement>
		CREATE TRIGGER closing_year_delete_bills2
		BEFORE delete ON bills2
		FOR EACH ROW
		WHEN OLD.tr_type=7 and (date(OLD.date_)&#60;=
		(select ifnull(max(date_),date(OLD.date_,'-1 day')) from closing_year))
		BEGIN
		SELECT RAISE(ABORT, 'CLOSED YEAR') ;
		END;
	</statement>
	<statement>drop TRIGGER if exists closing_year_insert_bills2;</statement>
	<statement>
		CREATE TRIGGER closing_year_insert_bills2
		BEFORE insert ON bills2
		FOR EACH ROW
		WHEN NEW.tr_type=7 and (date(NEW.date_)&#60;=
		(select ifnull(max(date_),date(NEW.date_,'-1 day')) from closing_year))
		BEGIN
		SELECT RAISE(ABORT, 'CLOSED YEAR') ;
		END;
	</statement>
	--2024-08
	<statement>
		CREATE TRIGGER closing_year_update_bills2
		BEFORE update ON bills2
		FOR EACH ROW
		WHEN OLD.tr_type=7 and (date(OLD.date_)&#60;=
		(select ifnull(max(date_),date(OLD.date_,'-1 day')) from closing_year))
		BEGIN
		SELECT RAISE(ABORT, 'CLOSED YEAR') ;
		END;
	</statement>

	<statement>
		drop trigger if exists closing_year_update_items;
	</statement>
	-- also recreated in closing year function
	<statement>
		CREATE TRIGGER if not exists closing_year_update_items
		BEFORE update ON items
		FOR EACH ROW
		WHEN (date(OLD.o_date)&#60;=
		(select ifnull(max(date_),date(OLD.o_date,'-1 day')) from closing_year)
		-- to allow change barcode after closing year
		and (OLD.o_date!=NEW.o_date or OLD.o_qty!=NEW.o_qty or OLD.o_cost!=NEW.o_cost
		-- to allow change base unit after closing year--2023-01-23
		--or OLD.unit_id!=NEW.unit_id
		)
		)
		BEGIN
		SELECT RAISE(ABORT, 'CLOSED YEAR') ;
		END;
	</statement>

	<statement>
		CREATE TRIGGER if not exists closing_year_delete_items
		BEFORE delete ON items
		FOR EACH ROW
		WHEN (date(OLD.o_date)&#60;=
		(select ifnull(max(date_),date(OLD.o_date,'-1 day')) from closing_year))
		BEGIN
		SELECT RAISE(ABORT, 'CLOSED YEAR') ;
		END;
	</statement>

	<statement>
		CREATE TRIGGER if not exists closing_year_insert_items
		BEFORE insert ON items
		FOR EACH ROW
		WHEN (date(NEW.o_date)&#60;=
		(select ifnull(max(date_),date(NEW.o_date,'-1 day')) from closing_year))
		BEGIN
		SELECT RAISE(ABORT, 'CLOSED YEAR') ;
		END;
	</statement>
	--############# END

	--2024-01 prevent qty with zero
	--2024-12 prevent item_type_id is null
	--2024-12 prevent Neg cost_price
	--#### Begin
	<statement>drop TRIGGER if exists prevent_invalid_bill_trans; </statement>
	<statement>
		CREATE TRIGGER prevent_invalid_bill_trans
		BEFORE INSERT  ON bill_transactions
		FOR EACH ROW
		WHEN (not(NEW.qty_pr>0) or NEW.item_type_id is null or not(NEW.cost_price>=0)
        or NEW.bill_id is null
        )
		BEGIN
		SELECT RAISE(ABORT, 'ERROR:Invalid Bill Transaction') ;
		END;
	</statement>
	--2024-12
	<statement>drop TRIGGER if exists prevent_invalid_bill_trans_update; </statement>
	<statement>
		CREATE TRIGGER prevent_invalid_bill_trans_update
		BEFORE UPDATE  ON bill_transactions
		FOR EACH ROW
		WHEN (not(NEW.qty_pr>0) or NEW.item_type_id is null or not(NEW.cost_price>=0)
        or NEW.bill_id is null
        )
		BEGIN
		SELECT RAISE(ABORT, 'ERROR:Invalid Bill Transaction') ;
		END;
	</statement>
	<statement>drop TRIGGER if exists prevent_invalid_bill_trans2; </statement>
	<statement>
		--	CREATE TRIGGER prevent_invalid_bill_trans2
		--	BEFORE INSERT  ON bill_transactions2
		--	FOR EACH ROWF
		--	WHEN (not(NEW.qty_pr>0) )
		--	BEGIN
		--	SELECT RAISE(ABORT, 'ERROR:Invalid Bill Transaction2') ;
		--	END;
	</statement>




	--### End


	<statement>
		CREATE TABLE IF NOT EXISTS  notifications (
		`id` INTEGER PRIMARY KEY,
		`name` TEXT ,
		date_              text default (strftime('%Y-%m-%d','now')),
		status integer default 0,
		param1 text,
		param2 text
		)
	</statement>

	<statement>
		CREATE TABLE IF NOT EXISTS `chat_rooms` (
		`chat_room_id` INTEGER PRIMARY KEY,
		`name` TEXT ,
		`level_` INTEGER,
		`last_msg` TEXT,
		created_at TEXT
		)
	</statement>
	<statement>
		CREATE TABLE IF NOT EXISTS `messages` (
		`message_id` INTEGER PRIMARY KEY AUTOINCREMENT,
		`chat_room_id` INTEGER NOT NULL,
		`user_name` TEXT NOT NULL,
		`user_id` INTEGER NOT NULL,
		`message` text NOT NULL,
		`created_at` TEXT NOT NULL,
		server_id INTEGER,
		reply_id INTEGER default 0,
		reply_msg text,
		reply_to text,
		unique(server_id, chat_room_id)
		)
	</statement>

	<statement>
		--fix issue items
		update items set item_type_id=0 where item_type_id is null ;
	</statement>
	--fix currency issue
	--2024-10 fix
	<statement>
		update transactions set curr_price=0 where curr_id=0 and curr_mod=0 and curr_price not in(0,1)
		and ROWID in(select b.rowid from transactions b where b.curr_id=0 and b.curr_mod=0 and b.curr_price not in(0,1) limit 100);
	</statement>

	--2024-10
	<statement>
		update transactions set p_curr_price=0 where p_curr_id=0 and curr_mod=0 and p_curr_price not in(0,1)
		and ROWID in(select b.rowid from transactions b where b.p_curr_id=0 and b.curr_mod=0 and b.p_curr_price not in(0,1) limit 100);
	</statement>
	--fix doc p_remarks print
	<statement>
		update transactions
		set remarks=(select name  as _id from tran_type where id=tr_type)||(case when p_remarks=null or p_remarks='' then '' else '/'||p_remarks end)||'#'||p_ref_no
		where bill_id=-3 and cus_id=cash_id   and p_remarks!='' and remarks=(select name  as _id from tran_type where id=tr_type)||'#'||p_ref_no;
	</statement>

	--####### online requests
	<statement>
		CREATE TABLE IF NOT EXISTS requests
		(
		`id`	INTEGER PRIMARY KEY AUTOINCREMENT,
		date_            text default (strftime('%Y-%m-%d','now')),
		f_user_id integer,t_user_id integer,
		f_br_id integer,t_br_id integer,
		ref_no text,tr_type integer,acc_type integer,
		json_ text,status integer
		);
	</statement>
	<statement>
		CREATE TABLE IF NOT EXISTS requests_bills
		(
		`id`	INTEGER PRIMARY KEY AUTOINCREMENT,
		bill_id integer,
		bill_sys integer,
		date_            text default (strftime('%Y-%m-%d','now')),
		remarks         text,
		bill_no text ,-- used for return bill
		curr_name text,
		curr_id          integer default 0 references currency(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		bill_type integer default 0,
		tr_type integer references tran_type(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		is_back            integer default 0,
		br_id          integer default 0 references branches(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		cus_name text,
		cus_id integer  ,
		tran_status integer,
		to_br_id integer  references branches(id) ON UPDATE RESTRICT ON DELETE RESTRICT ,
		amount real default 0,
		d_amount real default 0,
		tax_amount             real default 0,
		param1 text,
		param2 text,
		param3 text,
		curr_price real default 0,
		bill_no2 integer default 0,
		cash_name text,
		cash_id integer default -3,


		status integer default 0,
		unique(bill_sys)
		);
	</statement>
	<statement>
		alter table requests_bills add adj_id  integer default 0;
	</statement>
	<statement>
		alter table requests_bills add remarks  text;
	</statement>
	<statement>
		CREATE TABLE IF NOT EXISTS requests_bills_det
		(
		bill_id                  integer references requests_bills(bill_sys) ON UPDATE RESTRICT ON DELETE RESTRICT,
		item_name text,
		item_unit text,
		item_id                    integer references items(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		item_type_id                    integer references item_type(id) ,
		curr_id2                    integer references currency(id) ,
		qty                          integer,
		cost_price                    real default 0,
		sls_u_price               	real default 0,
		d_amount2 real default 0,

		cash_id2 integer default -3,
		remark2         text,
		unit_name text,
		unit_id integer default 0 references units(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		u_val real default 1,
		base_name text,
		base_unit integer default -1,
		qty_pr real,
		qty_t text
		);
	</statement>
	<statement>
		alter table bills add online integer default 1;
	</statement>
	<statement>
		alter table bills_h add online integer default 1;
	</statement>
	<statement>
		alter table bills add online_ref integer default 0;
	</statement>
	<statement>
		alter table bills_h add online_ref integer default 0;
	</statement>
	<statement>
		alter table transactions add online integer default 1;
	</statement>
	<statement>
		alter table transactions_h add online integer default 1;
	</statement>
	<statement>
		alter table transactions_h2 add online integer default 1;
	</statement>
	<statement>
		alter table transactions add online_ref integer default 0;
	</statement>
	<statement>
		alter table transactions_h add online_ref integer default 0;
	</statement>
	<statement>
		alter table transactions_h2 add online_ref integer default 0;
	</statement>

	<statement>
		alter table bills2 add online integer default 1;
	</statement>

	<statement>
		alter table bills2 add online_ref integer default 0;
	</statement>

	<statement>
		CREATE TABLE IF NOT EXISTS requests_out
		(
		`id`	INTEGER PRIMARY KEY AUTOINCREMENT,
		date_            text default (strftime('%Y-%m-%d','now')),
		_un text,_p_un text,
		f_br_id integer,t_br_id integer,
		ref_no text,tr_type integer,acc_type integer,action_type integer default 0,
		json_ text,status integer default 0
		);
	</statement>
	<statement>
		CREATE TABLE IF NOT EXISTS doc_hdr
		(
		`id`	INTEGER PRIMARY KEY AUTOINCREMENT,
		tr_id integer,
		tr_sys integer,
		`cus_id`	INTEGER,
		cus_name text,
		`in`	TEXT,
		`out`	TEXT,
		`date_`	TEXT,
		`remarks`	TEXT,
		now_ TEXT,
		`param1`	TEXT,
		`param2`	TEXT,
		t_cus_id INTEGER ,
		t_cus_name text,
		`f1`	TEXT,
		`f2`	TEXT,
		`f3`	TEXT,
		curr_id integer default 0 references currency(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		curr_name text,
		bill_id integer default 0,
		p_id integer default 0,
		p_remarks text,
		p_amount real default 0,
		p_status integer default 0,
		p_ref_no text,
		d_amount real default 0,
		d_remarks text,
		p_curr_id integer default 0,
		p_curr_name text,
		curr_price real default 0,
		p_curr_price real default 0,
		p_date text,
		cash_id integer default -3,
		cash_name text,
		curr_mod integer default 0,
		status integer default 0,
		tr_type integer references tran_type(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		unique(tr_sys)


		);
	</statement>
	<statement>
		CREATE TABLE IF NOT EXISTS doc_det
		(
		tr_id                  integer references doc_hdr(tr_sys) ON UPDATE RESTRICT ON DELETE RESTRICT,
		`cus_id`	INTEGER,
		cus_name text,
		`in`	TEXT,
		`out`	TEXT,
		`date_`	TEXT,
		`remarks`	TEXT,
		now_ TEXT,
		`param1`	TEXT,
		`param2`	TEXT,
		t_cus_id INTEGER ,
		t_cus_name text,
		`f1`	TEXT,
		`f2`	TEXT,
		`f3`	TEXT,
		curr_id integer default 0 references currency(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		curr_name text,
		bill_id integer default 0,
		p_id integer default 0,
		p_remarks text,
		p_amount real default 0,
		p_status integer default 0,
		p_ref_no text,
		d_amount real default 0,
		d_remarks text,
		p_curr_id integer default 0,
		p_curr_name text,
		curr_price real default 0,
		p_curr_price real default 0,
		p_date text,
		cash_id integer default -3,
		cash_name text,
		curr_mod integer default 0,
		tr_type integer references tran_type(id) ON UPDATE RESTRICT ON DELETE RESTRICT

		);
	</statement>
	<statement>
		alter table doc_hdr add req_id integer;
	</statement>
	<statement>
		alter table requests_bills add req_id integer;
	</statement>
	<statement>
		alter table requests_bills add req_id integer;
	</statement>

	<statement>
		CREATE TABLE IF NOT EXISTS requests_notify
		(
		id	INTEGER PRIMARY KEY AUTOINCREMENT,
		title text,date_ text default (strftime('%Y-%m-%d','now'))
		,message text,param1 text,param2 text

		);
	</statement>

	<statement>
		CREATE TABLE IF NOT EXISTS requests_items
		(
		id	INTEGER PRIMARY KEY AUTOINCREMENT,

		req_id integer,
		date_            text default (strftime('%Y-%m-%d','now')),

		item_name text,
		item_id          integer references items(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		unit_name text,
		unit_id          integer references units(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		o_date text,

		status integer default 0,
		unique(req_id,item_name)

		);
	</statement>

	<statement>
		CREATE TABLE IF NOT EXISTS requests_items_unit
		(
		req_id                  integer ,
		item_name text,
		item_id          integer references items(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		unit_name text,
		unit_id          integer references units(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		u_val real default 1
		);
	</statement>

	<statement>
		CREATE TABLE IF NOT EXISTS requests_items_price
		(
		req_id                  integer ,
		date_ text default (strftime('%Y-%m-%d','now')),
		item_name text,
		item_id          integer references items(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		unit_name text,
		unit_id          integer references units(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		curr_name text,
		curr_id          integer references currency(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		sls_u_price real default 1
		);
	</statement>
	<statement>
		alter table requests add err text;
	</statement>

	<statement>
		CREATE TABLE IF NOT EXISTS requests_names
		(
		id	INTEGER PRIMARY KEY AUTOINCREMENT,

		req_id integer,


		cus_name text,
		cus_id          integer references customers(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		g_name text,
		g_id          integer references groups(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		gsm text,
		address text,
		status integer default 0,
		unique(req_id,cus_name)

		);
	</statement>
	<statement>
		alter table requests_names add acc_p_id text;
	</statement>
	--###update online requests--items--customers--currency--def 0
	<statement>
		alter table items add online integer default 0;
	</statement>
	<statement>
		alter table items_h add online integer default 0;
	</statement>

	<statement>
		alter table items add online_ref integer default 0;
	</statement>
	<statement>
		alter table items_h add online_ref integer default 0;
	</statement>
	--v1.119 end_date
	<statement>
		alter table items add e_date2 text ;
	</statement>
	<statement>
		alter table items_h add e_date2 text ;
	</statement>
	<statement>
		alter table customers add online integer default 0;
	</statement>
	<statement>
		alter table customers add online_ref integer default 0;
	</statement>
	<statement>
		alter table currency add online integer default 0;
	</statement>
	<statement>
		alter table currency add online_ref integer default 0;
	</statement>
	--trigger update online=0 when add curr_price/item_price or update customers/items/curr
	<statement>
		drop TRIGGER item_price_update;
	</statement>
	<statement>
		CREATE TRIGGER item_price_update after insert ON item_price
		BEGIN
		insert or ignore into item_price_history(item_id,curr_id,sls_u_price,date_,remarks,param1,param2,unit_id)
		VALUES(NEW.item_id,NEW.curr_id,NEW.sls_u_price,NEW.date_,NEW.remarks,NEW.param1,NEW.param2,NEW.unit_id);

		--update online--0
		--update items set online=0 where id=NEW.item_id;


		END;
	</statement>

	<statement>drop TRIGGER currency_price_insert</statement>
	<statement>
		CREATE TRIGGER currency_price_insert after insert ON currency_price
		--when (NEW.tr_type=2 and NEW.bill_type=2 and NEW.is_back=0)
		BEGIN
		--update the below plan
		update currency_price set
		t_date=date(strftime('%Y-%m-%d',NEW.f_date),'-1 day')
		where curr_id=NEW.curr_id and t_date is null and id !=NEW.id
		and f_date
		--Less than char
		&#60;
		NEW.f_date;
		--update me to the above plan
		update currency_price set
		t_date=date(strftime('%Y-%m-%d',((select min(f_date) from currency_price where curr_id=NEW.curr_id  and id !=NEW.id))),'-1 day')
		where curr_id=NEW.curr_id and t_date is null and id ==NEW.id
		and f_date
		--Less than char
		&#60;
		(select min(f_date) from currency_price where curr_id=NEW.curr_id and id !=NEW.id);
		--update old date ...step1...t_date is null
		update currency_price set
		t_date=((select min(t_date) from currency_price a where a.curr_id=NEW.curr_id  and NEW.f_date between a.f_date and a.t_date))
		where curr_id=NEW.curr_id and t_date is null and id ==NEW.id
		and  NEW.f_date between f_date and  ifnull(t_date,NEW.f_date)


		;
		--update old date ...step2...t_date is not null
		update currency_price set
		t_date=date(strftime('%Y-%m-%d',NEW.f_date),'-1 day')
		where curr_id=NEW.curr_id and t_date is not null and id !=NEW.id
		and  NEW.f_date between f_date and t_date;

		--update online--0
		update currency set online=0 where id=NEW.curr_id;

		END
	</statement>

	<statement>drop TRIGGER  customers_online_update</statement>
	<statement>
		--CREATE TRIGGER customers_online_update after update ON customers
		--when (NEW.online ==2)
		--BEGIN
		--update customers set online=0 where id=NEW.id;
		--END
	</statement>
	<statement>drop TRIGGER if exists items_online_update</statement>

	<statement>drop TRIGGER  unit_item_insert</statement>
	<statement>
		--v1.136 sync
		--CREATE TRIGGER unit_item_insert after insert ON unit_item
		--BEGIN
		--update items set online=0 where id=NEW.item_id;
		--END
	</statement>
	<statement>drop TRIGGER  unit_item_update</statement>
	<statement>
		--v1.136 sync
		--CREATE TRIGGER unit_item_update after update ON unit_item
		--BEGIN
		--update items set online=0 where id=NEW.item_id;
		--END
	</statement>
	<statement>
		CREATE TABLE IF NOT EXISTS requests_curr
		(
		id	INTEGER PRIMARY KEY AUTOINCREMENT,

		req_id integer,


		curr_name text,
		curr_id          integer references currency(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		code_name text,
		status integer default 0,
		unique(req_id,curr_name)

		);
	</statement>
	<statement>
		alter table requests_curr add status integer default 0;
	</statement>
	<statement>
		alter table requests_curr add fils_name text;
	</statement>
	<statement>
		alter table requests_items add remarks text;
	</statement>
	<statement>
		alter table requests_names add online_ref integer;
	</statement>
	<statement>
		alter table requests_curr add online_ref integer;
	</statement>
	<statement>
		alter table requests_items add online_ref integer;
	</statement>
	<statement>
		alter table requests_items add type_id integer;
	</statement>
	<statement>
		alter table requests_items add type_name text;
	</statement>
	<statement>
		CREATE TABLE IF NOT EXISTS requests_curr_price
		(
		req_id                  integer ,
		f_date text default (strftime('%Y-%m-%d','now')),
		t_date text,
		curr_name text,
		curr_id          integer references currency(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		price real default 1
		);
	</statement>

	--trigger prevent online client from update customers/items/currency--need discussion

	--######

	--##### end online requests

	-- ##### TAX features
	<statement>
		create table if not exists tax_type(id INTEGER PRIMARY KEY AUTOINCREMENT,
		name text, unique(name) );
	</statement>
	<statement>insert or ignore into tax_type(id,name) values(-1,'متضمن');</statement>
	<statement>insert or ignore into tax_type(id,name) values(1,'غير متضمن');</statement>

	<statement>
		create table if not exists tax(id INTEGER PRIMARY KEY AUTOINCREMENT,
		name text ,
		tax_type_id integer default 1  references tax_type(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		per real default 0 ,is_active integer default 1,is_default integer default 0
		,unique(name),unique(per) );
	</statement>

	<statement>insert or ignore into tax(id,name,tax_type_id,per,is_default)
		values(-1,'بدون',1,0,1);
	</statement>
	<statement>insert or ignore into tax(id,name,tax_type_id,per,is_default)
		values(0,'ض.قيمة مضافة',1,5,0);
	</statement>

	<statement>alter table bills add tax_id integer default -1  references
		tax(id) ON UPDATE RESTRICT ON DELETE RESTRICT;
	</statement>
	<statement>alter table bills add t_val real default 0;</statement>

	<statement>alter table bills_h add tax_id integer default -1
		references tax(id) ON UPDATE RESTRICT ON DELETE RESTRICT;
	</statement>
	<statement>alter table bills_h add t_val real default 0</statement>

	<statement>alter table bills add time_ text ;</statement>


	<statement>alter table bills_h add time_ text ;</statement>

	<statement>alter table bills2 add time_ text ;</statement>
	<statement>alter table requests_bills add time_ text ;</statement>
	<statement>alter table requests_bills add cost2 real ;</statement>
	<statement>alter table bills2 add t_val real default 0;</statement>
	<statement>alter table bills2 add tax_amount real default 0;</statement>

	--online TAX
	<statement>alter table requests_bills add t_val real default 0;</statement>
	<statement>alter table requests_bills add tax_amount real default 0;</statement>
	<statement>
		alter table requests_bills add online_ref  integer default 0;
	</statement>
	<statement>
		alter table doc_hdr add online_ref  integer default 0;
	</statement>

	<statement>drop TRIGGER if exists tax_default_insert</statement>
	<statement>
		CREATE TRIGGER tax_default_insert after insert ON tax
		when (NEW.is_default ==1)
		BEGIN
		update tax set is_default=0 where id!=NEW.id;
		END
	</statement>

	<statement>drop TRIGGER if exists tax_default_update</statement>
	<statement>
		CREATE TRIGGER tax_default_update after update ON tax
		when (NEW.is_default ==1)
		BEGIN
		update tax set is_default=0 where id!=NEW.id;
		END
	</statement>

	<statement>drop TRIGGER if exists tax_default_update2</statement>
	<statement>
		CREATE TRIGGER tax_default_update2 after update ON tax
		when ((select count(*) from tax where is_default=1)==0)
		BEGIN
		update tax set is_default=1 where id= -1 ;
		END
	</statement>

	--Prevent update used TAX
	<statement>drop TRIGGER if exists  tax_used_update; </statement>
	<statement>
		CREATE TRIGGER tax_used_update
		BEFORE update  ON tax
		FOR EACH ROW
		WHEN ( NEW.per!=OLD.per and (select count(*) from bills where t_val=OLD.per)>0 )
		BEGIN
		SELECT RAISE(ABORT, 'ERROR:Invalid Update TAX') ;
		END;
	</statement>
	<statement>drop TRIGGER if exists  tax_used_delete; </statement>
	<statement>
		CREATE TRIGGER tax_used_delete
		BEFORE delete  ON tax
		FOR EACH ROW
		WHEN ((select count(*) from bills where t_val=OLD.per)>0 )
		BEGIN
		SELECT RAISE(ABORT, 'ERROR:Invalid Delete TAX') ;
		END;
	</statement>


	--##### end TAX

	--####### other cost
	<statement>alter table bills add cost2 real default 0</statement>
	<statement>alter table bills_h add cost2 real default 0</statement>

	<statement>alter table bills add cost_id integer default -24</statement>
	<statement>alter table bills_h add cost_id integer default -24</statement>

	<statement>alter table bills add r_cost real default 0</statement>
	<statement>alter table bills_h add r_cost real default 0</statement>




	<statement>drop TRIGGER  bills_cost2_update</statement>
	<statement>
		CREATE TRIGGER bills_cost2_update after update ON bills
		when (NEW.cost2!=0  and (select is_active from trigger_flags)=1 )
		BEGIN
		update bill_transactions
		set u_cost2=
		((NEW.cost2)*((cost_price+sls_u_price)*u_val*qty_t)/(NEW.amount-NEW.tax_amount-NEW.cost2+NEW.d_amount))
		/(u_val*qty_t)
		where bill_id=NEW.id;
		END;
	</statement>
	--##2023-05
	<statement>drop TRIGGER  bills_cost2_update2</statement>
	<statement>
		CREATE TRIGGER bills_cost2_update2 after update ON bills
		when (NEW.cost2=0 and OLD.cost2!=0  and (select is_active from trigger_flags)=1 )
		BEGIN
		update bill_transactions
		set u_cost2=0
		where bill_id=NEW.id;
		END;
	</statement>


	--###2023-03
	<statement>drop TRIGGER bills_discount_update</statement>
	<statement>
		CREATE TRIGGER bills_discount_update after update ON bills
		when (NEW.d_amount!=0  and (select is_active from trigger_flags)=1 )
		BEGIN
		update bill_transactions
		set d_amount=
		((NEW.d_amount)*((cost_price+sls_u_price)*u_val*qty_t)/(NEW.amount-NEW.tax_amount-NEW.cost2+NEW.d_amount))
		/(qty_t)
		where bill_id=NEW.id;
		END;
	</statement>

	--###2023-09
	<statement>drop TRIGGER bills_discount_update2</statement>
	<statement>
		CREATE TRIGGER bills_discount_update2 after update ON bills
		when (NEW.d_amount=0 and OLD.d_amount!=0  and (select is_active from trigger_flags)=1 )
		BEGIN
		update bill_transactions
		set d_amount=0
		where bill_id=NEW.id;
		END;
	</statement>


	--###2023-5
	<statement>
		update bill_transactions
		set u_cost2=0
		where
		u_cost2!=0 and
		EXISTS(
		select 1 from bills b
		where b.id=bill_transactions.bill_id
		and b.cost2=0
		and (date(strftime('%Y-%m-%d',b.date_ )) !=
		(select ifnull(max(closing_year.date_),date(b.date_,'-1 day'))
		from closing_year))
		);
	</statement>
	--###2023-09
	<statement>
		update bill_transactions
		set d_amount=0
		where
		d_amount!=0 and
		EXISTS(
		select 1 from bills b
		where b.id=bill_transactions.bill_id
		and b.d_amount=0
		and (date(strftime('%Y-%m-%d',b.date_ )) !=
		(select ifnull(max(closing_year.date_),date(b.date_,'-1 day'))
		from closing_year))
		);
	</statement>
	--2023-05 fix bills trans curr_price
	-- update transactions(after update curr_price)
	<statement>drop TRIGGER bill_transactions_curr_price_insert2</statement>
	<statement>
		CREATE TRIGGER bill_transactions_curr_price_insert2 after update ON transactions
		when (NEW.curr_id!=0 and NEW.bill_id>0 and (select curr_mod from bills where id=NEW.bill_id)=1
		and (select is_active from trigger_flags)=1 )
		BEGIN
		update transactions set curr_mod=1
		,curr_price=(select curr_price from bills where id=NEW.bill_id)
		where id=NEW.id and bill_id>0;
		END
	</statement>



	--###### end other cost

	--##### begin cus limit
	<statement>
		create table if not exists cus_limit
		(id INTEGER PRIMARY key AUTOINCREMENT,
		cus_id INTEGER references customers(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		curr_id INTEGER references currency(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		cr REAL,
		db REAL,

		unique(cus_id,curr_id)
		);

	</statement>
	<statement>
		create table if not exists cus_limit_h
		(id INTEGER PRIMARY key AUTOINCREMENT,
		cus_id INTEGER references customers(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		curr_id INTEGER references currency(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		cr REAL,
		db REAL,

		unique(cus_id,curr_id)
		);

	</statement>
	-- Prevent insert transactions upper limit for cus_id
	<statement>drop TRIGGER if exists cus_limit_cr_insert; </statement>
	-- Prevent insert transactions upper limit for t_cus_id##2023-05
	<statement>drop TRIGGER if exists t_cus_limit_cr_insert; </statement>
	--fix bills curr issue then create limit trigger ##2023-05
	<statement>
		update bills
		set curr_mod=curr_mod
		where curr_mod=1 and curr_id!=0
		and (date(strftime('%Y-%m-%d',date_ )) !=
		(select ifnull(max(closing_year.date_),date(bills.date_,'-1 day'))
		from closing_year))
		and not EXISTS
		(select 1 from transactions b
		where b.bill_id=bills.id
		and b.curr_mod=bills.curr_mod and b.curr_id=bills.curr_id and b.curr_price=bills.curr_price

		);
	</statement>
	--##2023-03 fix discount bills issue
	<statement>
		update bills
		set d_amount=d_amount
		where d_amount!=0
		and (date(strftime('%Y-%m-%d',date_ )) !=
		(select ifnull(max(closing_year.date_),date(bills.date_,'-1 day'))
		from closing_year))
		and EXISTS
		(select 1 from bill_transactions b
		where b.bill_id=bills.id
		and b.d_amount!=
		((bills.d_amount)*((cost_price+sls_u_price)*u_val*qty_t)/(bills.amount-bills.tax_amount-bills.cost2+bills.d_amount))
		/(qty_t)
		);
	</statement>
	<statement>
		update bills
		set cost2=cost2
		where cost2!=0
		and (date(strftime('%Y-%m-%d',date_ )) !=
		(select ifnull(max(closing_year.date_),date(bills.date_,'-1 day'))
		from closing_year))
		and EXISTS
		(select 1 from bill_transactions b
		where b.bill_id=bills.id
		and b.u_cost2!=
		((bills.cost2)*((cost_price+sls_u_price)*u_val*qty_t)/(bills.amount-bills.tax_amount-bills.cost2+bills.d_amount))
		/(u_val*qty_t)
		);
	</statement>
	--## 2023-06 fix sales cost2
	<statement>
		update bills
		set amount=amount
		where  tr_type=1 and cost2!=0
		and (date(strftime('%Y-%m-%d',date_ )) !=
		(select ifnull(max(closing_year.date_),date(bills.date_,'-1 day'))
		from closing_year))
		and not EXISTS(select 1 from transactions b where b.bill_id=bills.id
		and b.cus_id=(case when bills.bill_type=2 then bills.cus_id else bills.cash_id end)
		and round(bills.amount)=round(b.out)
		) ;
	</statement>
	--##2024-05 fix bills in old version
	--## 2024-05 fix sales cost2--slow
	<statement>
		update bills
		set amount=amount
		where  tr_type=1 and bill_type=2 and is_back=0 and bills.cost2=0 and bills.tax_amount=0 and bills.r_cost=0
		and (date(strftime('%Y-%m-%d',date_ )) !=
		(select ifnull(max(closing_year.date_),date(bills.date_,'-1 day'))
		from closing_year))
		and not EXISTS(select 1 from transactions b where b.bill_id=bills.id and b.cus_id=bills.cus_id
		) ;
	</statement>
	--## 2024-05 fix purchase cost2
	<statement>
		update bills
		set amount=amount
		where  tr_type=2 and bill_type=2 and is_back=0 and bills.cost2=0 and bills.tax_amount=0 and bills.r_cost=0
		and (date(strftime('%Y-%m-%d',date_ )) !=
		(select ifnull(max(closing_year.date_),date(bills.date_,'-1 day'))
		from closing_year))
		and not EXISTS(select 1 from transactions b where b.bill_id=bills.id and b.t_cus_id=bills.cus_id
		) ;
	</statement>
	--##2024-07 fix sales cash tr--slow running
	<statement>
		UPDATE   bills
		set amount=amount
		where  tr_type=1 and bill_type=1 and is_back=0
		and (date(strftime('%Y-%m-%d',date_ )) !=
		(select ifnull(max(closing_year.date_),date(bills.date_,'-1 day'))
		from closing_year))
		and  not  EXISTS(select 1 from transactions b where b.bill_id=bills.id and b.cus_id=bills.cash_id
		);
	</statement>
	--##2024-07 fix purchase cash tr
	<statement>
		UPDATE   bills
		set amount=amount
		where  tr_type=2 and bill_type=1 and is_back=0
		and (date(strftime('%Y-%m-%d',date_ )) !=
		(select ifnull(max(closing_year.date_),date(bills.date_,'-1 day'))
		from closing_year))
		and  not  EXISTS(select 1 from transactions b where b.bill_id=bills.id and b.t_cus_id=bills.cash_id
		);
	</statement>

	--2024-11 updated to -30 instead of -13
	<statement>
		update bills
		set amount=amount
		where  tr_type in(11,21)
		and (date(strftime('%Y-%m-%d',date_ )) !=
		(select ifnull(max(closing_year.date_),date(bills.date_,'-1 day'))
		from closing_year))
		and EXISTS(select 1 from transactions b where b.bill_id=bills.id and (b.cus_id=-13 or b.t_cus_id=-13)
		) ;
	</statement>

	--##2024-08 temp index to speedup then drop
	<statement>--drop INDEX if exists transactions_bill_id_t;</statement>
	--##2024-08 fix null transactions in old version to fix trail_bal report..pause till issue by client
	<statement>
		--delete from transactions where cus_id is null
		--and not (date(strftime('%Y-%m-%d',substr( date_,7,4)||'-' ||substr( date_,4,2)||'-'||substr( date_,1,2) ))>'2019-01-01') ;
	</statement>
	<statement>
		CREATE TRIGGER cus_limit_cr_insert
		BEFORE insert  ON transactions
		FOR EACH ROW
		WHEN (  NEW.cus_id is not null
		and exists(select * from cus_limit where cus_id=NEW.cus_id and curr_id=NEW.curr_id
		) )
		BEGIN
		SELECT RAISE(ABORT, 'تجاوزت سقف الحساب ')
		where ifnull((select balance*1 from cus_curr where
		id=NEW.cus_id and curr_id=NEW.curr_id),0)
		+( NEW.[in]*NEW.[out]*1)
		not between (select db*-1  from cus_limit
		where cus_id=NEW.cus_id and curr_id=NEW.curr_id
		)
		and (select cr  from cus_limit
		where cus_id=NEW.cus_id and curr_id=NEW.curr_id
		)

		;
		END;
	</statement>

	-- Prevent insert transactions upper limit for t_cus_id
	<statement>drop TRIGGER if exists t_cus_limit_cr_insert; </statement>
	<statement>
		CREATE TRIGGER if not exists t_cus_limit_cr_insert
		BEFORE insert  ON transactions
		FOR EACH ROW
		WHEN ( NEW.t_cus_id is not null
		and exists(select * from cus_limit where cus_id=NEW.t_cus_id and curr_id=NEW.curr_id
		) )
		BEGIN
		SELECT RAISE(ABORT, 'تجاوزت سقف الحساب# ')
		where ifnull((select balance*1 from cus_curr where
		id=NEW.t_cus_id and curr_id=NEW.curr_id),0)
		+( NEW.[out]*-1)

		not between (select db*-1  from cus_limit
		where cus_id=NEW.t_cus_id and curr_id=NEW.curr_id
		)
		and (select cr  from cus_limit
		where cus_id=NEW.t_cus_id and curr_id=NEW.curr_id
		)
		;
		END;
	</statement>


	<statement>drop TRIGGER if exists cus_limit_cr_update; </statement>
	<statement>
		CREATE TRIGGER if not exists cus_limit_cr_update
		BEFORE update  ON transactions
		FOR EACH ROW
		WHEN ( NEW.cus_id is not null
		and exists(select * from cus_limit where cus_id=NEW.cus_id and curr_id=NEW.curr_id
		) )
		BEGIN
		SELECT RAISE(ABORT, 'تجاوزت سقف الحساب ')
		where ifnull((select balance*1 from cus_curr where
		id=NEW.cus_id and curr_id=NEW.curr_id),0)
		-(select ( [in]*[out]*1) from transactions where id=NEW.id )
		+( NEW.[in]*NEW.[out]*1)

		not between (select db*-1  from cus_limit
		where cus_id=NEW.cus_id and curr_id=NEW.curr_id
		)
		and (select cr  from cus_limit
		where cus_id=NEW.cus_id and curr_id=NEW.curr_id
		)
		;
		END;
	</statement>

	-- Prevent insert transactions upper limit for t_cus_id
	<statement>drop TRIGGER if exists t_cus_limit_cr_update; </statement>
	<statement>
		CREATE TRIGGER if not exists t_cus_limit_cr_update
		BEFORE update  ON transactions
		FOR EACH ROW
		WHEN ( NEW.t_cus_id is not null
		and exists(select * from cus_limit where cus_id=NEW.t_cus_id and curr_id=NEW.curr_id
		) )
		BEGIN
		SELECT RAISE(ABORT, 'تجاوزت سقف الحساب# ')
		where ifnull((select balance*1 from cus_curr where
		id=NEW.t_cus_id and curr_id=NEW.curr_id),0)
		-(select ( [out]*-1) from transactions where id=NEW.id )
		+( NEW.[out]*-1)

		not between (select db*-1  from cus_limit
		where cus_id=NEW.t_cus_id and curr_id=NEW.curr_id
		)
		and (select cr  from cus_limit
		where cus_id=NEW.t_cus_id and curr_id=NEW.curr_id
		)
		;
		END;
	</statement>


	--###### end cus limit

	-- ###### reminders
	<statement>
		CREATE TABLE IF NOT EXISTS reminders
		(
		`id`	INTEGER PRIMARY KEY AUTOINCREMENT,
		cus_id integer references customers(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		date_            text default (strftime('%Y-%m-%d','now')),
		time_ text,
		remarks text,
		flag integer default 0,
		param1 text,param2 text

		);
	</statement>
	-- ##### end remiders

	--##fix customers with new line char
	<statement>
		update customers set name=replace(name,char(10),'') where name like '%'||char(10)||'%'
		and not EXISTS(select * from customers b where b.name=replace(customers.name,char(10),'') ) ;
	</statement>
	--##fix customers with new line char2
	<statement>
		update customers set name=replace(name,char(10),'.') where name like '%'||char(10)||'%' ;
	</statement>

	--##2025-09
	--##fix customers with new line char
	<statement>
		update customers set name=replace(name,char(13),'') where name like '%'||char(13)||'%'
		and not EXISTS(select * from customers b where b.name=replace(customers.name,char(13),'') ) ;
	</statement>
	--##fix customers with new line char2
	<statement>
		update customers set name=replace(name,char(13),'.') where name like '%'||char(13)||'%' ;
	</statement>



	--### v1.101 curr sales
	<statement>
		insert or ignore into  `tran_type` (id,name) values(-6,'عملات');
	</statement>
	<statement>
		insert or ignore into  `tran_type` (id,name) values(10,'بيع عملة');
	</statement>
	<statement>
		insert or ignore into  `tran_type` (id,name) values(11,'شراء عملة');
	</statement>
	<statement>
		alter table transactions add c_curr_id integer default 0;
	</statement>
	<statement>
		alter table transactions_h add c_curr_id integer default 0;
	</statement>
	<statement>
		alter table transactions_h2 add c_curr_id integer default 0;
	</statement>
	<statement>
		alter table transactions add c_curr_price real default 0;
	</statement>
	<statement>
		alter table transactions_h add c_curr_price real default 0;
	</statement>
	<statement>
		alter table transactions_h2 add c_curr_price real default 0;
	</statement>
	<statement>
		alter table transactions add c_price real default 0;
	</statement>
	<statement>
		alter table transactions_h add c_price real default 0;
	</statement>
	<statement>
		alter table transactions_h2 add c_price real default 0;
	</statement>
	<statement>
		alter table transactions add c_diff_id integer default -27;
	</statement>
	<statement>
		alter table transactions_h add c_diff_id integer default -27;
	</statement>
	<statement>
		alter table transactions_h2 add c_diff_id integer default -27;
	</statement>
	<statement>
		alter table transactions add c_p_id integer default 0;
	</statement>
	<statement>
		alter table transactions_h add c_p_id integer default 0;
	</statement>
	<statement>
		alter table transactions_h2 add c_p_id integer default 0;
	</statement>
	-- Prevent invalid curr transactions
	<statement>drop TRIGGER if exists prevent_invalid_tr2; </statement>
	<statement>
		CREATE TRIGGER if not exists prevent_invalid_tr2
		BEFORE INSERT  ON transactions
		FOR EACH ROW
		WHEN (NEW.bill_id=-6 and NEW.c_p_id=0 and (NEW.curr_id=0 or NEW.curr_id=NEW.c_curr_id
		or NEW.c_price=0 or NEW.cus_id=NEW.c_diff_id ))
		BEGIN
		SELECT RAISE(ABORT, 'ERROR:Invalid Transaction Curr') ;
		END;
	</statement>
	<statement>
		insert or ignore into customers (id,cus_type_id,name,acc_p_id) values(-27,7,'فوارق بيع وشراء العملات',42);
	</statement>
	<statement>drop TRIGGER if exists transactions_curr_insert</statement>
	<statement>
		CREATE TRIGGER if not exists transactions_curr_insert after insert ON transactions
		when (NEW.bill_id=-6 and NEW.c_p_id=0 )
		BEGIN
		-- reverse curr
		insert or ignore into transactions(cus_id,[in],[out],curr_id,curr_price,curr_mod,date_,bill_id,remarks,param2
		,p_ref_no,c_p_id,c_curr_id,c_curr_price,c_price,c_diff_id,tr_type,p_date,p_id)
		select NEW.cus_id,
		NEW.[in]*-1,NEW.out*NEW.c_price,NEW.c_curr_id,NEW.c_curr_price,1,NEW.date_,NEW.bill_id,
		NEW.out||
		(select name from currency where id=NEW.curr_id)||' '||'بسعر'||NEW.c_price||' '
		||(select name from currency where id=NEW.c_curr_id)||' '||NEW.remarks
		,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.p_ref_no,NEW.p_id,NEW.c_curr_id,NEW.c_curr_price,NEW.c_price,NEW.c_diff_id
		,NEW.tr_type,NEW.p_date,NEW.p_id
		where NEW.tr_type in(10,11) ;

		-- diff_id positive rev
		insert or ignore into transactions(cus_id,[in],[out],curr_id,curr_price,curr_mod,date_,bill_id,remarks,param2
		,p_ref_no,c_p_id,c_curr_id,c_curr_price,c_price,c_diff_id,tr_type,p_date,p_id)
		select NEW.c_diff_id
		,(case when NEW.tr_type=10 then -1 else 1 end),
		abs(NEW.out*(NEW.c_price*(case when NEW.c_curr_price=0 then 1 else NEW.c_curr_price end)-NEW.curr_price))
		,0,0,1,NEW.date_,NEW.bill_id,
		NEW.out||
		(select name from currency where id=NEW.curr_id)||' '||'بسعر'||NEW.c_price||' '
		||(select name from currency where id=NEW.c_curr_id)||' '||NEW.remarks
		,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.p_ref_no,NEW.p_id,NEW.c_curr_id,NEW.c_curr_price,NEW.c_price,NEW.c_diff_id
		,NEW.tr_type,NEW.p_date,NEW.p_id
		where NEW.out*(NEW.c_price*(case when NEW.c_curr_price=0 then 1 else NEW.c_curr_price end)
		-NEW.curr_price)>0 ;

		-- diff_id negative rev
		insert or ignore into transactions(cus_id,[in],[out],curr_id,curr_price,curr_mod,date_,bill_id,remarks,param2
		,p_ref_no,c_p_id,c_curr_id,c_curr_price,c_price,c_diff_id,tr_type,p_date,p_id)
		select NEW.c_diff_id
		,(case when NEW.tr_type=10 then 1 else -1 end),
		abs(NEW.out*(NEW.c_price*(case when NEW.c_curr_price=0 then 1 else NEW.c_curr_price end)-NEW.curr_price))
		,0,0,1,NEW.date_,NEW.bill_id,
		NEW.out||
		(select name from currency where id=NEW.curr_id)||' '||'بسعر'||NEW.c_price||' '
		||(select name from currency where id=NEW.c_curr_id)||' '||NEW.remarks
		,
		strftime('%H:%M', datetime('now','localtime'))
		,NEW.p_ref_no,NEW.p_id,NEW.c_curr_id,NEW.c_curr_price,NEW.c_price,NEW.c_diff_id
		,NEW.tr_type,NEW.p_date,NEW.p_id
		where not(NEW.out*(NEW.c_price*(case when NEW.c_curr_price=0 then 1 else NEW.c_curr_price end)
		-NEW.curr_price)>0) ;
		END
	</statement>
	<statement>
		alter table tr_p_temps add c_price real default 0;
	</statement>
	<statement>
		alter table tr_p_temps add c_diff_id integer default -27;
	</statement>
	<statement>drop view if exists transactions_v;</statement>
	<statement>create view transactions_v as select * from transactions;</statement>
	--## end v1.101

	--## begin v1.135 Users
	--only one index (if more than one index you should check upgrade_db in fun.java to be more than one for recreate)
	<statement>
		CREATE TABLE if not exists users (
		id	INTEGER PRIMARY KEY AUTOINCREMENT,
		user_name	TEXT UNIQUE,
		name	TEXT ,
		pwd	TEXT,
		gsm	TEXT,
		is_active INTEGER default 1,
		cash_id integer  references customers(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		br_id integer  references branches(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		date_ text default (strftime('%Y-%m-%d','now')),
		`param1`	TEXT,
		`param2`	TEXT,
		`f1`	TEXT,
		`f2`	TEXT,
		`f3`	TEXT
		)
	</statement>
	--to remove old UQs
	<statement>
		--insert or ignore into users select * from users_old;
	</statement>
	<statement>
		CREATE TABLE if not exists user_priv (
		id	INTEGER PRIMARY KEY AUTOINCREMENT,
		screen_id integer  references screens(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		user_id integer  references users(id) ON UPDATE RESTRICT ON DELETE RESTRICT,
		new INTEGER default 0,
		edit INTEGER default 0,
		view INTEGER default 0,
		del INTEGER default 0,
		date_ text default (strftime('%Y-%m-%d','now')),
		`param1`	TEXT,
		`param2`	TEXT,
		unique(user_id,screen_id)
		)
	</statement>
	<statement>drop TRIGGER if exists user_priv_insert</statement>
	<statement>
		CREATE TRIGGER user_priv_insert after insert ON users
		WHEN ( (select count(*) from user_priv where user_id=NEW.id  )=0 )
		BEGIN
		insert or ignore into user_priv (screen_id,user_id,new,edit,view,del)
		select id,NEW.id,new,edit,view,del
		from screens where p_id!=0;
		update user_priv set new=0 where new=1 and user_id=NEW.id and NEW.id!=0 and screen_id not   in(-6,-7,9,-8,6,7,-2,-3,16,29,30);
		update user_priv set edit=0 where edit=1 and user_id=NEW.id and NEW.id!=0 and screen_id not in(-6,-7,9,-8,6,7,-2,-3,16,29,30);
		update user_priv set view=0 where view=1 and user_id=NEW.id and NEW.id!=0 and screen_id not in(-6,-7,9,-8,6,7,-2,-3,16,29,30);
		update user_priv set del=0 where del=1 and user_id=NEW.id and NEW.id!=0 ;
		END;
	</statement>
	<statement>insert or ignore into users(id,user_name,name,is_active,cash_id,br_id) values(0,'مدير النظام','مدير النظام',0,-3,0)</statement>
	<statement>alter table bills add user_id integer references users(id) ON UPDATE RESTRICT ON DELETE RESTRICT;</statement>
	<statement>alter table bills_h add user_id integer references users(id) ON UPDATE RESTRICT ON DELETE RESTRICT;</statement>
	<statement>alter table bills add last_user integer references users(id) ON UPDATE RESTRICT ON DELETE RESTRICT;</statement>
	<statement>alter table bills_h add last_user integer references users(id) ON UPDATE RESTRICT ON DELETE RESTRICT;</statement>
	<statement>alter table bills add last_update text;</statement>
	<statement>alter table bills_h add last_update text;</statement>

	<statement>alter table transactions add user_id integer references users(id) ON UPDATE RESTRICT ON DELETE RESTRICT;</statement>
	<statement>alter table transactions_h add user_id integer references users(id) ON UPDATE RESTRICT ON DELETE RESTRICT;</statement>
	<statement>alter table transactions_h2 add user_id integer references users(id) ON UPDATE RESTRICT ON DELETE RESTRICT;</statement>
	<statement>alter table transactions add last_user integer references users(id) ON UPDATE RESTRICT ON DELETE RESTRICT;</statement>
	<statement>alter table transactions_h add last_user integer references users(id) ON UPDATE RESTRICT ON DELETE RESTRICT;</statement>
	<statement>alter table transactions_h2 add last_user integer references users(id) ON UPDATE RESTRICT ON DELETE RESTRICT;</statement>
	<statement>alter table transactions add last_update text;</statement>
	<statement>alter table transactions_h add last_update text;</statement>
	<statement>alter table transactions_h2 add last_update text;</statement>

	<statement>alter table bills2 add user_id integer references users(id) ON UPDATE RESTRICT ON DELETE RESTRICT;</statement>
	<statement>alter table bills2 add last_user integer references users(id) ON UPDATE RESTRICT ON DELETE RESTRICT;</statement>
	<statement>alter table bills2 add last_update text;</statement>

	<statement>drop TRIGGER if exists user_priv_update_edit</statement>
	<statement>
		CREATE TRIGGER user_priv_update_edit after update ON user_priv
		WHEN ( ( (NEW.edit=1 and OLD.edit=0) or (NEW.new=1 and OLD.new=0) or (NEW.del=1 and OLD.del=0)
		) and NEW.view=0 and OLD.view=0 )
		BEGIN
		update user_priv set view=1 where id=NEW.id;
		END;
	</statement>
	<statement>drop TRIGGER if exists user_priv_update_view</statement>
	<statement>
		CREATE TRIGGER user_priv_update_view after update ON user_priv
		WHEN (((NEW.edit=1 or NEW.new=1 or NEW.del=1) ) and NEW.view=0 and OLD.view=1 )
		BEGIN
		update user_priv set edit=(case when edit!=-1 then 0 else edit end)
		,new=(case when new!=-1 then 0 else new end)
		,del=(case when del!=-1 then 0 else del end)
		where id=NEW.id;
		END;
	</statement>
	-- Prevent update -1 of edit or new or view or Admin Priv
	<statement>drop TRIGGER prevent_invalid_priv; </statement>
	<statement>
		CREATE TRIGGER prevent_invalid_priv
		BEFORE update  ON user_priv
		FOR EACH ROW
		WHEN (( OLD.new=-1 and NEW.new!=-1 ) or ( OLD.edit=-1 and NEW.edit!=-1 )
		or ( OLD.del=-1 and NEW.del!=-1 )
		or ( OLD.view=-1 and NEW.view!=-1 )
		or NEW.user_id=0
		)
		BEGIN
		SELECT RAISE(ABORT, 'Invalid Update Priv') ;
		END;
	</statement>

	-- Prevent stop Admin if users activated found
	<statement>drop TRIGGER prevent_stop_admin; </statement>
	<statement>
		CREATE TRIGGER prevent_stop_admin
		BEFORE update  ON users
		FOR EACH ROW
		WHEN (NEW.id=0 and NEW.is_active=0 and(select count(*) from users where is_active=1 and id!=0)>0
		)
		BEGIN
		SELECT RAISE(ABORT, 'Invalid Stop Admin') ;
		END;
	</statement>
	-- Prevent add user if Admin is not active
    --  and NEW.is_active=1 --2026-05
	<statement>drop TRIGGER prevent_add_user; </statement>
	<statement>
		CREATE TRIGGER prevent_add_user
		BEFORE insert  ON users
		FOR EACH ROW
		WHEN ((select count(*) from users where is_active=1 and id=0)=0
        and NEW.is_active=1
		)
		BEGIN
		SELECT RAISE(ABORT, 'Invalid Add User') ;
		END;
	</statement>
	-- Prevent change br or cash of Admin
	<statement>drop TRIGGER prevent_update_admin_br_cash; </statement>
	<statement>
		CREATE TRIGGER prevent_update_admin_br_cash
		BEFORE update  ON users
		FOR EACH ROW
		WHEN (NEW.id=0 and (NEW.br_id!=0 or NEW.cash_id!=-3)
		)
		BEGIN
		SELECT RAISE(ABORT, 'Invalid Update Admin') ;
		END;
	</statement>
	-- Prevent change br or cash of user
	<statement>drop TRIGGER prevent_update_user_br_cash; </statement>
	<statement>
		CREATE TRIGGER prevent_update_user_br_cash
		BEFORE update  ON users
		FOR EACH ROW
		WHEN (
		--(NEW.br_id!=OLD.br_id or NEW.cash_id!=OLD.cash_id) or
		(NEW.id!=0 and NEW.is_active=1 and (select count(*) from users where is_active=1 and id=0)=0)
		)
		BEGIN
		SELECT RAISE(ABORT, 'Invalid Update User') ;
		END;
	</statement>

	-- update user_id in transactions table
	<statement>drop TRIGGER bills_user_id_insert</statement>
	<statement>
		CREATE TRIGGER bills_user_id_insert after insert ON bills
		when (NEW.user_id!=null  )
		BEGIN
		update transactions set
		user_id=NEW.user_id
		where bill_id=NEW.id;
		END
	</statement>

	--## end v1.135

	--##2023-06 delete old prices history of closed years
	<statement>
		delete from item_price_history
		where (select max(b.date_) from closing_year b )>=date_;
	</statement>
	--##fix transactions_tmp structure android 14
	<statement>
		drop table if exists transactions_tmp;
	</statement>
	--##BEGIN v1.136 sync
	<statement>alter table groups add online integer default 0;</statement>
	<statement>alter table groups add online_ref2 Text;</statement>
	<statement>alter table account_tree add online integer default 1;</statement>
	<statement>alter table account_tree add online_ref2 Text;</statement>
	<statement>alter table branches add online integer default 1;</statement>
	<statement>alter table branches add online_ref2 Text;</statement>
	<statement>alter table currency_price add online integer default 1;</statement>
	<statement>alter table currency_price add online_ref2 Text;</statement>
	<statement>alter table item_type add online integer default 1;</statement>
	<statement>alter table item_type add online_ref2 Text;</statement>
	<statement>alter table units add online integer default 1;</statement>
	<statement>alter table units add online_ref2 Text;</statement>
	<statement>alter table unit_item add online integer default 1;</statement>
	<statement>alter table unit_item add online_ref2 Text;</statement>
	<statement>alter table tax add online integer default 1;</statement>
	<statement>alter table tax add online_ref2 Text;</statement>
	<statement>alter table item_price add online integer default 1;</statement>
	<statement>alter table item_price add online_ref2 Text;</statement>
	<statement>alter table users add online integer default 1;</statement>
	<statement>alter table users add online_ref2 Text;</statement>
	<statement>alter table user_priv add online integer default 1;</statement>
	<statement>alter table user_priv add online_ref2 Text;</statement>
	<statement>alter table closing_year add online integer default 1;</statement>
	<statement>alter table closing_year add online_ref2 Text;</statement>
	<statement>alter table cus_type add online integer default 1;</statement>
	<statement>alter table cus_type add online_ref2 Text;</statement>

	<statement>alter table bills add online_ref2 Text;</statement>
	<statement>alter table bills_h add online_ref2 Text;</statement>
	<statement>alter table bills2 add online_ref2 Text;</statement>
	--2025-01
	<statement>alter table bills2_h add online_ref2 Text;</statement>

	<statement>alter table transactions add online_ref2 Text;</statement>
	<statement>alter table transactions_h add online_ref2 Text;</statement>
	<statement>alter table transactions_h2 add online_ref2 Text;</statement>
	<statement>alter table items add online_ref2 Text;</statement>
	<statement>alter table items_h add online_ref2 Text;</statement>
	<statement>alter table customers add online_ref2 Text;</statement>
	<statement>alter table currency add online_ref2 Text;</statement>

	<statement>alter table cus_limit add online integer default 0;</statement>
	<statement>alter table cus_limit add online_ref2 Text;</statement>

	<statement>alter table cus_limit_h add online integer default 0;</statement>
	<statement>alter table cus_limit_h add online_ref2 Text;</statement>

	<statement>alter table reminders add online integer default 0;</statement>
	<statement>alter table reminders add online_ref2 Text;</statement>





	<statement>CREATE UNIQUE INDEX if not exists bills_ref2_uq ON bills(online_ref2);</statement>
	<statement>CREATE UNIQUE INDEX if not exists bills_h_ref2_uq ON bills_h(online_ref2,date_);</statement>
	<statement>CREATE UNIQUE INDEX if not exists bills2_ref2_uq ON bills2(online_ref2);</statement>
	<statement>CREATE UNIQUE INDEX if not exists bills2_h_ref2_uq ON bills2_h(online_ref2);</statement>
	<statement>CREATE UNIQUE INDEX if not exists items_ref2_uq ON items(online_ref2);</statement>
	<statement>drop index if exists items_h_ref2_uq;</statement>
	<statement>--CREATE UNIQUE INDEX if not exists items_h_ref2_uq ON items_h(online_ref2,name);</statement>
	<statement>CREATE UNIQUE INDEX if not exists items_h_ref2_uq2 ON items_h(online_ref2,name,date_);</statement>
	<statement>CREATE UNIQUE INDEX if not exists currency_ref2_uq ON currency(online_ref2);</statement>
	<statement>CREATE UNIQUE INDEX if not exists currency_price_ref2_uq ON currency_price(online_ref2);</statement>
	<statement>CREATE UNIQUE INDEX if not exists customers_ref2_uq ON customers(online_ref2);</statement>
	<statement>CREATE UNIQUE INDEX if not exists cus_type_ref2_uq ON cus_type(online_ref2);</statement>
	<statement>CREATE UNIQUE INDEX if not exists groups_ref2_uq ON groups(online_ref2);</statement>
	<statement>CREATE UNIQUE INDEX if not exists closing_year_ref2_uq ON closing_year(online_ref2);</statement>
	<statement>CREATE UNIQUE INDEX if not exists users_ref2_uq ON users(online_ref2);</statement>
	<statement>CREATE UNIQUE INDEX if not exists user_priv_ref2_uq ON user_priv(online_ref2);</statement>
	<statement>CREATE UNIQUE INDEX if not exists item_price_ref2_uq ON item_price(online_ref2);</statement>
	<statement>CREATE UNIQUE INDEX if not exists tax_ref2_uq ON tax(online_ref2);</statement>
	<statement>CREATE UNIQUE INDEX if not exists units_ref2_uq ON units(online_ref2);</statement>
	<statement>CREATE UNIQUE INDEX if not exists unit_item_ref2_uq ON unit_item(online_ref2);</statement>
	<statement>CREATE UNIQUE INDEX if not exists item_type_ref2_uq ON item_type(online_ref2);</statement>
	<statement>CREATE UNIQUE INDEX if not exists branches_ref2_uq ON branches(online_ref2);</statement>
	<statement>CREATE UNIQUE INDEX if not exists account_tree_ref2_uq ON account_tree(online_ref2);</statement>

	<statement>CREATE UNIQUE INDEX if not exists cus_limit_ref2_uq ON cus_limit(online_ref2);</statement>
	<statement>CREATE UNIQUE INDEX if not exists reminders_ref2_uq ON reminders(online_ref2);</statement>

	<statement>CREATE UNIQUE INDEX if not exists transactions_ref2_uq ON transactions(online_ref2);</statement>
	<statement>CREATE UNIQUE INDEX if not exists transactions_h_ref2_uq ON transactions_h(online_ref2,date_);</statement>

--2026-01 remove this trigger as it is updated above
	<statement>--drop TRIGGER if exists prevent_invalid_bill_trans; </statement>
	<statement>
		--CREATE TRIGGER prevent_invalid_bill_trans
		--BEFORE INSERT  ON bill_transactions
		--FOR EACH ROW
		--WHEN (NEW.bill_id is null  )
		--BEGIN
		--SELECT RAISE(ABORT, 'ERROR:Invalid Bill_Tran') ;
		--SELECT RAISE (IGNORE); -- Ignore the insert for this row
		--END;
	</statement>

	<statement>drop TRIGGER if exists prevent_invalid_bill_trans2; </statement>
	<statement>
		CREATE TRIGGER prevent_invalid_bill_trans2
		BEFORE INSERT  ON bill_transactions
		FOR EACH ROW
		WHEN (NEW.item_id is null  )
		BEGIN
		SELECT RAISE(ABORT, 'ERROR:Invalid Bill_Tran2') ;

		END;
	</statement>
	--2024-09 fix offer issue
	<statement>delete from bill_transactions2 where item_id is null; </statement>
	<statement>delete from bill_transactions2 where bill_id is null; </statement>
	<statement>drop TRIGGER if exists prevent_invalid_bill_trans2_; </statement>
	<statement>
		CREATE TRIGGER prevent_invalid_bill_trans2_
		BEFORE INSERT  ON bill_transactions2
		FOR EACH ROW
		WHEN (NEW.item_id is null or NEW.bill_id is null )
		BEGIN
		SELECT RAISE(ABORT, 'ERROR:Invalid Bill_Tran2_') ;

		END;
	</statement>
	--##end --2024-09 fix offer issue
    --2025-11 fix null item_id for sync
    <statement>delete from unit_item where item_id is null; </statement>

	--#publish#
	<statement>
		--drop INDEX if EXISTS uq_bills_no2 ;
	</statement>
	--v1.136 sync uq_bills_no2 is replaced by uq_bills_no3 which include online_ref2
	--#publish#
	<statement>--CREATE UNIQUE  INDEX  if not EXISTS uq_bills_no3 ON bills(tr_type,is_back,br_id,bill_no2,adj_id,online_ref2);</statement>

	--##END v1.136 sync
	--##2024-08 fix crash admin user without cash_id or br_id
	<statement>update users set cash_id=-3 where id=0 and cash_id is null and is_active=1;</statement>
	<statement>update users set br_id=0 where id=0 and br_id is null and is_active=1;</statement>

	--2024-11 auto sms/wa per customer
	<statement>alter table customers add sms integer default 0;</statement>
	<statement>alter table customers add wa integer default 0;</statement>
	--20241-12 check bills total
	<statement>drop view if exists bills_tot_issue</statement>
	<statement>
		create view if not exists bills_tot_issue as
		select a.id id,a.bill_no2 bill_no2,a.date_ date_,a.tr_type tr_type,d.name type_,a.bill_type bill_type,b.name bill_type_name,c.name cus_name
		,round(amount,3) amount,
		(select DISTINCT round(((sum((case when substr(a.tr_type,1,1)*1=1 then sls_u_price else  cost_price end)*qty_pr*u_val))-(case when a.discount_id=0 then a.d_amount when a.d_amount=0 then 0
		else sum((case when substr(a.tr_type,1,1)*1=1 then sls_u_price else  cost_price end)*qty_pr*u_val)*(a.d_val/100) end))*(1+a.t_val/100)+a.cost2,3) from bill_transactions b
		where b.bill_id=a.id) amount2
		,round(amount,3) -
		(select round(((sum((case when substr(a.tr_type,1,1)*1=1 then sls_u_price else  cost_price end)*qty_pr*u_val))-(case when a.discount_id=0 then a.d_amount when a.d_amount=0 then 0
		else sum((case when substr(a.tr_type,1,1)*1=1 then sls_u_price else  cost_price end)*qty_pr*u_val)*(a.d_val/100) end))*(1+a.t_val/100)+a.cost2,3) from bill_transactions b
		where b.bill_id=a.id)  diff,a.d_amount d_amount,a.tax_amount tax_amount,a.d_val d_val,a.discount_id discount_id,a.t_val t_val,a.tax_id tax_id
		,(select DISTINCT round(((sum((case when substr(a.tr_type,1,1)*1=1 then sls_u_price else  cost_price end)*qty_pr*u_val))-(case when a.discount_id=0 then a.d_amount when a.d_amount=0 then 0
		else sum((case when substr(a.tr_type,1,1)*1=1 then sls_u_price else  cost_price end)*qty_pr*u_val)*(a.d_val/100) end))*(0+a.t_val/100),3) from bill_transactions b
		where b.bill_id=a.id) tax_amount2
		,(select DISTINCT round(((case when a.discount_id=0 then a.d_amount when a.d_amount=0 then 0 else sum((case when substr(a.tr_type,1,1)*1=1 then sls_u_price else  cost_price end)*qty_pr*u_val)*(a.d_val/100) end)),3)
		from bill_transactions b
		where b.bill_id=a.id) d_amount2,a.cost2 cost2,a.r_cost r_cost
		from bills a,bill_type b,customers c,tran_type d
		where
		a.bill_type=b.id and a.cus_id=c.id and a.tr_type=d.id and
		abs(round(amount,3))-(select DISTINCT round(((sum((case when substr(a.tr_type,1,1)*1=1 then sls_u_price else  cost_price end)*qty_pr*u_val))-(case when a.discount_id=0 then a.d_amount when a.d_amount=0 then 0
		else sum((case when substr(a.tr_type,1,1)*1=1 then sls_u_price else  cost_price end)*qty_pr*u_val)*(a.d_val/100) end))*(1+a.t_val/100)+a.cost2,3) from bill_transactions b
		where b.bill_id=a.id) not BETWEEN -1 and 1
		and tr_type in(1,2,11,21);
	</statement>
	--###2024-12 fix bills_tot
	###begin
	<statement>create table if not exists bills_log as select * from bills_tot_issue;</statement>
	--if there is any changes in bills_tot_issue view
	<statement>--alter table bills_log add t_val real default 0;</statement>
	--save date before fix bills
	<statement>
		INSERT into bills_log select * from bills_tot_issue
		where (id,bill_no2,date_,tr_type,cus_name,amount,amount2,d_amount,d_amount2,tax_amount,tax_amount2,d_val,discount_id,t_val,tax_id,cost2,r_cost)
		not in
		(select b.id,b.bill_no2,b.date_,b.tr_type,b.cus_name,b.amount,b.amount2,b.d_amount,b.d_amount2,b.tax_amount,b.tax_amount2,b.d_val,b.discount_id,b.t_val,b.tax_id,b.cost2,b.r_cost
		from bills_log b);
	</statement>
	--fix bills total which has no_Tax and no_Discount--phase1
	<statement>
		update bills
		set amount=(select a.amount2 from bills_tot_issue a where bills.id=a.id and a.date_=bills.date_
		and a.tr_type in(1,2,11,21) and a.d_amount=0 and a.tax_amount=0 and a.tax_amount2=0 )
		where  tr_type in(1,2,11,21) and bills.d_amount=0 and bills.tax_amount=0
		and (date(strftime('%Y-%m-%d',date_ )) !=
		(select ifnull(max(closing_year.date_),date(bills.date_,'-1 day'))
		from closing_year))
		and EXISTS(select 1 from bills_tot_issue a where bills.id=a.id and a.date_=bills.date_
		and a.tr_type in(1,2,11,21) and a.d_amount=0 and a.tax_amount=0 and a.tax_amount2=0 );
	</statement>
	--### end 2024-12 fix bills_tot

	--###Begin 2025-01 fix deleted doc transactions OR null cus_id
	<statement>create table if not exists transactions_issue as
		select * from transactions where p_id in(select b.p_id from transactions b
		where b.bill_id=-3 and b.tr_type in(5,6) group by b.p_id HAVING count(*)=1);
	</statement>
	<statement>insert into transactions_issue
		select * from transactions where p_id in(select b.p_id from transactions b
		where b.bill_id=-3 and b.tr_type in(5,6) group by b.p_id HAVING count(*)=1)
		and id not in(select c.id from transactions_issue c) ;
	</statement>
	<statement>insert into transactions_issue
		select * from transactions where cus_id is null
		and id not in(select c.id from transactions_issue c) ;
	</statement>
	--No need to delete
	<statement>--delete from transactions where cus_id is null; </statement>
	<statement>
		delete from transactions where p_id in(select b.p_id from transactions b
		where b.bill_id=-3 and b.tr_type in(5,6) group by b.p_id HAVING count(*)=1);
	</statement>

	--2025-01 fix change issue###Begin
	<statement>insert into transactions_issue
		select * from transactions where EXISTS(select * from bills b where b.tr_type=4 and b.adj_id in(1,2,3) and b.id=transactions.bill_id )
		and id not in(select c.id from transactions_issue c) ;
	</statement>
	--only in Mohaseb Apps as No Trans with tr_type=4 and adj_id in(1,2,3)
	<statement>
		delete from transactions
		where EXISTS(select * from bills b where b.tr_type=4 and b.adj_id in(1,2,3)
		and b.id=transactions.bill_id )
	</statement>
	--2025-01 fix change issue###End


	--###End 2025-01 fix deleted doc transactions

	--###Begin 2025-01 prevent delete customers fk---
	<statement>
		CREATE TRIGGER if not EXISTS invalid_delete_customers
		BEFORE delete ON customers
		FOR EACH ROW
		WHEN (
		exists (select 1 from transactions where cus_id=OLD.id)
		or exists(select 1 from transactions where t_cus_id is not null and t_cus_id=OLD.id)
		or exists(select 1 from bills where cus_id is not null and cus_id=OLD.id)
		or exists(select 1 from bills2 where cus_id is not null and cus_id=OLD.id)
		)
		BEGIN
		SELECT RAISE(ABORT, 'Invalid Delete Customer') ;
		END;
	</statement>
	<statement>
		CREATE TRIGGER if not EXISTS invalid_delete_customers_h
		BEFORE delete ON customers
		FOR EACH ROW
		WHEN (
		exists (select 1 from transactions_h where cus_id=OLD.id)
		or exists(select 1 from transactions_h where t_cus_id is not null and t_cus_id=OLD.id)
		or exists(select 1 from bills_h where cus_id is not null and cus_id=OLD.id)
		or exists(select 1 from bills2_h where cus_id is not null and cus_id=OLD.id)
		)
		BEGIN
		SELECT RAISE(ABORT, 'Invalid Delete Customer h') ;
		END;
	</statement>
	--reInsert deleted customers
	<statement>
		INSERT into customers(id,name,acc_p_id)
		select _id,_id||'-d',221 from
		(
		select DISTINCT cus_id as _id from transactions where not exists(select 1 from customers b where b.id=cus_id) and cus_id is not null
		union

		select DISTINCT t_cus_id as _id from transactions where not exists(select 1 from customers b where b.id=t_cus_id) and t_cus_id is not null
		);
	</statement>
	--reInsert deleted customers h
	<statement>
		INSERT into customers(id,name,acc_p_id)
		select _id,_id||'-d',221 from
		(
		select DISTINCT cus_id as _id from transactions_h where not exists(select 1 from customers b where b.id=cus_id) and cus_id is not null
		union

		select DISTINCT t_cus_id as _id from transactions_h where not exists(select 1 from customers b where b.id=t_cus_id) and t_cus_id is not null
		union
		select DISTINCT cus_id as _id from transactions_h2 where not exists(select 1 from customers b where b.id=cus_id) and cus_id is not null
		union

		select DISTINCT t_cus_id as _id from transactions_h2 where not exists(select 1 from customers b where b.id=t_cus_id) and t_cus_id is not null
		);
	</statement>
	--###End 2025-01 prevent delete customers fk---

    --### coming from daftar db issue ##2026-04
    <statement>
        drop TRIGGER if EXISTS prevent_invalid_tr_d;
    </statement>


    --2026-04 qr phase2  #### Begin
    <statement>alter table bills add column qr text;</statement>
    <statement>alter table bills_h add column qr text;</statement>

    <statement>--alter table companies add column cr_no text;</statement>
    <statement>alter table customers add column cr_no text;</statement>
    <statement>--alter table companies add column org_name text;</statement>
    <statement>--alter table companies add column country_code text default 'SA';</statement>
    <statement>
        --  alter table companies add column industry_category text
        --  check (
        --  industry_category is null or
        -- (
        -- instr(industry_category, ' ') = 0 and
        -- instr(industry_category, char(9)) = 0 and
        -- instr(industry_category, char(10)) = 0 and
        -- instr(industry_category, char(13)) = 0
        -- )
        --);
    </statement>

    --backend
    <statement>--alter table companies add column taxer_id integer;</statement>
    <statement>--create unique index if not exists uq_companies_taxer_id on companies(taxer_id);</statement>

    --branches
    <statement>alter table branches add column cr_no text;</statement>
    <statement>alter table branches add column common_name text;</statement>
    <statement>alter table branches add column z_address text;</statement>
    <statement>alter table branches add column z_name text;</statement>



    --Zatca units
    <statement>
        create table if not exists z_units (
        id integer primary key autoincrement,
        br_id integer references branches(id),
        env_type integer default 1,
        icv integer,
        pih text default 'NWZlY2ViNjZmZmM4NmYzOGQ5NTI3ODZjNmQ2OTZjNzljMmRiYzIzOWRkNGU5MWI0NjcyOWQ3M2EyN2ZiNTdlOQ==',
        serial_no text unique,
        taxer_id integer,
        egs_id integer,
        cert_id integer unique,
        is_active integer default 0,
        otp text,
        pk text,
        token text,
        date_ text default (strftime('%Y-%m-%d','now'))
        );
    </statement>



    <statement>alter table z_units add column unit_name text;</statement>
    <statement>alter table z_units add column auto_ integer default 1;</statement>
    <statement>alter table z_units add column vat_no text;</statement>
    <statement>alter table z_units add column cr_no text;</statement>
    <statement>alter table z_units add column z_name text;</statement>
    <statement>alter table z_units add column user_id integer references users(id)</statement>
    <statement>alter table z_units add column device_id text default '';</statement>
    <statement>alter table z_units add column z_address text;</statement>
    <statement>drop index if exists uq_z_units_active;</statement>

    <statement>
       -- create unique index if not exists uq_z_units_active2
       -- on z_units (br_id, env_type,device_id)
       -- where is_active = 1;
    </statement>

    --Zatca status
    <statement>
        create table if not exists z_status (
        id integer primary key,
        name text unique
        );
    </statement>

    <statement>insert or ignore into z_status (id,name) values(0,'Not_Submitted');</statement>
    <statement>insert or ignore into z_status (id,name) values(1,'REPORTED');</statement>
    <statement>insert or ignore into z_status (id,name) values(2,'CLEARED');</statement>
    <statement>insert or ignore into z_status (id,name) values(3,'REPORTED+Warning');</statement>
    <statement>insert or ignore into z_status (id,name) values(4,'CLEARED+Warning');</statement>
    <statement>insert or ignore into z_status (id,name) values(-1,'NOT_REPORTED');</statement>
    <statement>insert or ignore into z_status (id,name) values(-2,'NOT_CLEARED');</statement>
    <statement>insert or ignore into z_status (id,name) values(-3,'Failed');</statement>
    <statement>insert or ignore into z_status (id,name) values(-4,'Error');</statement>
    <statement>update z_status set name='REPORTED+' where id=3;</statement>
    <statement>update z_status set name='CLEARED+' where id=4;</statement>
    <statement>alter table z_status add column desc_ text;</statement>
    CREATE UNIQUE INDEX if not exists z_status_desc ON z_status(desc_);
    <statement>update z_status set desc_='لم يتم الإرسال' where id=0; </statement>
    <statement>update z_status set desc_='تم قبول المستند' where id=1; </statement>
    <statement>update z_status set desc_='تم إعتماد المستند' where id=2; </statement>
    <statement>update z_status set desc_='تم قبول المستند + تحذيرات' where id=3;</statement>
    <statement>update z_status set desc_='تم إعتماد المستند + تحذيرات' where id=4; </statement>
    <statement>update z_status set desc_='لم يتم قبول المستند' where id=-1;</statement>
    <statement>update z_status set desc_='لم يتم اعتماد المستند' where id=-2;</statement>
    <statement>update z_status set desc_='فشلت العملية' where id=-3;</statement>
    <statement>update z_status set desc_='خطأ' where id=-4;</statement>


    --Bills/Bills_h
    <statement>alter table bills add column z_status integer references z_status(id);</statement>
    <statement>alter table bills_h add column z_status integer references z_status(id);</statement>

    <statement>alter table bills add column z_unit_id integer references z_units(id);</statement>
    <statement>alter table bills_h add column z_unit_id integer references z_units(id);</statement>

    <statement>alter table bills add column z_user_id integer references users(id);</statement>
    <statement>alter table bills_h add column z_user_id integer references users(id);</statement>

    <statement>alter table bills add column z_date text;</statement>
    <statement>alter table bills_h add column z_date text;</statement>

    <statement>alter table bills add column z_code text;</statement>
    <statement>alter table bills add column z_reply text;</statement>
    <statement>alter table bills add column z_uuid text;</statement>
    <statement>alter table bills add column z_pih text;</statement>
    <statement>alter table bills add column z_inv_hash text;</statement>

    <statement>alter table bills_h add column z_code text;</statement>
    <statement>alter table bills_h add column z_reply text;</statement>
    <statement>alter table bills_h add column z_uuid text;</statement>
    <statement>alter table bills_h add column z_pih text;</statement>
    <statement>alter table bills_h add column z_inv_hash text;</statement>

    <statement>alter table bills add column z_cleared_inv text;</statement>
    <statement>alter table bills_h add column z_cleared_inv text;</statement>

    <statement>alter table bills add column xml_file text;</statement>
    <statement>alter table bills_h add column xml_file text;</statement>

    <statement>alter table currency_price add column price_sar numeric;</statement>
    <statement>alter table bills add column curr_price_sar numeric default 0;</statement>
    <statement>alter table bills_h add column curr_price_sar numeric default 0;</statement>

    --2026-04 qr phase2  #### End



    --########Upgrade DB ver
	<statement>
		-- to stop upgrade db...very important..impact auto backup if still upgrade with each upgrade
		create table if not exists app_ver(name text,unique(name));
	</statement>
	<statement>
		-- to inforce upgrade db
		insert or ignore into app_ver (name) values ('1.10154');
	</statement>
	--2024-12
	<statement>alter table app_ver add date_ text;</statement>
	<statement>update app_ver set date_=strftime('%Y-%m-%d','now')  where date_ is null;</statement>
	--########End Upgrade DB ver

</sql>